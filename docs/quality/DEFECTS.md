---
type: defect-log
status: active
owner: project
last_verified: 2026-08-03
related:
  - "[[docs/README]]"
  - "[[docs/quality/TESTING]]"
---

# Дефекты

Обнаруженные дефекты не удаляются: после исправления запись переносится в
соответствующий раздел с датой и commit.

## Open

Найдены архитектурным ревью и код-ревью 2026-08-02, см.
[[docs/reviews/REVIEW_2026-08-02_ARCHITECTURE]] и [[docs/reviews/CODE_REVIEW_1c_2026-08-02]].
Записи 276–280 найдены ревью 2026-08-03, см. [[docs/reviews/REVIEW_2026-08-03_AUDIT]].
Решение принято 2026-08-03: 277–280 исправлены, 276 закрыт как `Won't Fix`.

| # | Title | Discovered | Component | Description |
|---|---|---|---|---|
| 245 | Матрица готовности проверяется как таблица, а не как критерий | 2026-08-02 | `scripts/test-1c-readiness.py` | Сверяются формулировки с мастер-планом и существование названного теста; совпадение критерия с кодом не проверяется. Частично снято: критерий 7 переведён в `частично` (№252), критерий 8 подкреплён вызываемым исполнителем (№243), а `частично` теперь допускает несколько тестов при одном дефекте, поэтому статус больше не приходится завышать из-за формы проверки. Остаётся класс: ничто машинно не связывает формулировку критерия с кодом, который её выполняет. |
| 252 | `<PROJECT_ROOT>` в профилях запуска не подставляется никем | 2026-08-02 | `templates/new-project/capabilities/1c/launch/` | Оба bootstrap подставляют три плейсхолдера — имя проекта, дату и версию схемы; `<PROJECT_ROOT>` в набор не входит и переменной EDT не является (у Eclipse `${project_loc}`). Проверено на созданном проекте: `configurations/launch/toolkit.launch` содержит литеральный `<PROJECT_ROOT>` в `/Execute`. Дефект №241 записан как «путь исправлен на внутрипроектный», фактически абсолютный путь заменён на нерезолвимый плейсхолдер; `EDT_SETUP.md` описывает результат как рабочий и не говорит, что строку надо дописать руками. Сами обработки доставляются корректно, байт в байт. Решение о пути отложено до проверки на машине с EDT: угадывать, что резолвит `ATTR_STARTUP_OPTION`, без 1С нельзя. До тех пор `doctor-1c` сообщает такой профиль как ненастроенный (№253), а критерий готовности 7 держится в `частично`. |

## Fixed

Записи, исправленные до релиза `v1.10.0` (№ 1–3, 5–27), перенесены при
консолидации в [[docs/quality/DEFECTS_ARCHIVE|архив дефектов]] без изменения
содержимого. Нумерация сквозная и не переиспользуется.

| # | Title | Discovered | Fixed | Commit | Root Cause |
|---|---|---|---|---|---|
| 280 | Runtime capability не сверялся с установленным release | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Поставка версионирована до байта — `release_id` есть хэш паспорта и ledger вместе, — а runtime не сверялся ни с чем. `one_c_session`, `one_c_doctor` и рендерер клиентских проекций запускаются из чекаута стандарта против каталога проекта; их обновляют в разные дни, и ничто их не сравнивало. Скрипты, решающие, можно ли писать в живую базу, могли быть другой версией стандарта, чем та, что проект установил. Строгость версий заканчивалась ровно там, где начинается действие на живой системе. Общий `one_c_release_guard` сверяет `capability_releases` проекта с `config/1c-release.json` чекаута; отказ — там, где пишут (замок сессии, рендерер), строка отчёта — там, где только читают (диагностика никогда не валит прогон). Два состояния намеренно не считаются несовпадением: capability в проекте не установлена и паспорт чекаута нечитаем — второе называется поломкой чекаута, а не виной проекта. Код-ревью правки нашло в ней две собственные ошибки до слияния. Барьер стоял до разбора команды, поэтому отказ получали и `release`, и `show`: пользователь с несовпадающим чекаутом не мог снять устаревший замок иначе как удалив файл руками — барьер запирал выход. Обе команды выведены из-под него: одна только снимает состояние, вторая только печатает. Второе: в рендерере клиентских проекций барьер срабатывал и без `--write`, тогда как комментарий рядом обещал защиту именно записи — расхождение проверки со своим же описанием, то есть ровно тот класс, который это ревью закрывало весь день. Теперь пишущий режим отказывает, режим отчёта сообщает и продолжает, как и диагностика. |
| 279 | Разрешение на перераспространение upstream нигде не записано | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | 161 из 240 строк доставки `1c` — код `comol/ai_rules_1c @ 1b6e2ed`, и copyright у него не оформлен: `license: null`, файла `LICENSE` нет. Разрешение при этом дано явно — раздел «Лицензия» в README: «Никакой лицензии, берите и используйте как хотите» (проверено 2026-08-03). То есть право есть, а у нас оно не было записано ничем, и читатель репозитория видел чужой код без основания. Добавлен `NOTICE.md` в поддереве: источник, коммит, цитата, дата прочтения и что именно мы с файлами делаем. Тест сверяет коммит в NOTICE с пином в `config/1c-upstream-inventory.txt`, поэтому обновление пина не может оставить NOTICE описывающим другой релиз. Просьба к автору оформить MIT или CC0 рассматривалась и закрыта 2026-08-04 без отправки: разрешение уже дано явно и зафиксировано у нас с датой прочтения, а обращение в чужой публичный репозиторий — отдельное действие, которое стандарту ничего не добавляет. Если условия upstream когда-нибудь изменятся, отправной точкой служит NOTICE с датой, на которую они читались. |
| 278 | Доменная схема 1С была зашита в ядровой валидатор | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Колонки реестра баз, enum-ы и диапазон портов жили в `validate-project.py`, который отвечает на вопрос «соответствует ли проект своему профилю». Вторая capability со своим реестром добавила бы второй такой блок в файл, который не должен знать ни о первой, ни о второй. Блок вынесен в `scripts/one_c_validation.py`, ядро вызывает его через `CAPABILITY_VALIDATORS` — одна строка на capability — и превращает кортежи в findings. `machine_path` переехал в `validate_project_support` как факт о путях, а не о capability. Намеренно сделан только шов: DSL валидации под второго потребителя, которого пока нет, не вводится. Тест запрещает ядру называть `ONE_C_*` и `config/1c-*`. |
| 277 | Ядро capability было объявлено и в коде, и в конфиге | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | `CAPABILITY_CORE` и `CAPABILITY_NAMES` в `project_metadata.py` дублировали `config/capability-core.tsv`, который читают оба bootstrap, а согласованность держал тест. Тест, сравнивающий две копии, находит расхождение после того, как оно возникло; один источник не может разойтись сам с собой. Третий рецидив класса №232/№234. Модуль читает манифест, литералы удалены, `CAPABILITY_NAMES` выводится из него. Тест переписан с «копии совпадают» на «источник один»: манифест подменяется на другой, и ответ обязан измениться — литерал такую проверку не проходит. |
| 283 | CI на Windows шёл 13.5 минут при 1.5 на Ubuntu | 2026-08-03 | 2026-08-04 | ветки `perf/fewer-spawns-per-file`, `perf/shard-the-windows-suite` | Замерено до правок: 11 из 13.5 минут — один шаг с 31 python-набором подряд, при 1 м 28 с тех же наборов на Ubuntu. Первая гипотеза — антивирус — **не подтвердилась**: исключение применилось, время не изменилось (688 → 729 с, в пределах разброса), и та ветка закрыта, а не влита, потому что шаг с комментарием об ускорении, которого нет, — тот же класс, что №211, №212, №225 и №268. Настоящих причин оказалось две. Первая: доставка порождала два процесса на каждый файл (`dirname` и безусловный `mkdir -p`) при 705 файлах capability `1c`, а в git-bash на Windows запуск процесса дорог — снято раскрытием параметра и кэшем созданных каталогов, доставка проверена побайтно (259 файлов, SHA-256 один в один). Вторая: наборы шли одной очередью — разложены по трём шардам, `powershell` сохранён как обязательный контекст-агрегатор над `windows-suite` и `windows-native`. Итог 13 м 27 с → 5–6 мин. Попутно исчезли три рукописных списка тестов: набором делает имя файла, что закрывает класс №228/№259 по построению. Дальше не пошли осознанно: `test-preset-core` — 318 из 606 с и новый пол, а удешевлять его можно только сокращая проверки поставки, то есть меняя быстрый CI на CI, который перестал ловить. Пересмотреть, если этот тест станет мешать. |
| 282 | Взаимные пины делали одновременную актуальность невозможной | 2026-08-03 | 2026-08-03 | ветки `fix/pin-drift-is-a-note` | Найден доведением собственной правки до конца. NPR пинит BP, BP пинит NPR, и оба сторожа считали «пин отстал от main» ошибкой. Обновление любого пина — это коммит, то есть сдвиг main своего репозитория, после которого отстаёт пин соседа: пара не может быть актуальной одновременно ни в один момент, и отчёт красный при любых действиях. Ровно то состояние, которое приучает игнорировать проверку, — и проект уже принял это решение для другого upstream: «report only, and deliberately not a gate», решение 1.24. `bp-pin-watch` ему противоречил с самого начала, а построенный вчера `check_npr_pin` скопировал нарушение. Отставание стало NOTE с кодом возврата 0 в обоих; ошибками остались нерезолвимая ревизия, отсутствие исполняемого скрипта в ней и расхождение хэшей закреплённого интерфейса — то, ради чего сторожа и написаны. |
| 281 | Предупреждение о размере CHANGELOG называло нерабочее средство | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Найден применением инструмента к самому стандарту. Дефект №251 закрыл случай «весь объём в `Unreleased`»; остался третий: самый новый релиз никогда не архивируется, поэтому когда он один превышает порог, совет «понизить `--changelog-keep`» снова отправляет к настройке, которая ничего не меняет. Проверено на месте: после нарезки релиза `v1.17.0` занял 48 КБ из 60, и понижение порога с 5 до 3 не сдвинуло ни байта. Сообщение теперь называет релиз, его размер и настоящее средство — выпускать чаще. |
| 275 | `git_commit_all` выбрасывал `CalledProcessError` мимо обработчика | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Вызов идёт после копирования файлов, поэтому отказ git — это невыполненный шаг, а не несостоявшийся запуск. `main` ловит `OSError` и свои исключения, и пользователь вместо отчёта о фактическом состоянии проекта получал traceback. Ошибка git теперь переводится в `StandardizationApplyError` с последней строкой её вывода. |
| 274 | `one_c_provider` возвращал 0, когда все роли отвечают FAIL | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Ноль оправдан для неразвёрнутого провайдера — это не сломанный проект. Но тот же ноль возвращался при развёрнутом провайдере, у которого health-check не прошёл ни по одной роли: по коду возврата два разных ответа стали неразличимы, и вызывающая сторона слышала «всё в порядке» о проваленной проверке. Отсутствие по-прежнему 0, полный отказ — 1. |
| 273 | Не-UTF-8 глобальный файл политики ронял весь валидатор | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | `sync_global_agents` ловил при чтении только `OSError`, а `UnicodeDecodeError` проходил насквозь; `validate-project.check_global_rules` ждёт `SyncConfigError`, поэтому один нечитаемый файл вне репозитория обрушивал проверку всего проекта. |
| 272 | Замок сессии: `created_at` без защиты типа и общий tmp-файл | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | `float(lock.get("created_at", 0))` на отредактированном руками замке давал traceback вместо `SessionError`. Staging-файл назывался фиксированно, поэтому два процесса в одном чекауте затирали запись друг друга — в `plan_migration.atomic_write` для этого давно используется имя с pid. |
| 271 | CI не проверял заявленный минимум Python | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | `TOOLS.md` и `MIN_PYTHON` объявляют 3.9, а в `.github/workflows/` не было ни одного `setup-python`: джобы брали то, что даёт runner (3.12+). Конструкция новее заявленного порога прошла бы CI и упала бы на машине пользователя. Джоба `shell` закреплена на 3.9, остальные намеренно оставлены на умолчании runner'а, чтобы проверялись оба конца диапазона. |
| 270 | `reflect-and-record` уводил кросс-проектные уроки в maintainer-only тупик | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Скилл срабатывает на каждую ошибку и поправку и отправлял урок в `promote-project-knowledge`, который maintainer-only и принимает только **уже принятую** практику Best Practices — свежий урок не удовлетворяет его предусловиям никогда. `harvest-practice-candidates` не назывался ни разу. Следствие измеримо: 368 коммитов NPR за месяц дали ноль кандидатов в BP. Маршрут переписан на пользовательский, инвариант в `test-check-skills.py` запрещает отправлять пользователя в maintainer-only ворота. |
| 269 | TSV-читатели трактовали кавычку как разметку | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Реестр баз читался с `QUOTE_NONE`, а `capabilities.tsv`, `profiles.tsv`, `migrations.tsv`, `policy-contract.tsv` и остальные — с умолчаниями csv: кавычки в значении молча съедались, а незакрытая кавычка поглощала табуляции и сдвигала строку. Часть читателей ловила сдвиг проверкой на `None`, часть — нет: `Artifact(**row)` принимал `docs_section=None`. Все 16 читателей переведены на `QUOTE_NONE`; в `release_manifest` кавычка проверяется по сырой строке, чтобы диагноз остался конкретным. |
| 268 | Диагностика печатала значения ключей, о которых заявляла обратное | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Строка отчёта утверждает «значения не читаются — только имя ключа и признак задан», а `mask()` возвращал строку без изменений, если имя не содержало маркера. `EDT_WORKSPACE` и путь к manifest провайдера уходили в отчёт полностью — те самые machine-specific пути, которые остальной код специально скрывает. Список маркеров всегда отстаёт от имён, которые изобретает проект, поэтому правило инвертировано: значение не печатается никогда. Продолжение №212. |
| 267 | Диагностика читала реестр без `utf-8-sig` и молча роняла строки | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Реестр правят в табличном редакторе, который пишет BOM; все остальные читатели открывают файл `utf-8-sig`, а `one_c_doctor.read` — нет. BOM оставался на первой ячейке заголовка, `project_id` становился ключом, которого никто не ищет, и каждая база в отчёте теряла имя — при строке «реестр баз OK». Строка с неверным числом колонок отбрасывалась без сообщения, что читается как «эта база в порядке», а не «эту базу не смотрели»; теперь она называется. |
| 266 | Компрессия переписывала концы строк во всём файле и писала не атомарно | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | `read_text` нормализовал CRLF в `\n`, `write_text` транслировал обратно в `os.linesep`: перенос трёх записей между двумя файлами давал диff на весь файл — на Windows в одну сторону, на macOS в другую. Плюс запись шла поверх исходника без staging: падение между урезанным журналом и его архивом теряло переносимые записи. Чтение и запись переведены на `newline=""`, запись — на staging с `os.replace`. Побочный урок, пойманный Windows-джобой ещё в ветке: отказ от нормализации на чтении сдвигает условия для **каждого** читателя этого текста. `frontmatter_field` сверял начало документа с `---\n`, что было верно ровно потому, что CRLF снимался при чтении; на Windows любой документ стал открываться `---\r\n` и молча считаться документом без frontmatter. Предикат переписан на сравнение первой строки, регрессия закреплена CRLF-фикстурой и потому проверяется на всех платформах, а не только там, где сломалось. |
| 265 | Откат после сбоя записи ledger не удалял созданные каталоги | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Вторая ветка `except` восстанавливала файлы, но, в отличие от первой, оставляла созданные пустые каталоги: «проект не тронут» было верно про файлы и неверно про дерево. |
| 264 | `verify_preconditions` не проверял ни `adopt`, ни project records | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Проверялись только `create`, `update` и `remove`. `adopt` записывал в ledger хэш релиза для того, чем файл стал между планом и применением, — состояние, в котором проект никогда не был. Документы проекта (`INDEX.md`, `docs/README.md`, `.project-standard.json`) строятся **из** текущего содержимого файла и записывались поверх правки пользователя без слова — ровно тот молчаливый перезапис, ради запрета которого модуль и написан. План теперь запоминает baseline каждого документа и отказывается применяться, если файл изменился. |
| 263 | `splitlines()` терял символы и переписывал концы строк индекса | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | `docs_index_document` разбирал файл `splitlines()` и собирал одним выбранным окончанием: одна CRLF-строка переводила в CRLF весь файл, а form feed и прочие символы, которые `splitlines` считает переносами, исчезали — при докстринге, обещающем побайтную сохранность вокруг собственной правки. Разбор переведён на `keepends`, новая строка берёт окончание соседней. Код-ревью этой же правки нашло в ней регрессию до слияния: у файла без перевода строки в конце новая запись приклеивалась к предыдущей — две ссылки оказывались в одной строке без разделителя, — а прежняя сборка это скрывала, потому что переписывала окончания всем строкам. Тот же урок, что в №266: отказ от нормализации возвращает к жизни все краевые случаи, которые она скрывала, и их надо перепроверить поимённо. Фикстура добавлена в обеих кодировках концов строк. |
| 262 | Более длинная ссылка скрывала более короткую | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Наличие ссылки проверялось префиксом `[[<link>`, поэтому индекс с `[[docs/quality/DEFECTS_ARCHIVE\|…]]` считался уже ссылающимся на `docs/quality/DEFECTS`. Установщик не добавлял запись никогда, а валидатор не сообщал о её отсутствии — оба задавали один и тот же неверный вопрос. Предикат `link_present` определён один раз в `validate_project_support` и требует после имени `]`, `\|` или `#`. |
| 261 | `--plan-rebootstrap --json` падал `ValueError` вместо плана | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | План ре-bootstrap пишет под `--destination`, который по контракту лежит **вне** оцениваемого корня, поэтому `relative_to(root)` там бросает. Текстовый рендер это учитывал, JSON — нет, и падал именно тот режим, который существует ради чтения другой программой. Оба рендера ходят через общий `display_path`. |
| 260 | `text=True` без явной кодировки в 69 местах | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Дефект №231 был этим же и был закрыт в двух местах, где его заметили. `text=True` декодирует локальной кодировкой; на Windows это ANSI code page, а git отвечает UTF-8, и кириллица не падает, а возвращается искажённой: `rev-parse --show-toplevel` перестаёт совпадать с путём, и репозиторий выглядит не корнем репозитория. Корень самого проекта — кириллический. Кодировка проставлена всем 69 вызовам, включая тестовую обвязку, а инвариант в `test-supply-chain.py` запрещает возврат класса. |
| 259 | Три рукописных списка тестов в CI без инварианта | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Регрессионный набор перечислен трижды — джоба `shell`, джоба `powershell` и `macos-smoke.yml`. Дефекты №228 и №229 — один класс: №229 закрыт инвариантом на документацию, №228 (пять тестов, не запускавшихся на macOS) — правкой списка, то есть вниманием. Новый тест мог попасть в `TESTING.md` и `INDEX.md` и не попасть ни в одну джобу. Инвариант в `test-check-skills.py` требует каждый `scripts/test-*.py` во всех трёх списках; исключение обязано назвать workflow, который его запускает. |
| 258 | Барьеры сессии 1С сравнивали ячейку реестра с литералом нижнего регистра | 2026-08-03 | 2026-08-03 | ветка `fix/audit-2026-08-03` | Все проверки сверяли `row.get("is_production")` с `"true"` и `application_kind` с `"managed"`. Реестр — TSV, который правят в табличном редакторе, а он возвращает `True` и `Managed`: сравнение не совпадало, строка читалась как непроизводственная, и `approved-write` выдавался на production-базу, а managed-база пропускала требование свидетельства о переключателе записи — свой единственный барьер. Воспроизведено: `acquire(..., write_mode="approved-write")` на `is_production=True` выдавал замок с `switch_read=''`. Enum-проверка в валидаторе — отдельный запуск и барьером быть не может. Чтение нормализовано, значение вне enum отвергается: база, чей production-статус нечитаем, считается небезопасной, а не безопасной. |
| 217 | `1c-metadata-manage` назван skill'ом, но для Claude Code им не является | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Три скилла называют его жёстким гейтом, а `bridge` у всех десяти vendored-скиллов был `none`: гейт, который в половине клиентов нельзя соблюсти. Мосты созданы для всех десяти. По решению S.9.1 они лежат вне побайтного subtree, поэтому проверка моста теперь определяет каталог capability, а не отсчитывает два уровня вверх. |
| 216 | `ONE_C_WORKSPACE.md` обещает заполнение скиллом, который его не трогает | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | `add-1c-base` ведёт реестр `config/1c-projects.tsv`, а этот обзорный файл не открывает. Текст разведён: реестр за скиллом, обзор за человеком. |
| 214 | `deploy-and-test-1c` читает флаг вместо каталога компонентов | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Скилл утверждал, что состояние YAxUnit берётся из каталога компонентов, тогда как `--not-installed` — ручной флаг. Каталог знает, что компонент объявлен, но не проверяет его на этой машине. Формулировка приведена к факту: состояние выясняет `doctor-1c`, а флаг — осознанное утверждение человека. |
| 213 | `transcribe` обещает три файла, пишет один | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | В `OUTPUTS` числились `summary` и `detailed`, которых скрипт не пишет: суммаризатора у него нет, и сам скилл говорит, что summary пишет AI-клиент по готовой расшифровке. Ожидание трёх файлов было ожиданием того, чего никто не создаст. `OUTPUTS` оставлен один, скилл в дереве результатов помечает, что пишет скрипт, а что клиент. Попутно закрыт второй разрыв: `--format json` писал JSON в файл с расширением `.md` — расширение теперь следует формату, а неизвестный формат отвергается до записи. |
| 212 | `one_c_doctor.settings()` маскирует по содержимому, а не по имени ключа | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | Решение принималось по всей строке и по списку из пяти маркеров, поэтому `DB_PWD`, `ONEC_CREDENTIAL` и `BASE_LOGIN` печатались целиком. Теперь имя ключа и значение разбираются раздельно: маскируется ключ, чьё имя называет учётные данные, и значение, похожее на строку соединения. Тест, закреплявший утечку, расширен этими тремя ключами и падает при возврате прежнего списка маркеров. |
| 211 | `one_c_doctor.occupied()` на Windows не видит слушателя на `0.0.0.0` | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | Проба привязывалась только к `127.0.0.1`, поэтому порт, занятый на всех интерфейсах, считался свободным — ровно случай, ради которого проверка написана: платформа 1С слушает `6003` на `0.0.0.0`. Теперь спрашиваются оба адреса, а на Windows вместо `SO_REUSEADDR` ставится `SO_EXCLUSIVEADDRUSE`: первый там позволяет привязаться поверх активного сокета и делает пробу бессмысленной. Тест на «занят на всех интерфейсах» на POSIX не различает реализации, поэтому добавлен структурный: проба обязана спросить про каждый интерфейс, и он падает на старом коде. |
| 210 | Нет теста на связку профиль ↔ `docs_section` capability | 2026-08-01 | 2026-08-02 | ветка `fix/review-blocking` | №205 был исправлен данными — обеим capability подняли минимальный профиль, — но класс ошибки оставался открытым: ничто не проверяло саму связку. Инвариант добавлен в `test-preset-core.py`, где полный bootstrap не нужен: capability с непустым `docs_section` обязана требовать профиль не ниже `software`, на котором появляется `docs/README.md`. Проверено откатом — при возврате `jira-confluence` на `minimal` тест падает именно этой строкой. |
| 92 | План не фиксирует формат 1С-исходников для metadata tooling | 2026-07-25 | Не дефект при принятом контракте минимальной адаптации. `1c-metadata-manage` переносится без изменения и работает только с ожидаемым им XML-деревом; наличие `Configuration.xml` и нужной структуры проверяют сами upstream tools. EDT workflow остаётся отдельным владельцем и не требует общего `source_format`. Решение 2026-07-25: поле и compatibility router не вводятся. |
| 87 | Provider-shared MCP не имеют согласованной модели state/mount isolation | 2026-07-25 | Не дефект consumer capability: S.4.4 уже назначил внешний MCP-проект владельцем runtime, портов, mounts, индексов и container lifecycle. Повторное проектирование provider internals расширяло scope и дублировало внешнюю систему; наш контракт проверяет только обнаруженный endpoint, identity, health и tools. Решение 2026-07-25: отдельный S.4.5 не реализуется. |
| 33 | PowerShell bootstrap-тест падает локально на macOS в mock-git секции | 2026-07-02 | Дефект тестовой обвязки, а не продукта: pwsh на macOS кеширует резолв `git` в удалённый `mock-bin` путь из временного PATH (command caching). CI (Windows PowerShell 5.1 / pwsh 7 на windows-latest) сценарий не воспроизводит и полностью покрывает; профильные проверки скрипта проходят. Решение 2026-07-03: не чинить. |
| 4 | POSIX symlink-тесты падают в Git Bash без symlink support | 2026-06-28 | Git Bash `ln -s` на этой Windows копирует файл, поэтому resolver-тесты получают неверный каталог. Windows поддерживается PowerShell-набором; POSIX symlink-сценарии выполняются в macOS/Linux и CI. |

## Won't Fix

| # | Title | Discovered | Reason |
|---|---|---|---|
| 276 | Масса capability `1c` превышает массу самого стандарта | 2026-08-03 | Факт верен: 243 из 291 файла `templates/` и 240 из 265 строк манифеста поставки — это `1c`, при 24 файлах собственно шаблонов проекта, и 161 из них вендорится из upstream. Предлагалось вынести upstream в получение по коммиту при установке и, в перспективе, capability в отдельный репозиторий. Отклонено 2026-08-03. Вендоринг здесь не побочный эффект, а свойство: запиненные байты, ledger, побайтная сверка и **полностью офлайновый bootstrap**. Замена на сетевую загрузку меняет реальное свойство на счётчик файлов, а при отсутствии формальной лицензии у upstream (№279) добавляет риск: исчезнувший или переписанный источник ломает установку capability, которая раньше работала. Заявленные издержки — время CI, поверхность ревью, когнитивная нагрузка — к инертному payload, который проверяется хэшем и который никто не читает, почти не применимы; 2.8 МБ. Отдельный репозиторий — второй CI, второй пин и третий межрепозиторный контракт при одном сопровождающем. Реальная цена массы лежала не в `templates/`, а в ядре, и это №277 и №278: оба закрыты. Пересмотреть, если появится вторая доменная capability сопоставимого размера. |
