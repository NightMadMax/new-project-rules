---
type: defect-log
status: active
owner: project
last_verified: 2026-07-26
related:
  - "[[docs/README]]"
  - "[[docs/quality/TESTING]]"
---

# Дефекты

Обнаруженные дефекты не удаляются: после исправления запись переносится в
соответствующий раздел с датой и commit.

## Open

| # | Title | Discovered | Component | Description |
|---|---|---|---|---|
| 147 | `test-bootstrap.ps1` падает на macOS с PowerShell 7 на mock-git | 2026-07-26 | `scripts/test-bootstrap.ps1` / mock Git wrapper | Блок «Git identity and failure handling» подменяет `git` shell-обёрткой в `mock-bin` и добавляет её в `PATH`, после чего вызов `& git -C $Dir status --porcelain` (строка 65) падает с `Program 'git' failed to run … mock-bin/git: No such file or directory`. Воспроизводится и на `main`, то есть дефект не связан с этапом Э3; на Windows CI тот же тест зелёный (PR #25 и #26), поэтому регрессии в поставке нет. Ранее в этой же сессии тест проходил, значит поведение зависит от окружения или порядка запусков. Нужно разобраться, почему обёртка не исполняется под pwsh на macOS: права, shebang, кэш разрешения команд PowerShell или очистка временного каталога. |
| 146 | Reference-файлы skills capability не упомянуты ни в одном `SKILL.md` | 2026-07-26 | `templates/new-project/capabilities/jira-confluence` | `references/configuration.md` и `references/models.md` поставляются в проект строками `config/capabilities.tsv`, но после удаления scaffolding (№143) видно, что ни один `SKILL.md` на них не ссылается: агент их не откроет. Нужно либо сослаться из соответствующего skill, либо убрать из поставки. Проверка `check_skills.py` такие осиротевшие файлы не ловит. |
| 145 | Skills capability не получают Claude-мост в созданном проекте | 2026-07-26 | `config/capabilities.tsv` / bootstrap | `capabilities.tsv` доставляет только `.agents/skills/**`, а `.claude/skills/<name>/SKILL.md` для `setup-atlassian-mcp` и `work-with-jira-confluence` не создаётся ни манифестом, ни bootstrap. В созданном проекте эти skills видит Codex, но не Claude Code, хотя мастер-план требует мост для обоих клиентов. В `config/skills.tsv` это зафиксировано значением `bridge=none`, то есть проверка соответствует факту, а не требованию. |
| 104 | Ошибка ffmpeg split может дать пустую транскрипцию с успешным завершением | 2026-07-25 | Общий skill `transcribe` / [[docs/research/GENERAL_TRANSCRIBE_SKILL_PLAN]] | `split_media()` не проверяет return code ffmpeg. Если длительность больше часа, но ни одна часть не создана, возвращается пустой список; generic flow затем записывает пустой transcript и сообщает об успехе. Нужен fail-closed split contract. |
| 103 | Runtime/provider contract `transcribe` невоспроизводим | 2026-07-25 | Общий skill `transcribe` / [[docs/research/GENERAL_TRANSCRIBE_SKILL_PLAN]] | `google-genai`, `python-dotenv`, ffmpeg/ffprobe и hardcoded Gemini model не имеют release lock/preflight, а cost estimate со временем меняется. Нужны pinned/tested dependencies, model ownership, общий doctor и отдельные строки пользовательской документации. |
| 102 | `transcribe` не гарантирует безопасный lifecycle загруженного Gemini media | 2026-07-25 | Общий skill `transcribe` / [[docs/research/GENERAL_TRANSCRIBE_SKILL_PLAN]] | Скрипт отправляет пользовательское аудио/видео во внешний Gemini File API, но не делает отдельный disclosure/confirmation; remote delete вызывается не в `finally`, processing poll не имеет timeout, а sync SDK client явно не закрывается. При ошибке файл может остаться у provider, локальный HTTP-resource — открытым, или задача зависнуть. Нужны локальный default либо явное разрешение, bounded wait, guaranteed remote cleanup/report и client close. |
| 101 | Документированные имена output `transcribe` не совпадают со скриптом | 2026-07-25 | Общий skill `transcribe` / [[docs/research/GENERAL_TRANSCRIBE_SKILL_PLAN]] | `SKILL.md` обещает suffixes `transcript`, `summary`, `detailed` и каталог `Transcript`, тогда как Python пишет `транскрипция`, `саммари`, `детальный` и `Транскрипция`. Пользователь и последующий agent не могут надёжно определить результат; нужен один канонический контракт. |
| 61 | Проверка instruction sources не запускает packaged Codex Desktop на Windows | 2026-07-14 | `create-new-project` / instruction-source verification | При создании `favorit-web` команда `codex --cd <destination> --ask-for-approval never exec ...` разрешилась в `WindowsApps/.../codex.exe`, но PowerShell не смог запустить executable: `Отказано в доступе`. Workflow считает наличие команды достаточным и не различает invocable CLI и packaged app executable. Нужен переносимый preflight/launcher fallback либо явный статус skipped с диагностикой, покрытый Windows-тестом. Это отдельный дефект от №56: subcommand `exec` указан правильно. Уточнение 2026-07-16: на той же машине рабочий бинарник существует по прямому пути (`~/.codex/plugins/.plugin-appserver/codex.exe`, codex-cli 0.144.2) и проверка через него проходит — fallback реализуем, дефект именно в discovery, а не в отсутствии CLI. Уточнение 2026-07-26: исправление отложено до сессии на Windows, где поведение App Execution Alias проверяется фактически; до этого дефект остаётся предусловием контрактов `doctor-1c` и `activate-1c-client` (запись №137). |
| 126 | Инвариант ядра противоречит порядку шагов создания проекта | 2026-07-25 | План capability `1c` / `.agents/skills/create-new-project/SKILL.md` | План делает наличие стека `1c` в `.best-practices.json` частью машинно проверяемого ядра (`ONE_C_CAPABILITY_PLAN.md:48`, `:1113`, `:1797`), но `create-new-project/SKILL.md:51` запускает валидатор на шаге 6, а `.best-practices.json` создаётся позже (`:70`–`:97`) и условно: при отказе пишется `optout`, при недоступности базы шаг откладывается, стек разрешено не выбирать. Валидация ядра в текущем порядке либо всегда падает, либо не проверяет треть ядра. Уточнение 2026-07-26: контракт зафиксирован в [[docs/architecture/one-c/DELIVERY_AND_STATE_PLAN\|подплане доставки и состояния]]; реализация ждёт итогового утверждения мастер-плана. |
| 127 | `.best-practices.json` хранит preferences, а не реестр стеков, и стек `1c` пуст | 2026-07-25 | План capability `1c` / `scripts/best_practices_manifest.py` | Манифест хранит preferences `ask`/`optout` (`scripts/best_practices_manifest.py:14`, `:73`), где `--stack` лишь пишет значение, совпадающее с глобальным умолчанием, поэтому проверка «стек `1c` присутствует» не отличает подключённый стек от явного отказа; `scripts/validate-project.py` этот файл сегодня вообще не читает. При этом план объявляет Best Practices `1c` обязательной частью ядра и pinned-источником release (`ONE_C_CAPABILITY_PLAN.md:48`, `:224`, `:1074`), а соседняя база содержит только `README.md` с текстом «Принятых практик пока нет». Уточнение 2026-07-26: контракт зафиксирован в [[docs/architecture/one-c/DELIVERY_AND_STATE_PLAN\|подплане доставки и состояния]]; реализация ждёт итогового утверждения мастер-плана. |
| 137 | План опирается на discovery AI-клиента, сломанный открытым дефектом №61 | 2026-07-25 | План capability `1c` / `activate-1c-client` и `doctor-1c` | `activate-1c-client` первым шагом проверяет наличие и версию выбранного CLI (`ONE_C_CAPABILITY_PLAN.md:346`), `doctor-1c` по результатам discovery выдаёт команду активации (`:358`), на этом же построен критерий готовности (`:1937`). Открытый дефект №61 фиксирует, что на Windows команда `codex` разрешается в неисполнимый `WindowsApps` executable, а рабочий бинарник доступен по прямому пути; Windows при этом объявлен базовой средой capability (`one-c/ENVIRONMENT_SETUP_PLAN.md:21`). Новые контракты воспроизведут уже известный ложный вывод. Уточнение 2026-07-26: решением пользователя закрытие №61 выполняется на Windows-машине; до этого contract-требование «discovery проверяет запускаемость, а не наличие имени» остаётся невыполненным. |

## Fixed

Записи, исправленные до релиза `v1.10.0` (№ 1–3, 5–27), перенесены при
консолидации в [[docs/quality/DEFECTS_ARCHIVE|архив дефектов]] без изменения
содержимого. Нумерация сквозная и не переиспользуется.

| # | Title | Discovered | Fixed | Commit | Root Cause |
|---|---|---|---|---|---|
| 125 | SemVer capability и `release_id` не отображаются на движок миграций | 2026-07-25 | 2026-07-26 | `3755deb` | Движок знает только целочисленные переходы schema, поэтому история релизов capability в него не укладывалась. Оси разведены: артефакты capability планируются сравнением фактического состояния с ledger, а не цепочкой schema; `plan_migration.py` признаёт handler `capability_artifacts` как внешний и не пытается вести его сам. |
| 122 | Обработчик миграций `capability_artifacts` не существует | 2026-07-25 | 2026-07-26 | `3755deb` | Доставка обновлений в созданные проекты держалась на несуществующем обработчике. Добавлен `scripts/capability_artifacts.py`: многофайловый план (`create`/`update`/`remove`/`skip`), staging с переименованием и откатом всей цепочки при отказе, остановка на дрейфе вместо перезаписи, seed создаётся один раз и не удаляется, ledger обновляется только после успешной записи. CLI `apply-capability-artifacts` read-only по умолчанию и требует `--apply --yes`. |
| 124 | Механика доставки capability не поддерживает каталоги, бинарники и смешанные файлы | 2026-07-25 | 2026-07-26 | `a1ab42e` | Единственным способом доставки была текстовая подстановка `sed`/`Replace`, поэтому побайтный payload и бинарные артефакты повреждались бы при установке. В `config/capabilities.tsv` добавлена колонка `payload_class`: `-`/`template` сохраняют прежнее поведение, `verbatim` и `binary` копируются побайтно в обоих bootstrap. Неизвестный класс останавливает установку. Каталоги остаются вне манифеста по решению подплана: subtree разворачивается в строки на build-time. |
| 128 | Запись release в `.project-standard.json` требует bump схемы, не запланированного в этапах | 2026-07-25 | 2026-07-26 | текущий commit | Формат metadata менялся без повышения `STANDARD_VERSION`, из-за чего `validate_metadata` отклонил бы проект. Схема повышена до 5, добавлено поле `capability_releases` с проверкой SemVer и `release_id`, `1c` внесён в `CAPABILITY_NAMES`, добавлены миграции 0013–0015 и тест апгрейда схемы 4 → 5 с проверкой валидности результата. |
| 123 | Generic-механизм `.project-standard-artifacts.json` не спроектирован | 2026-07-25 | 2026-07-26 | текущий commit | Ledger существовал только в тексте плана: без формата, владельца схемы и валидатора. Введены `scripts/artifacts_ledger.py` со схемой 1 и правилами policy/payload_class/hash, раздел в документе схемы, проверка в `validate-project.py` для проектов и отдельная тестовая сюита. |
| 129 | Проверки skills не масштабируются на capability-скиллы и vendored subtrees | 2026-07-25 | 2026-07-26 | текущий commit | Список из девяти имён был захардкожен в обоих entry point, а обязательная триада с внутренним `agents/openai.yaml` не подходит побайтным vendored-скиллам. Введён реестр `config/skills.tsv` и общая проверка `scripts/check_skills.py` с двумя классами; обход дополнен сверкой фактических каталогов и `config/capabilities.tsv`, поэтому забытая строка реестра теперь ошибка, а не тихий пропуск. |
| 144 | UI-метаданные skills capability записаны в CP1251 и превращаются в мусор | 2026-07-26 | 2026-07-26 | текущий commit | `agents/openai.yaml` обоих skills `jira-confluence` содержали `short_description` в кодировке CP1251, поэтому в интерфейсе клиента отображалась бы нечитаемая строка, а чтение файла как UTF-8 падало с `UnicodeDecodeError`. Значения переписаны в ASCII по образцу собственных skills репозитория; проверка `check_skills.py` читает метаданные побайтно и валит ASCII-контракт вместо падения. Уточнение: исправлен шаблон; уже созданные проекты получат правку только после появления обработчика `capability_artifacts` (дефект №122). |
| 143 | В шаблоны skills capability попал неудалённый scaffolding skill-creator | 2026-07-26 | 2026-07-26 | текущий commit | `setup-atlassian-mcp/SKILL.md` и `work-with-jira-confluence/SKILL.md` содержали блок «Structuring This Skill» с инструкциями-заглушками и разделами `[TODO: ...]` — около 75 строк после реального содержания. Каждый проект, созданный с capability `jira-confluence`, получал этот мусор. Блок удалён; регресс ловится проверкой на `TODO`, которая раньше не применялась к capability-шаблонам, потому что они не были ни в одном тесте. Уточнение: исправлен шаблон; уже созданные проекты получат правку только после появления обработчика `capability_artifacts` (дефект №122). |
| 117 | План не фиксирует формат исходников конфигурации в Git | 2026-07-25 | 2026-07-26 | `2fbc80f` | Формат репозитория не был выбран, поэтому `export-1c-source`/`deploy-1c-source` не имели полного контракта, а две машины могли выгрузить одну базу по-разному. Решением 1.21 каноном выбран EDT-формат, введена колонка `source_format`, а XML-зависимые инструменты получили детерминированную конвертацию во временный gitignored каталог с явным шагом возврата и показом diff. Стоимость конвертации записана в остаточные риски. |
| 142 | Отложенное решение по hooks не пересмотрено при появлении сигнала | 2026-07-25 | 2026-07-26 | `cdf3696` | Условие возврата к отложенному решению не проверялось, хотя capability вводит необратимую запись в живую ИБ. Решением 1.27 вопрос разобран и hook отклонён с обоснованием: асимметрия Claude/Codex, исполняемая конфигурация как отдельный риск, дублирование принятых барьеров и слепота к переключению записи в UI. Остаточный риск на `managed` подтверждён как принятый. |
| 139 | Кандидат продвижения secret-safe inspection не учтён в диагностике `doctor-1c` | 2026-07-25 | 2026-07-26 | `cdf3696` | Контракт диагностики описывал только частный случай с токеном, тогда как `doctor-1c` читает файлы с допустимыми dev/test credentials. Решением 1.26 в план добавлен раздел про allowlist, запрет рекурсивного обхода профиля, исключение state/backup/session/logs и маскирование вывода; кандидат `PC-2026-e6f54a0fe78a` остаётся в backlog для отдельного решения о продвижении. |
| 136 | Release capability `1c` предписывает прямой push в защищённый `main` | 2026-07-25 | 2026-07-26 | `cdf3696` | План описывал прямой push как обычный шаг, умалчивая, что он проходит только через admin-bypass ruleset `Protect main`. Решением 1.25 маршрут сохранён, но назван bypass'ом и обязан сопровождаться записью в `ACTIONS.md` с перечнем локально прогнанных проверок. |
| 141 | Внешние URL каталога компонентов нигде не проверяются | 2026-07-25 | 2026-07-26 | `a8e7daf` | Адреса официальных источников установки были обычным текстом документа и протухали бы незаметно до момента установки. Решением 1.24 введена report-only проверка ссылок скриптом стандартной библиотеки, без новой зависимости и без блокировки сборки. |
| 140 | Бюджет цепочки инструкций проверяется только для native AGENTS | 2026-07-25 | 2026-07-26 | `a8e7daf` | Проверка учитывала только файлы AGENTS, тогда как companion-файлы попадают в контекст рекурсивным импортом и в лимит не входили. Решением 1.23 измеряется вся фактически загружаемая цепочка; порог 32 КиБ и проверка добавлены в этап 7 и критерии готовности. |
| 121 | Работа с типовой конфигурацией не отражена в модели данных проекта | 2026-07-25 | 2026-07-26 | `3fae528` | Режим поддержки и версия БСП существовали только как текст в user-owned `USER-RULES.md`, поэтому агент с write-доступом не мог проверить, допустима ли правка типовой. Решением 1.20 введена обязательная колонка `support_mode` в реестре баз, версия БСП добавлена в карточку базы, а правило «доработка расширением по умолчанию» перенесено в scoped `configurations/AGENTS.md`. Обновление через `cfu` вынесено в границы scope. |
| 120 | План не фиксирует границу scope для репозиторного lifecycle 1С | 2026-07-25 | 2026-07-26 | `3fae528` | Практики командной 1С-разработки не имели ни решения «включаем», ни решения «вне scope», из-за чего проект оставался без ответа, как код проверяется без агента. Решением 1.19 в v1 включены BSL Language Server как проверка репозитория вне MCP и модель ветвления/ревью; CI/CD, миграция из хранилища, SonarQube, АПК и `cfu` записаны как отказ с причиной. |
| 119 | Модель тестирования 1С объявлена, но не определена | 2026-07-25 | 2026-07-26 | `3fae528` | `TEST_MODEL.md`, `deploy-and-test-1c` и ключ `UI_TESTING` существовали без исполнителя: ни фреймворка, ни размещения тестов, ни контура. Решением 1.18 YAxUnit включён в v1 conditional-компонентом, описаны уровни проверок и контракт прогона, а отсутствие UI-тестов в v1 названо явно. |
| 118 | Нет контракта `.gitattributes` для 1С-репозитория | 2026-07-25 | 2026-07-26 | `3fae528` | Generic `* text=auto` из bootstrap нормализовал бы поставляемые бинарные обработки и сломал их SHA-256 — гейт безопасности записи. В состав артефактов добавлен managed-блок `.gitattributes`: `epf`/`erf`/`cf`/`cfe`/`cfu` объявлены `binary`, для `bsl`/`xml`/`mdo` фиксируются eol и кодировка. Конкретные text-правила дерева исходников ждут решения 1.21 о формате выгрузки. |
| 138 | План не связывает свой маршрут с открытыми дефектами валидатора и стандартизации | 2026-07-25 | 2026-07-26 | `a7f1008` | Запись опиралась на устаревший раздел `Open`: дефекты №69, №70 и №71 были исправлены ещё 2026-07-16 в `a210e02`, но их записи не перенесли в `Fixed`. Проверка кода и прогон `test-validator.py` и `test-standardize-existing-project.py` подтвердили исправления. Оставшееся предусловие — только №61, оно покрыто отдельной записью №137. Корневая причина шире одного плана: несинхронизированный журнал дефектов заставляет повторно принимать решения по уже закрытым проблемам. |
| 71 | Планировщик стандартизации игнорирует profile из `.project-standard.json` | 2026-07-16 | 2026-07-16 | `a210e02` | При `--profile auto` профиль выводился эвристикой по числу отсутствующих артефактов, а authoritative `profile` из `.project-standard.json` не читался, из-за чего adopted-проект получал рекомендацию `re-bootstrap-from-existing`. `detect_candidate_profile` теперь принимает `metadata_profile` и использует его при `auto`, как это уже делал `validate-project.py`. |
| 70 | Резолвер wikilink видит только `.md` и не разрешает ссылки на вложения | 2026-07-16 | 2026-07-16 | `a210e02` | Индексы `direct` и `by_stem` строились только из Markdown-файлов, хотя обход отдаёт все git-tracked файлы, поэтому ссылка на существующее вложение никогда не разрешалась. Добавлен отдельный индекс `by_stem_attachment`, к которому резолвер обращается, когда заметки с таким stem нет; приоритет заметки над вложением сохранён. |
| 69 | Валидатор считает битым wikilink с экранированным `\|` внутри Markdown-таблицы | 2026-07-16 | 2026-07-16 | `a210e02` | `WIKILINK_RE` захватывал хвостовой `\` в target, а нормализация превращала его в `/`, поэтому обязательное для Obsidian экранирование alias-разделителя внутри таблиц давало ложную `wikilink.missing`. Target теперь очищается `rstrip("\\")` до нормализации разделителей. |
| 135 | Upstream-инвентарь плана не позволяет проверить полноту будущего ledger | 2026-07-25 | 2026-07-26 | `7050d44` | Полнота декларировалась прозой по секциям, без суммарной контрольной величины, поэтому проверить её было нечем. В разбор источника внесены 241 tracked-файл как критерий приёмки ledger и поимённо названы ранее собирательные позиции; `content/mcp-servers.json` получил действие `route:1c-mcp-catalog` вместо статуса «запрещён». |
| 134 | Раздел «Проект разработки кода» отрицает skill `develop-1c` и содержит устаревший счёт skills | 2026-07-25 | 2026-07-26 | `7050d44` | Раздел писался до решения S.6 и не был обновлён после появления `develop-1c`, а счёт skills устарел после решений 1.11 и 1.14. Основным dev-skill назван `develop-1c`, правило о кандидате в Best Practices сужено до непокрытых практик, порядковый номер убран. |
| 133 | Классы разрешений Claude обещаны разделу «Безопасность», но там не определены | 2026-07-25 | 2026-07-26 | `7689a1e` | Forward-ссылка из раздела адаптеров не была закрыта при написании раздела безопасности. Решением 1.17 добавлен подраздел «Классы разрешений AI-клиента» с распределением по всем ролям каталога и явным обоснованием единственного `deny`. |
| 132 | Размещение `1c-projects.tsv` и `ENVIRONMENT_REGISTRY.md` задано двумя несовместимыми способами | 2026-07-25 | 2026-07-26 | `7050d44` | Дерево рабочей области рисовалось раньше остальных разделов и предполагало отдельную папку `1C/`, а последующие разделы использовали корневые пути. Решением 1.15 выбран корень проекта: дерево, scoped-правила (`configurations/AGENTS.md`) и все упоминания приведены к одному варианту. |
| 131 | Таблица MCP-каталога закрепляет порты внешних Docker MCP | 2026-07-25 | 2026-07-26 | `7050d44` | Таблица осталась от модели, в которой capability сама назначала порты provider-shared контейнеров. Пересмотренное решение 1.8 передало endpoint provider manifest, но таблицу не обновили. Литеральные порты заменены на «endpoint из provider manifest», добавлен абзац о единственном собственном диапазоне `6003`–`6012`. |
| 130 | Профили запуска EDT противоречат решению 1.6 о типе приложения | 2026-07-25 | 2026-07-26 | `7050d44` | Раздел профилей писался до разделения по `application_kind` и переносил ordinary-атрибут `ATTR_CLIENT_TYPE` и read-only EPF на все базы. Профили разведены: для `ordinary` — оба шаблона, для `managed` профиль пока не поставляется, `doctor-1c` проверяет ordinary-атрибут условно. Автозапуск для `managed` требует пересборки штатной обработки Toolkit и зафиксирован отдельным решением 1.16 как отложенная работа. |
| 116 | Детальная проработка крупного 1С-плана накапливалась в одном файле | 2026-07-25 | 2026-07-25 | текущий commit | Мастер-план одновременно служил картой решений, подробной спецификацией всех подсистем и журналом обсуждения. Из-за этого связанные решения было трудно проверять и синхронизировать, что уже проявлялось в дефектах №88 и №112. В `AGENTS.md`, глобальную политику и шаблон нового проекта добавлено правило «master plan = краткая карта, детали = связанные подпланы»; принятый контракт среды вынесен в отдельный двусторонне связанный подплан. |
| 115 | План смешивал создание проекта с настройкой машины и называл feature-зависимости optional | 2026-07-25 | 2026-07-25 | текущий commit | Границу транзакционного bootstrap ошибочно превратили в пользовательский запрет на установку, а все неядерные runtime packages объединили словом `optional`, хотя без них недоступны заявленные функции. Зафиксирован единый двухфазный workflow `create → setup → doctor` с отдельным approval внешних изменений и классы `required`/`conditional`/`optional`; Node.js, `docx`, Pillow, `v8unpack` и OpenSpec CLI стали conditional. |
| 114 | План не определяет позднее подключение второго AI-клиента | 2026-07-25 | 2026-07-25 | текущий commit | Постоянная поставка двух статических проекций была ошибочно принята за полный lifecycle: она не материализует актуальную topology для клиента, установленного позже. Добавлен идемпотентный `activate-1c-client`, client-scoped диагностика, `SKIP` отсутствующего CLI и тест поздней активации без re-bootstrap или изменения уже работающего клиента. |
| 82 | Автопереназначение порта вносит машинный конфликт в shared MCP-конфигурацию | 2026-07-25 | 2026-07-25 | текущий commit | План смешивал shared topology с локальной занятостью сокета и предписывал машинному конфликту менять committed TSV, EDT-профиль и client projections. Теперь назначение порта стабильно: `doctor-1c` блокирует локальный конфликт и предлагает освободить порт; переназначение возможно только как явное общее изменение topology. |
| 81 | Диапазон портов ограничивает число зарегистрированных баз, а не только одновременно запущенных | 2026-07-25 | 2026-07-25 | текущий commit | Уникальность порта ошибочно применялась ко всем строкам TSV. Теперь `server_port` обязателен и уникален только для `mcp_enabled=true`, у отключённых строк пуст; зарегистрированных баз может быть сколько угодно, одновременно экспонируемых — не более десяти. |
| 113 | Обязательность Docker ошибочно означала блокировку всех 1С-workflow | 2026-07-25 | 2026-07-25 | текущий commit | При синхронизации обязательного external provider формулировка «1С-workflow блокируется» стала шире уже принятой точечной деградации: отсутствие контейнерных MCP не делает недоступными независимые EDT и Toolkit. Текст сужен до MCP-зависимой разработки и обязательных проверок со ссылкой на таблицу доменов отказа. |
| 112 | Хвост плана capability `1c` не был синхронизирован с решениями S.1–S.13 | 2026-07-25 | 2026-07-25 | текущий commit | Последовательный разбор upstream обновлял канонические секции S.1–S.13, но не имел обязательного финального прохода по перечню артефактов, skills, зависимостям, этапам и критериям готовности. Поэтому там остались старое имя `verify-1c-workspace`, ровно шесть skills, собственное Docker-развёртывание capability и слишком широкое Windows-only ограничение. Хвост синхронизирован; отдельные нерешённые противоречия портов №81–82 намеренно не замаскированы. |
| 111 | INDEX преждевременно называл draft-план capability `1c` утверждённым | 2026-07-25 | 2026-07-25 | текущий commit | После возврата плана в draft навигационная строка сохранила старое слово `accepted`, хотя итоговый approval ещё не получен. INDEX приведён к фактическому статусу последовательной проработки. |
| 110 | Generic skill `transcribe` ошибочно считался частью capability `1c` | 2026-07-25 | 2026-07-25 | текущий commit | Полнота импорта из 1С-репозитория была ошибочно отождествлена с 1С-владением каждого payload. S.9.10 теперь исключает skill из capability, сохраняет явный import route и переносит все предложенные изменения в отдельный общий план. |
| 109 | Корневые support-файлы upstream не имели import routes | 2026-07-25 | 2026-07-25 | текущий commit | После разбора функциональных payload оставались `README.md`, `References.md` и `.gitignore`: прямое копирование README затёрло бы identity проекта, а отказ потерял бы справочник и runtime ignore rules. S.13 назначил provider-only documentation route, побайтный справочник и managed merge ignore patterns. |
| 108 | Upstream installer конкурировал бы с lifecycle стандартного проекта | 2026-07-25 | 2026-07-25 | текущий commit | `install.ps1` объединяет init/update/client placement/MCP/doctor/remove в собственном `.ai-rules.json`, тогда как план уже назначил эти операции bootstrap, migrations, renderer и validator. S.12 классифицировал installer как provider-only reference и пофункционально маршрутизировал полезные контракты без второго runtime owner. |
| 107 | Upstream adapters не имели явной границы с принятым client renderer | 2026-07-25 | 2026-07-25 | текущий commit | Одиннадцать YAML можно было либо потерять, либо ошибочно использовать как второй installer, получив глобальные Codex prompts, статический MCP и дубли rules/skills. S.11 сохранил все adapters как build inputs, назначил compiled Codex/Claude runtime descriptors и закрепил приоритет контрактов S.4.2/S.7/S.8. |
| 106 | OpenSpec оставался отложенным без import и lifecycle contract | 2026-07-25 | 2026-07-25 | текущий commit | Основной ruleset ссылался на отдельный будущий разбор, но план не назначал судьбу scaffold, четырёх workflow, client bundles, CLI dependency и approval boundary. S.10 закрепил полный route, роли относительно `PROJECT.md`/ADR/docs, обязательный approval перед `apply` и build-time refresh. |
| 105 | Два оставшихся dependency-light skill не имели явного import route | 2026-07-25 | 2026-07-25 | текущий commit | `prompt-enhancer` и `v8unpack-cf` оставались в upstream inventory без решения, хотя внутренней адаптации не требуют. S.9.9/S.9.11 закрепили побайтный payload, внешние client bridges, user-owned outputs и внешний optional dependency contract для `v8unpack`. |
| 100 | План не фиксирует общий PowerShell guidance для Windows-first 1С-операций | 2026-07-25 | 2026-07-25 | текущий commit | Upstream agents ссылались на `powershell-windows`, но отдельного import/update-контракта skill не было; generic guidance также можно было ошибочно активировать на POSIX или превратить в владельца provider lifecycle. S.9.8 закрепил побайтный platform-scoped skill, внешнее владение Compose workflow и Windows PowerShell 5.1 contract tests. |
| 99 | План не назначает поставку generic skill `mermaid-diagrams` внутри полной 1С-capability | 2026-07-25 | 2026-07-25 | текущий commit | Полнота upstream требовала явного route даже для generic documentation skill, но он не имел import/update-контракта и мог быть ошибочно превращён в обязательный renderer. S.9.7 зафиксировал побайтный dependency-free skill, условный trigger, обязательный sidecar и запрет автоматической передачи приватных данных внешнему сервису. |
| 98 | `md-to-docx` не фиксирует фактическую минимальную версию Node.js | 2026-07-25 | 2026-07-25 | текущий commit | Upstream называл только Node.js, хотя bundled `nanoid` исключает старые runtime. S.9.6 сохранил payload без изменений, но закрепил effective Node 18 LTS/≥20 во внешнем dependency contract, `doctor-1c` и отдельной строке пользовательской документации; npm packages устанавливаются локально из lock только с разрешения. |
| 97 | `overlay-grid.py` допускает деление на ноль для некорректной или предельно плоской сетки | 2026-07-25 | 2026-07-25 | текущий commit | Upstream не валидировал `cols`/`rows`, а auto-расчёт мог вернуть ноль. S.9.5 разрешил единственную внутреннюю адаптацию скрипта — guards положительных размеров, включая auto-result, с regression tests для zero/flat inputs. |
| 96 | `img-grid-analysis` требует Pillow без управляемого dependency-контракта | 2026-07-25 | 2026-07-25 | текущий commit | Upstream предлагал ad-hoc глобальный `pip install`, а первоначальное решение учитывало только технический manifest и не делало зависимость видимой пользователю. S.9.5 ввёл pinned release dependency, project-local install с разрешением, диагностику и обязательную отдельную строку `Зависимость: Pillow` в пользовательском гайде и `TOOLS.md`. |
| 95 | План не определяет жизненный цикл handoff-файлов между AI-клиентами | 2026-07-25 | 2026-07-25 | текущий commit | Upstream skill не имел import/update-контракта, а его session artifacts можно было ошибочно смешать с `memory.md`, Git-документацией или автоматически игнорировать, лишив межмашинного переноса. S.9.4 зафиксировал побайтный skill, user-owned output, ссылочную структуру, запрет secrets/automatic memory writes и явный выбор local versus portable target. |
| 94 | План сохраняет команду `caveman`, но не фиксирует поставку её skill | 2026-07-25 | 2026-07-25 | текущий commit | S.8 описывал изменение `CAVEMAN`, но не назначал import/update-контракт каноническому поведению режима. S.9.3 зафиксировал побайтный перенос skill, внешний client bridging, неизменные defaults/precedence/safety boundaries и проверки toggle без renderer/restart. |
| 93 | План не разделяет behavioral skill `mcp-1c-tools` и runtime MCP-каталог | 2026-07-25 | 2026-07-25 | текущий commit | Девять upstream-файлов не имели отдельного import/update-контракта, а формулировка «single source of truth» пересекалась с принятым `config/1c-mcp-catalog.json`; fallback `remember` также ослаблял S.5.2. S.9.2 разделил connection topology и task→tool policy, сохранил восемь файлов побайтно и ограничил адаптацию одной memory fallback-формулировкой. |
| 91 | План не сопоставляет 13 upstream commands с принятыми границами capability | 2026-07-25 | 2026-07-25 | текущий commit | Commands не имели явных владельцев и пересекались с provider lifecycle, refresh и write gates. S.8 назначил semantic owner всем 13 командам: канонические skills, provider delegation, maintainer-only refresh и upstream persistent controls в `.dev.env` с обязательными mutation/production guards. |
| 90 | План не сопоставляет 13 upstream agents с безопасной моделью Codex/Claude | 2026-07-25 | 2026-07-25 | текущий commit | Agents ошибочно рассматривались как одинаковые client-файлы с общим `allowParallel`. S.7 ввёл нейтральный канон и две generated projections, role-based permissions с реальной записью mutating ролям, model inheritance, serialized writes/worktree isolation, explicit triggers и parent-owned approval/integration/verification gates. |
| 89 | План не назначает владельца основному 1С-ruleset upstream | 2026-07-25 | 2026-07-25 | текущий commit | Ruleset рассматривался как клиентские копии, а не как единый семантический пакет. S.6 ввёл тонкий scoped AGENTS, канонический `develop-1c` skill с references, пофайловый semantic map, project-managed update contract и проверки полного покрытия/context budget/Codex-Claude parity. |
| 75 | Разбор upstream `AGENTS.md` потерял три обязательных companion-слоя | 2026-07-25 | 2026-07-25 | текущий commit | Файловый inventory сначала анализировался без связанного precedence-контракта. S.5.1–S.5.3 назначили `USER-RULES.md`, `memory.md` и `LLM-RULES.md` корневыми project-seed, развели правила/факты/self-improvement, определили writers, обновление, приоритет и одинаковую загрузку Codex/Claude. |
| 88 | Последовательный разбор плана повторно открывает уже принятые решения | 2026-07-25 | 2026-07-25 | текущий commit | Новый подпункт рассматривался изолированно без восстановления уже принятых ограничений. В «Режим проработки» добавлен обязательный шаг наследования журнала и канонических разделов; S.5.1 разобран уже внутри решений S.2, S.3, project memory и клиентских адаптеров. |
| 86 | План не сопоставляет три дополнительных MCP из upstream-каталога | 2026-07-25 | 2026-07-25 | текущий commit | Начальные семь ролей ошибочно считались полным inventory. S.4.4 сопоставил десять ролей: сохранил семь initial, назначил Code Metadata и Graph optional, а Data — optional-disabled до отдельного security review. |
| 85 | Нормализация shared MCP в `onec-*` может повторно зарегистрировать upstream endpoints | 2026-07-25 | 2026-07-25 | текущий commit | Ограничение OpenCode на letter-leading function names было ошибочно распространено на все клиенты. S.4.4 сохраняет canonical provider ids в Codex/Claude, вводит identity reconciliation и запрещает второй alias одного endpoint; `onec-*` остаётся для generated namespaces и нуждающихся в нормализации клиентов. |
| 84 | Scope `shared` не определяет владение портами между несколькими проектами | 2026-07-25 | 2026-07-25 | текущий commit | Provider runtime и project adapters рассматривались как один слой. S.4.4 ввёл `provider-shared`: capability переиспользует существующие endpoints, не разворачивает второй комплект и при dynamic external mode читает фактические ids/URLs из provider manifest/registry без записи машинных значений в Git. Изоляция stateful content выделена отдельно в №87. |
| 83 | Анализ S.4.4 ошибочно отождествил встроенный Toolkit с Docker-прокси | 2026-07-25 | 2026-07-25 | текущий commit | Docker-снимок был интерпретирован без повторной проверки канонического раздела плана. Контейнер `1c-mcp-toolkit-proxy` на `6003:6003` не входит в принятую архитектуру: план явно использует встроенный HTTP-сервер обработки Toolkit внутри runtime 1С. Диапазон `6003`–`6012` относится к этим встроенным per-base endpoints; shared контейнерные MCP используют отдельные `800x`. |
| 80 | S.4.2 допускает машинный local-слой во входах shared MCP-проекций | 2026-07-25 | 2026-07-25 | текущий commit | Входы runtime resolution и shared renderer были объединены без явной границы. S.4.3 запретил машинные литералы local-слоя в Git-проекциях: он используется только для runtime resolution/проверок; output допускает committed значения, имена переменных и поддерживаемые плейсхолдеры. |
| 79 | Нейтральный MCP-каталог не имеет scope для EDT workspace | 2026-07-25 | 2026-07-25 | текущий commit | Первичная модель ошибочно считала любой server либо общим, либо привязанным к базе. S.4.3 добавил `per-workspace`, отдельный EDT namespace на уникальный workspace и ссылки нескольких баз на один workspace endpoint без дублирования. |
| 77 | `select-1c-project` не может переподключить статический MCP Codex к выбранной базе | 2026-07-25 | 2026-07-25 | текущий commit | `select` ошибочно трактовался как динамическая перенастройка MCP. S.4.3 заранее объявляет отдельные namespaces и превращает `select` в проверку точного endpoint с session lock; новая сессия нужна только после изменения топологии или невозможности переподключения. |
| 78 | S.4 определяет конфигурационный адаптер Codex, но не Claude Code | 2026-07-25 | 2026-07-25 | текущий commit | Первичный разбор ошибочно принял Codex config за весь client contract. S.4.2 ввёл единый нейтральный MCP-каталог, обязательные проекции для Codex и Claude Code, key-aware владение `.mcp.json`/`.claude/settings.json` и запрет изменения локальных пользовательских слоёв. |
| 76 | План capability `1c` не определяет безопасное владение `.codex/config.toml` | 2026-07-25 | 2026-07-25 | текущий commit | Upstream-адаптер рассматривался как готовый renderer общего TOML-файла. S.4.2 ограничил владение маркированным MCP-блоком, исключил недокументированные ключи, ввёл artifact-ledger conflict detection и транзакционное обновление с сохранением остальных настроек. |
| 74 | Непроработанный план capability `1c` был преждевременно отмечен как `accepted` | 2026-07-22 | 2026-07-22 | текущий commit | Не было явного гейта между обсуждением отдельных гипотез и утверждением всего документа; `status: accepted` и галочка первого пункта были ошибочно приняты за разрешение начать реализацию. План возвращён в `draft`; добавлены порядок разбора всех разделов и запрет менять артефакты capability до явного итогового утверждения. |
| 73 | Глобальные и проектные AGENTS не имели явного контракта владения правилами | 2026-07-22 | 2026-07-22 | текущий commit | Глобальный шаблон, project baseline и локальный `AGENTS.md` стандарта развивались как отдельные тексты без документированной границы ответственности. В project baseline дважды повторялись ограничения размера и перезапуска, а repository rule допускал ошибочную трактовку подпапки как отдельного проекта. Schema 4 формализует три уровня, оставляет только объявленный portable safety subset и доставляет изменение последовательными миграциями. |
| 72 | Contract fixture не копирует обязательный capability manifest | 2026-07-19 | 2026-07-19 | текущий commit | При добавлении `config/capabilities.tsv` PowerShell fixture обновили, а POSIX fixture секции `Manifest drives bootstrap output` — нет. Изолированный bootstrap завершался до проверки изменённого `profiles.tsv`, потому что обязательный capability manifest отсутствовал. Fixture теперь копирует его вместе с остальными конфигурационными manifest-файлами; полный POSIX contract suite проходит 68/68. |
| 68 | `standardize-existing-project` не доводил legacy-проект до green | 2026-07-16 | 2026-07-16 | текущий commit | Обнаружен на `Other` (дефект `favorit-web` №13). Круговой пробел: `plan-migration --target project-agents` блокировал `unmanaged_conflict` советом «run standardize-existing-project first», а adopt-in-place относил `AGENTS.md` к manual review и baseline не вносил. Блок был обоснован: под то же состояние попадают проект с чисто локальными правилами (дописать снизу) и проект с немаркированной копией baseline (дописывание продублирует правила) — различить их может только человек. Planner принимает этот review как `--accept-unmanaged-as-local` и без него по-прежнему блокирует; поведение выровнено с target `global`, который уже принимал `unmanaged_conflict`, включая backup. Отдельно `validate-project` требовал `docs/quality/STANDARD_ADOPTION.json` для профиля `minimal`, которого adopt-in-place не создавал: файл добавлен в safe creates, а журнал начинается с `adopted_at` из `.project-standard.json` (fallback — сегодня), потому что дату создания legacy-проекта восстановить нельзя. Проверено сквозным прогоном на фикстуре, повторяющей исходное состояние `Other`: 9 errors → 0 errors без ручных шагов, локальные заметки сохранены построчно. |
| 67 | `plan-migration.ps1` отвергает target `project-agents` | 2026-07-16 | 2026-07-16 | текущий commit | Обнаружен в `favorit-web` (его дефект №12). `config/migrations.tsv` объявляет `0003-adopt-project-agents-managed-block` и `0006-upgrade-project-agents-managed-block-v2` для target `project-agents`, `plan_migration.py` его поддерживает, а POSIX-обёртка target не валидирует и передаёт как есть — но `ValidateSet` в `plan-migration.ps1` перечислял только `project` и `global`. Обе baseline-миграции были недостижимы на Windows через documented path; обходом оставался прямой вызов Python. `ValidateSet` дополнен `project-agents`; проверено на `Other` — plan возвращает `up_to_date`. |
| 66 | `sync-global-agents --check` падает traceback'ом на состоянии `managed_upgrade` | 2026-07-16 | 2026-07-16 | текущий commit | Обнаружен в `favorit-web` (его дефект №11). Parser корректно определял `managed_upgrade` и печатал `status=managed_upgrade`, после чего `status_message()` падал с `KeyError: 'managed_upgrade'`: словарь `messages` перечислял семь состояний из восьми. Пользователь получал traceback вместо диагностики ровно в тот момент, когда инструмент должен объяснить, что нужна миграция. Существующий тест доходил до `inspect_state`, но `status_message` не вызывал, поэтому пробел не ловился. Добавлено сообщение для `managed_upgrade` и регрессионный тест, проверяющий непустое сообщение для каждого достижимого состояния. |
| 65 | Validator сканирует зависимости и выдаёт ложные errors | 2026-07-15 | 2026-07-15 | текущий commit | Обнаружен в `favorit-web` (его дефект №8). `iter_files()` обходила всё дерево через `rglob` и исключала только `.git`, `__pycache__` и `.trash`, не учитывая `.gitignore`: 12 ложных `wikilink.missing` и 4 `path.machine_specific` из README пакетов в `node_modules`, exit code `1`, и 10378 прочитанных файлов вместо 47 проектных. Обход стал git-aware (`git ls-files --cached --others --exclude-standard`) с fallback на `rglob` при отсутствии git; `IGNORED_PARTS` расширен зависимостями и применяется к пути относительно корня, а не абсолютному. |
| 64 | Scheduled governance audit требовал недоступные `GITHUB_TOKEN` данные | 2026-07-14 | 2026-07-14 | текущий commit | Default workflow token не читает collaborators другого repo и полностью скрывает bypass actors. Полный локальный audit остаётся strict; scheduled self-audit проверяет active guards/checks и sole owner-admin собственного repo, не заявляя проверку скрытого bypass. |
| 63 | Migration planner test сравнивал сохранённый prefix с LF-only строкой | 2026-07-14 | 2026-07-14 | текущий commit | Windows Git fixture сохранила корректный локальный prefix с CRLF, а assertion требовал LF bytes. Проверка нормализует только line endings перед семантическим сравнением; production сохраняет исходный стиль. |
| 62 | Pinned BP hashes зависели от working-tree line endings | 2026-07-14 | 2026-07-14 | текущий commit | Contract verifier хешировал checkout bytes, которые `core.autocrlf` меняет без изменения Git blob. Проверка теперь хеширует `git show HEAD:<path>` и отдельно блокирует dirty pinned files. |
| 60 | Create workflow блокируется внутренней validation актуальной Best Practices | 2026-07-14 | 2026-07-14 | BP `4640e7e`, `a492d82`; текущий commit NPR | BP validator сериализовал относительный путь через platform-native separators; NPR E2E не запускал полную validation. BP использует POSIX paths и трёхплатформенный gate со стабильным check `validate`; NPR запускает full validation/real report, live watch и fail-safe create workflow без ручных outcomes. |
| 59 | Bypass защиты `main` выдан роли Admin, а не конкретному аккаунту | 2026-07-08 | 2026-07-14 | текущий commit; API audit в [[ACTIONS]] | GitHub ruleset предоставляет bypass роли, а не отдельному пользователю. Admin-bypass принят как trust boundary для прямого push владельца; scheduled audit требует, чтобы владелец оставался единственным Admin обоих репозиториев. |
| 58 | PowerShell contract test содержал Cyrillic без совместимой Windows encoding | 2026-07-08 | 2026-07-08 | (текущая ветка) | В `test-skills.ps1` добавили русские literals; Windows PowerShell 5.1 разобрал UTF-8 без BOM как legacy encoding и завершился parser error. Cross-platform assertion переведён на ASCII contract marker, POSIX test сохраняет проверку русской формулировки. |
| 57 | `reflect-and-record` сохранял отменённый запрет mid-session edit | 2026-07-08 | 2026-07-08 | (текущая ветка) | После исправления дефекта №55 один связанный skill не был включён в синхронизацию. Формулировка согласована с каноном: редактировать можно, но изменение действует в новом процессе; shell/PowerShell contract tests запрещают возврат старого текста. |
| 56 | Команда проверки instruction sources запускала интерактивный Codex TUI | 2026-07-07 | 2026-07-07 | `e00de26` | В аудит и производные инструкции попала форма `codex ... "prompt"` без subcommand; в non-TTY она завершалась ошибкой `TERM is set to "dumb"`. Во всех исполняемых guides/templates/skills добавлен `codex exec`, после чего проверка выполняется отдельным non-interactive процессом. |
| 55 | Global policy и шаблоны закрепляли неподтверждённые ограничения AGENTS.md | 2026-07-07 | 2026-07-07 | `7b6fad0`, `e00de26` | Глобальный, проектный и template-слои копировали community-рекомендации как технические факты: лимиты 150/300 строк, recite-back, запрет scoped override и неверную причину mid-session. Аудит выявил расхождение с документацией; global policy сокращена, проектные процессы оставлены в template/skills, validator и policy contract очищены от вымышленных порогов. |
| 54 | Consumer writer принимал секции, которые BP loader отклоняет | 2026-07-07 | 2026-07-07 | (текущая ветка A4) | A3 проверяла только синтаксис section slug и использовала пример `python`, не сверяя enum с `validate.ALLOWED_STACKS` Best Practices. Writer ограничен семью canonical sections; cross-repo E2E исполняет NPR writer против реального BP loader на трёх ОС. |
| 53 | Create workflow писал несовместимый consumer manifest | 2026-07-07 | 2026-07-07 | (текущая ветка A3) | Skill, template и guides закрепляли schema 1 top-level `optout` и section `applied`, которых canonical BP schema 2 не поддерживает. Добавлен единый schema 2 writer с atomic write и strict existing-manifest validation; workflow использует `preferences`, а outcomes делегирует BP tooling. |
| 51 | Assessment классифицирует rules repository как consumer project | 2026-07-07 | 2026-07-07 | (в этой ветке) | Planner применял consumer profile inference к любому дереву. Добавлен строгий `target_kind=rules-repository` по совокупности пяти markers, `not_applicable` report и запрет всех consumer plan/apply; regression покрывает read-only JSON, прямые builders, CLI plan/apply и отрицательный consumer fixture. |
| 52 | Стратегический план сообщает устаревшие schema и GitHub governance | 2026-07-07 | 2026-07-07 | (в этой ветке) | Статус реализации дублировался вручную и не был обновлён после schema `2` и включения ruleset. План актуализирован, следующий этап отделён от завершённых A–F; навигация больше не называет его proposed-планом. |
| 50 | `STANDARD_VERSION` не отражает новый managed knowledge contract | 2026-07-07 | 2026-07-07 | PR №6, merge `3dc5e0a`; [[docs/reviews/archive/PHASE_5_SEQUENTIAL_MIGRATIONS_REVIEW_2026-07-07|review]] | Version оставалась `1`, а planner поддерживал только одиночный `0 → current`. Введена schema `2` и детерминированный graph `0 → 1 → 2` для metadata/global/project-agents; exact history, bootstrap parity, atomic apply, backups и schema-1 upgrades покрыты regression и трёхплатформенным CI. |
| 49 | Нет исполняемого cross-repository контракта NPR ↔ BP | 2026-07-07 | 2026-07-07 | PR №4, merge `ee4677a`; [[docs/reviews/archive/PHASE_4_CROSS_REPO_CONTRACT_REVIEW_2026-07-07|review]] | Репозитории тестировали себя независимо. Добавлен pinned offline contract и live checkout gate: repository/commit/hashes, accepted promotion source, retired routes и ADR consequences; suite подключён к Ubuntu, Windows и macOS CI. Broad governance verification выполнена по обновлённому PLAYBOOK-паттерну. |
| 48 | `new-project-rules/main` не защищён на GitHub | 2026-07-07 | 2026-07-07 | GitHub ruleset `18603924`; [[docs/reviews/archive/PHASE_2_MAIN_PROTECTION_REVIEW_2026-07-07|review]] | Governance оставался только документацией после прежнего `403`. Добавлены CODEOWNERS и regression test, затем включён active ruleset: PR + 1 approval + Code Owner review + resolved threads, strict `shell`/`powershell`, запрет deletion/non-fast-forward. API postcondition: `main.protected=true`. |
| 47 | Standardization tree digest читает transient `.git` locks | 2026-07-06 | 2026-07-06 | (в этой ветке) | Test helper включал внутренности `.git` в проверку отсутствия mutation. На macOS runner с Python 3.14 Git maintenance успевал удалить `objects/maintenance.lock` между `is_file()` и `read_bytes()`, вызывая flaky `FileNotFoundError`. Digest теперь проверяет только project artifacts и исключает `.git`, состояние которого тестируется отдельными Git assertions. |
| 46 | PowerShell metadata test зависит от whitespace `ConvertTo-Json` | 2026-07-06 | 2026-07-06 | (в этой ветке) | Новый regression test сравнивал JSON как строку с форматированием PowerShell 7, тогда как Windows PowerShell 5.1 добавляет другое число пробелов после `:`. Проверка переведена на `ConvertFrom-Json` и семантическую валидацию полей, дат и commit SHA. |
| 45 | Bootstrap не создаёт `.project-standard.json` | 2026-07-06 | 2026-07-06 | (в этой ветке) | Manifest и оба bootstrap-адаптера создавали документы и git repository, но пропускали обязательную provenance metadata. Новый проект сразу считался legacy, хотя README называл metadata источником истины. Metadata добавлена как manifest-driven generated artifact; shell/PowerShell фиксируют schema, profile, source, source commit и даты, а без Git bootstrap теперь явно отказывает. |
| 43 | Promotion backlog создаёт merge-конфликты как единая таблица | 2026-07-06 | 2026-07-06 | `b459bf3` | Все кандидаты изменяли один Markdown-файл. Backlog переведён на one-file-per-candidate; стабильный index больше не меняется при добавлении записи, schema удерживается validator и CI. |
| 44 | Последовательные promotion ID требуют централизованной координации | 2026-07-06 | 2026-07-06 | `b459bf3` | `PC-YYYY-NNN` требовал общего счётчика между ветками. Генератор использует 48-битный hex suffix с retry при локальной коллизии; validator запрещает дубли, legacy ID остаются валидными. |
| 28 | Активный `~/.codex/AGENTS.md` разошёлся с переносимой копией, managed markers утрачены | 2026-07-02 | 2026-07-02 | см. [[ACTIONS]] | После adoption 2026-06-30 активный файл был перезаписан старой редакцией без markers (вероятно, старый `setup-global-agents` на другом компьютере; root cause подтвердить). Исправлено: user-reviewed копия portable → active, затем migration `0002` (fingerprint `18a73ff1…f063d`), postcondition `managed_match`. |
| 29 | `docs/README.md` не содержит два research-файла | 2026-07-02 | 2026-07-02 | `7fb88eb` | Секция «Исследования» не пополнялась при добавлении research-файлов; ссылки на AGENT_RUNTIME_CAPABILITIES_2026 и AGENT_COMMUNITY_PRACTICES_2026 добавлены. |
| 30 | `USE_THIS_PROJECT.md` неполон | 2026-07-02 | 2026-07-02 | `21f3cdf` | Гайд писался до финализации набора skills: секция knowledge promotion не получила Claude Code эквиваленты, а `reflect-and-record` выпал из перечня workflows. Добавлены `/`-вызовы и секция 9 с фразами и таблицей выбора. |
| 31 | CHANGELOG `Unreleased` разросся и отстаёт | 2026-07-02 | 2026-07-02 | `21f3cdf` | Пять циклов работы после `v1.9.0` не нарезались в релиз, и записи о новых артефактах отставали. Нарезан `v1.10.0 — 2026-07-02`, добавлены записи о гайде и дефектах 28–29. |
| 32 | `TOOLS.md` не каталогизирует большинство скриптов | 2026-07-02 | 2026-07-02 | `21f3cdf` | Правило про TOOLS.md выполнялось только для Python/PowerShell runtime. Добавлен раздел `Script entry points`: ссылка на [[INDEX]]/[[docs/quality/TESTING|TESTING]] как авторитетные каталоги, семейства скриптов и явное исключение из правила парности `.sh`/`.ps1`. |
| 34 | Standardization regression suite не запускается в CI | 2026-07-03 | 2026-07-03 | `fcf16e7` | Suite добавлялся в локальную матрицу TESTING, но шаг в workflows не создавался. Подключён к ubuntu/windows jobs `ci` и `macos-smoke`; непортабельные тесты получили skip-guards (POSIX wrapper на Windows, symlink fixtures без прав на symlink). |
| 38 | PowerShell scoped-agent test падает локально на macOS из-за `/var` и `/private/var` | 2026-07-03 | 2026-07-03 | `83d5e11` | Containment-проверка сравнивала строки без резолва symlink-предков (`/var` → `/private/var`). Добавлен `Get-CanonicalExistingPath` (chdir + getcwd даёт физический путь на Unix); root, probe и directory сравниваются в канонической форме. `test-agent-setup.ps1` проходит на macOS pwsh (31/31). |
| 39 | Environment check требует HTTPS credential helper при SSH Git transport | 2026-07-03 | 2026-07-03 | `83d5e11` | Проверка не различала transport. Теперь при `gh git_protocol=ssh` и пустом `credential.helper` выводится `[ ok ]` (SSH аутентифицируется ключами); для HTTPS/неизвестного transport поведение прежнее. Обе ветки проверены в sh и pwsh с изолированным git-конфигом. |
| 35 | Standardization apply пишет через symlink за пределы проекта | 2026-07-03 | 2026-07-03 | `3ace8ca` | Планируемые writes никогда не проверялись на symlink/containment. Добавлен `unsafe_write_reason()`: план блокируется, а `apply_plan()` отказывает, если путь выходит за root или проходит через symlink; regression test с `INDEX.md` → внешний файл. |
| 36 | Re-bootstrap разыменовывает symlink и копирует внешний файл | 2026-07-03 | 2026-07-03 | `3ace8ca` | `copy_transfer_item()` использовал `shutil.copy2()` без проверки symlink. Теперь план блокируется при symlink в transfer set (`build_transfer_manifest()`), а apply дополнительно отказывает копировать symlink и проверяет containment target. |
| 37 | Re-bootstrap fingerprint не защищает содержимое transfer set | 2026-07-03 | 2026-07-03 | `3ace8ca` | Fingerprint включал только имена top-level paths. В payload добавлен `transfer_manifest` — полный список файлов с sha256 содержимого; изменение файла после review даёт fingerprint mismatch, что покрыто regression test. |
| 40 | Re-bootstrap разрешает destination внутри legacy repo и `.git` | 2026-07-03 | 2026-07-03 | `3ace8ca` | Guard сравнивал только `destination == root`. Добавлены проверки `is_relative_to` в обе стороны: destination внутри source root (включая `.git`) и source внутри destination блокируются; regression test покрывает оба вложенных пути. |
| 41 | План компрессии закоммичен с template-плейсхолдером даты, `validate-project` падает | 2026-07-06 | 2026-07-06 | (в этой ветке) | При описании плейсхолдера прозой в план попал сырой литерал `<`+`YYYY-MM-DD`+`>`; правка порогов `4c83ab9` закоммичена без прогона валидатора, и `placeholder.remaining` дошёл до `main`. Формулировки переписаны без сырого литерала; урок — прогонять `validate-project --report-only` перед commit docs и не писать template-плейсхолдеры прозой. |
| 42 | Сплит CHANGELOG в компрессии оставлял архив без обратной ссылки | 2026-07-06 | 2026-07-06 | (в этой ветке) | `split_changelog` создавал `CHANGELOG_ARCHIVE.md` со ссылкой `[[CHANGELOG]]`, но не оставлял обратную ссылку в `CHANGELOG.md` — архив был связан в одну сторону (риск orphan/дрейфа навигации), в отличие от паттерна DEFECTS. Найдено dogfood-применением на самом репозитории. Добавлен `CHANGELOG_ARCHIVE_POINTER` (идемпотентная вставка в преамбулу) и регрессионный тест `test_pointer_is_idempotent`; архив внесён в [[INDEX]]. |

## Won't Fix

| # | Title | Discovered | Reason |
|---|---|---|---|
| 92 | План не фиксирует формат 1С-исходников для metadata tooling | 2026-07-25 | Не дефект при принятом контракте минимальной адаптации. `1c-metadata-manage` переносится без изменения и работает только с ожидаемым им XML-деревом; наличие `Configuration.xml` и нужной структуры проверяют сами upstream tools. EDT workflow остаётся отдельным владельцем и не требует общего `source_format`. Решение 2026-07-25: поле и compatibility router не вводятся. |
| 87 | Provider-shared MCP не имеют согласованной модели state/mount isolation | 2026-07-25 | Не дефект consumer capability: S.4.4 уже назначил внешний MCP-проект владельцем runtime, портов, mounts, индексов и container lifecycle. Повторное проектирование provider internals расширяло scope и дублировало внешнюю систему; наш контракт проверяет только обнаруженный endpoint, identity, health и tools. Решение 2026-07-25: отдельный S.4.5 не реализуется. |
| 4 | POSIX symlink-тесты падают в Git Bash без symlink support | 2026-06-28 | Git Bash `ln -s` на этой Windows копирует файл, поэтому resolver-тесты получают неверный каталог. Windows поддерживается PowerShell-набором; POSIX symlink-сценарии выполняются в macOS/Linux и CI. |
| 33 | PowerShell bootstrap-тест падает локально на macOS в mock-git секции | 2026-07-02 | Дефект тестовой обвязки, а не продукта: pwsh на macOS кеширует резолв `git` в удалённый `mock-bin` путь из временного PATH (command caching). CI (Windows PowerShell 5.1 / pwsh 7 на windows-latest) сценарий не воспроизводит и полностью покрывает; профильные проверки скрипта проходят. Решение 2026-07-03: не чинить. |
