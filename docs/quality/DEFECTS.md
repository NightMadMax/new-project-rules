---
type: defect-log
status: active
owner: project
last_verified: 2026-07-26
related:
  - "[[docs/README]]"
  - "[[docs/quality/TESTING]]"
---

# Дефекты

Обнаруженные дефекты не удаляются: после исправления запись переносится в
соответствующий раздел с датой и commit.

## Open

| # | Title | Discovered | Component | Description |
|---|---|---|---|---|
| 239 | Стандарт не применил к себе Уровень 1 компрессии | 2026-08-01 | `CHANGELOG.md` | `compress-project --root .` планирует `changelog.split`: 75.7 КБ против порога 32 КБ. |
| 238 | `DEFECTS.md` не архивируется, критерии архивации расходятся | 2026-08-01 | `docs/quality/DEFECTS.md` | 174 записи в `Fixed` и 75 КБ при правиле «больше 30 — архивировать». Критерии в `reflect-and-record` и `compress-project` разные, и ни один не срабатывает. |
| 237 | `ARCHITECTURE.md` не описывает слой, несущий сложность | 2026-08-01 | `docs/architecture/ARCHITECTURE.md` | Документ описывает шесть слоёв и не упоминает capability, preset, release и ledger. |
| 236 | Обновление capability миграцией реализовано наполовину | 2026-08-01 | `config/migrations.tsv` | Обработчик объявлен внешним, но в манифесте миграций нет ни одной строки с ним. |
| 235 | `AGENTS.md` предписывает push, который ruleset отклоняет | 2026-08-01 | `AGENTS.md` | Строка 53 требует push в `origin/main`; ruleset этого не допускает, а единственный документированный обход записан дефектом №190. |
| 234 | Диапазон портов `6003–6012` продублирован | 2026-08-01 | `scripts/validate-project.py`, `scripts/one_c_clients.py` | Расширение диапазона в одном месте даст базу, которую валидатор примет, а рендерер молча отбросит. |
| 233 | Новые capability и skill не дошли до пользовательских руководств | 2026-08-01 | `README.md`, `docs/guides/USE_THIS_PROJECT.md`, `MANUAL_SCRIPTS.md` | `transcribe` отсутствует во всех трёх, `document-process-workflow` — в первых двух. Нарушено правило «скилл и гайд обновляются в одной задаче». |
| 232 | Список capability объявлен в восьми местах без сверки | 2026-08-01 | `scripts/project_metadata.py` и документация | Сверки с манифестом нет, в отличие от `CAPABILITY_CORE`, где она есть. Списки уже разошлись: `PROJECT_STANDARD_SCHEMA.md:44` и `.agents/skills/create-new-project/SKILL.md` не знают про `transcribe`, то есть capability невидима на главном маршруте своего выбора. |
| 231 | `tracked_files` декодирует вывод git локальной кодировкой | 2026-08-01 | `scripts/import_1c_upstream.py` | При выставленном `core.quotePath=false` вывод разбирается локальной кодировкой. Латентно, пока в upstream нет не-ASCII имён; при их появлении файл будет выглядеть одновременно отсутствующим и лишним. |
| 230 | У джоб CI нет таймаута | 2026-08-01 | `.github/workflows/` | Таймаут задан только в `macos-smoke`; у остальных действует дефолт в шесть часов. |
| 229 | `test-standard-metrics.py` не документирован | 2026-08-01 | `docs/quality/TESTING.md`, `INDEX.md` | Тест существует и запускается, но в обоих документах его нет. |
| 228 | `macos-smoke` не запускает пять существующих тестов | 2026-08-01 | `.github/workflows/macos-smoke.yml` | Отсутствуют `test-cli-discovery.py`, `test-1c-session.py`, `test-1c-doctor.py`, `test-1c-upstream-routing.py`, `test-github-governance.py`. |
| 227 | `test-skills.ps1` слабее своей `.sh`-пары | 2026-08-01 | `scripts/test-skills.ps1` | Один литерал против четырёх проверок, проверки плейсхолдера схемы нет. Правило парности требует одинакового вердикта. |
| 226 | Парсер PowerShell берёт 21 файл из 81 | 2026-08-01 | `scripts/test-powershell-syntax.ps1` | Проверяются только `scripts/*.ps1`; шестьдесят поставляемых `.ps1` payload не парсятся вовсе. |
| 224 | Пять мёртвых файлов в payload и битая ссылка в доставляемом README | 2026-08-01 | `templates/new-project/capabilities/1c/upstream` | Дубли `openspec/*` лежат и в корне payload, и в `openspec/`; доставляемый `openspec/README.md` ссылается на `AGENT-INSTALL.md`, который маршрутизирован как provider-only и не поставляется. |
| 223 | Добавить capability в уже созданный проект невозможно | 2026-08-01 | `scripts/capability_artifacts.py` | Проверка `rendered and current is None` стоит до ветки `seed` (строка 145), поэтому seed-шаблон даёт конфликт вместо создания. Сообщение описывает механику, а не проблему. |
| 222 | Компонент с `detect: manual` нельзя подтвердить | 2026-08-01 | `scripts/one_c_components.py`, `setup-1c-environment` | Ветка «подтверждено человеком» ждёт ответа `использовать`, а `options()` для ненайденного компонента его не предлагает. Платформа 1С помечена `required` и `manual`, поэтому на настроенной машине `setup-1c-environment` никогда не даёт `ready`. |
| 221 | Профили запуска EDT — `managed` с неподставляемыми плейсхолдерами | 2026-08-01 | `config/capabilities.tsv` | Строки 22–23 доставляют профили с `<EDT_PROJECT>` и `<RUNTIME_INSTALLATION>`, которые bootstrap не подставляет. EDT такой профиль не запустит; пользователь правит файл — и получает конфликт на ~230 артефактов capability `1c`. Тот же класс, что №207. |
| 220 | Флаг `--include-memory` объявлен и не реализован | 2026-08-01 | `scripts/compress-project.py`, `COMPRESS_PROJECT.md` | Флага нет ни в скрипте, ни в обёртках, хотя документация и скилл его называют. |
| 219 | Режимы `manage-1c-mcp` не поддержаны исполнителем | 2026-08-01 | `scripts/one_c_provider.py` | Скилл объявляет `install`, `update` и `repair`, а `one_c_provider.py` знает только `--root` и `--manifest`. |
| 218 | `evolve-1c-rules note …` не имеет исполнителя | 2026-08-01 | `.agents/skills/evolve-1c-rules` | Скилл называет команду сохранения сигнала в `Pending signals`, но исполнителя у неё нет. |
| 217 | `1c-metadata-manage` назван skill'ом, но для Claude Code им не является | 2026-08-01 | `config/skills.tsv` | `develop-1c` и `deploy-1c-source` направляют работу «через skill `1c-metadata-manage`», а у него `bridge = none`: Claude Code его не видит. Гейт, который нельзя соблюсти в половине клиентов. |
| 216 | `ONE_C_WORKSPACE.md` обещает заполнение скиллом, который его не трогает | 2026-08-01 | `ONE_C_WORKSPACE.template.md` | Строка 16 утверждает «Заполняется `add-1c-base`», но скилл этот файл не открывает. |
| 215 | Скиллы ссылаются на `python3 scripts/…`, которых в проекте нет | 2026-08-01 | capability `1c` skills | `deploy-and-test-1c`, `manage-1c-mcp`, `select-1c-project` называют скрипты стандарта, которых в созданном проекте нет. Правило про чекаут стандарта есть только в пяти скиллах из восьми. |
| 214 | `deploy-and-test-1c` читает флаг вместо каталога компонентов | 2026-08-01 | `.agents/skills/deploy-and-test-1c` | Скилл обещает, что состояние берётся из каталога компонентов, а `--not-installed` — ручной флаг; каталог не читается. |
| 213 | `transcribe` обещает три файла, пишет один | 2026-08-01 | общий skill `transcribe` | `transcribe.py:254` пишет единственный `transcript.md`, значение `detailed` в `OUTPUTS` мёртво, а `--format json` кладёт JSON в файл с расширением `.md`. Скилл описывает дерево из трёх файлов, «появляющихся сразу». |

## Fixed

Записи, исправленные до релиза `v1.10.0` (№ 1–3, 5–27), перенесены при
консолидации в [[docs/quality/DEFECTS_ARCHIVE|архив дефектов]] без изменения
содержимого. Нумерация сквозная и не переиспользуется.

| # | Title | Discovered | Fixed | Commit | Root Cause |
|---|---|---|---|---|---|
| 211 | `one_c_doctor.occupied()` на Windows не видит слушателя на `0.0.0.0` | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | Проба привязывалась только к `127.0.0.1`, поэтому порт, занятый на всех интерфейсах, считался свободным — ровно случай, ради которого проверка написана: платформа 1С слушает `6003` на `0.0.0.0`. Теперь спрашиваются оба адреса, а на Windows вместо `SO_REUSEADDR` ставится `SO_EXCLUSIVEADDRUSE`: первый там позволяет привязаться поверх активного сокета и делает пробу бессмысленной. Тест на «занят на всех интерфейсах» на POSIX не различает реализации, поэтому добавлен структурный: проба обязана спросить про каждый интерфейс, и он падает на старом коде. |
| 212 | `one_c_doctor.settings()` маскирует по содержимому, а не по имени ключа | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | Решение принималось по всей строке и по списку из пяти маркеров, поэтому `DB_PWD`, `ONEC_CREDENTIAL` и `BASE_LOGIN` печатались целиком. Теперь имя ключа и значение разбираются раздельно: маскируется ключ, чьё имя называет учётные данные, и значение, похожее на строку соединения. Тест, закреплявший утечку, расширен этими тремя ключами и падает при возврате прежнего списка маркеров. |
| 225 | `governance-audit` рапортует о проверке bypass-акторов, не выполняя её | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | При `--github-token-scope` пустой список bypass считался успехом, и ночной аудит печатал «reviewed bypass» о значении, которого не видел. Скрытое токеном поле — не нарушение, но и не проверка: теперь оно сообщается строкой `NOTE ... не проверено`, а строка успеха перечисляет только то, что действительно проверено. Тест, требовавший молчания, переписан на новое требование и падает при возврате прежнего поведения. |
| 210 | Нет теста на связку профиль ↔ `docs_section` capability | 2026-08-01 | 2026-08-02 | ветка `fix/review-blocking` | №205 был исправлен данными — обеим capability подняли минимальный профиль, — но класс ошибки оставался открытым: ничто не проверяло саму связку. Инвариант добавлен в `test-preset-core.py`, где полный bootstrap не нужен: capability с непустым `docs_section` обязана требовать профиль не ниже `software`, на котором появляется `docs/README.md`. Проверено откатом — при возврате `jira-confluence` на `minimal` тест падает именно этой строкой. |
| 205 | Capability с разделом документации роняет bootstrap на профиле без `docs/README.md` | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | Артефакт с непустым `docs_section` индексируется в `docs/README.md`, а тот появляется с профиля `software`. Ядро поднимало профиль только для `1c`, поэтому `minimal` + `jira-confluence` или `transcribe` записывал все файлы, падал на индексации и откатом удалял каталог назначения — при том, что `create-new-project` предлагает `minimal` как допустимый ответ. Обе capability получили `min_profile: software`; `CAPABILITY_REQUIRED_STACK` пропускает `-`, иначе прочерк стал бы именем обязательного стека. Класс закрыт не полностью: теста на связку профиль ↔ `docs_section` нет, он в №210. |
| 206 | Provider manifest мог дописать произвольную секцию в `.codex/config.toml` | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | Проверялся `health`, а регистрировался `url` — разные поля манифеста, и `url` не валидировался вовсе. Дальше он подставлялся в TOML конкатенацией: кавычка и перевод строки закрывали строку и открывали таблицу `[mcp_servers.…]` с произвольной командой, то есть внешний JSON решал, какую программу Codex запустит как MCP-сервер. Закрыто в двух слоях: манифест принимает только простой http(s)-адрес без кавычек и переводов строк, а `health` обязан принадлежать тому же серверу, что и `url`, иначе проверка доказывает не то; писатель TOML отказывается от значения, которое не может закавычить. |
| 207 | Заполняемые документы `jira-confluence` объявлены `managed` | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | Одиннадцать документов-бланков (`JIRA.md`, `CONFLUENCE.md`, модели статусов, каталоги запросов) были `managed`. Первая правка пользователя — то есть использование файла по назначению — становилась дрейфом, а дрейф останавливает транзакцию всей capability, включая её скиллы. Capability появилась до колонки `policy` и была размечена целиком. Переведены в `seed`; валидатор теперь отклоняет managed-документ в корне или `docs/`, если он не объявлен в `STANDARD_OWNED_PROSE` — список прозы, которой владеет стандарт. |
| 208 | Ключи `.dev.env`, которые читает диагностика, не доставлялись ни в одном образце | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | В проект ехал только upstream-овский `.dev.env.example` с ключами `PLATFORM_PATH`, `IB_USER`; наш шаблон с `EDT_MCP_VERSION`, `EDT_ORDINARY_PLUGIN`, `MCP_PROVIDER_MANIFEST` не был объявлен ни одной строкой манифеста. Пользователь копировал полученный образец и получал от `doctor-1c` сообщение, что ключ не записан. Тест при этом был зелёным: он сверял каталог с файлом в чекауте и называл его «shipped». Наш образец доставляется как `.dev.env.standard.example` (в upstream-файл дописать нельзя — он побайтный), скилл и `EDT_SETUP.md` объясняют, что `.dev.env` собирается из двух образцов, а тест проверяет манифест, а не чекаут. |
| 209 | Обязательная CI-джоба `cross-repo-e2e` не могла покраснеть на Windows | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | Два шага в одном многострочном `run`, а shell по умолчанию на Windows — `pwsh -command`: возвращается код последней команды. Падение `check_best_practices_contract.py` было невидимо, и защита ветки опиралась на проверку, которая не умеет отказывать. Ту же ловушку в соседней job обходят циклом с `$LASTEXITCODE`; здесь достаточно `shell: bash` и `set -e`. |
| 197 | Command-owner workflows не поставлены | 2026-07-31 | 2026-07-31 | ветка `feat/1c-command-owners` | Решение S.8 сохраняет смысл тринадцати upstream-команд в skills, но два владельца не были написаны: `evolve-1c-rules` (единственный writer `LLM-RULES.md` по S.5.3) и `manage-1c-mcp` (install/update/repair провайдера по S.8). Оба добавлены с мостами Claude. Вместе с закрытым №165 это закрывает вторую половину критерия 22. |
| 165 | `deploy-1c-source` и `deploy-and-test-1c` не имеют исполнителя | 2026-07-27 | 2026-07-31 | ветка `feat/1c-command-owners` | Оба skill'а были зарегистрированы наравне с исполняемыми и описывали операцию, которой в поставке не было. Дефект и был записан как ожидающий импорта upstream, откуда приходят команды пакетного режима. Импорт выполнен: `db-load-xml.ps1`, `db-load-git.ps1`, `db-load-cf.ps1` и обязательный после любой загрузки `db-update.ps1` поставляются в `.agents/skills/1c-metadata-manage/tools/1c-db-ops/scripts/`. Разделы «Исполнителя нет» заменены на фактический маршрут; для `deploy-and-test-1c` честно осталось, что исполнителя самих тестов capability не поставляет — он зависит от установленных компонентов и его отсутствие даёт `SKIP` с причиной. |
| 154 | Гейты skills не доходят до Claude Code до импорта upstream | 2026-07-27 | 2026-07-31 | ветка `feat/1c-readiness-after-delivery` | Мосты `.claude/skills` были отложены вместе с agent-проекциями, поэтому capability-skills видел только Codex, а таблица разрешений ставила для Claude второй барьер при отсутствующем первом. Одиннадцать 1С-skills получили мосты, доставляемые строками `config/capabilities.tsv`. Подъём правил 1, 2, 5, 7 и 8 в scoped `AGENTS.md` был смягчением на время отсутствия моста и остаётся как есть: правила уровня сессии полезны сами по себе. |
| 145 | Skills capability не получают Claude-мост в созданном проекте | 2026-07-26 | 2026-07-31 | ветка `feat/1c-readiness-after-delivery` | `capabilities.tsv` доставлял только `.agents/skills/**`, поэтому `setup-atlassian-mcp` и `work-with-jira-confluence` видел Codex, но не Claude Code. Причина была в словаре манифеста: `bridge` знал только `required` (мост в этом репозитории) и `none`, и выразить «мост, который capability ставит в созданный проект» было нечем — отсутствие моста выглядело как осознанный выбор. Добавлено значение `delivered`; его проверка требует и файла моста, и строки доставки, потому что шаблон, который никто не ставит, — это файл, а не мост. |
| 198 | Матрица готовности объясняла критерий 7 дефектом, который не мог его закрыть | 2026-07-31 | 2026-07-31 | ветка `feat/1c-readiness-after-delivery` | Критерий 7 стоял как «не выполнено» со ссылкой на №172 «upstream импортирован, но не доставлен», то есть обещал закрыться вместе с доставкой. Но upstream не содержит ни одной обработки: на закреплённом commit ноль файлов `.epf`/`.erf`. Настоящий блокер — решение 1.16, пересборка обработки Toolkit для `managed`, которую план прямо называет отложенной. Ссылка на дефект вместо решения превращала осознанно отложенную работу в невыполненную, и после доставки критерий выглядел бы просто забытым. У матрицы не было статуса для «отложено решением», потому что дефектом это не считается; статус добавлен, его доказательство — номер решения, а не номер дефекта. |
| 175 | Версия БСП нигде не хранится | 2026-07-28 | 2026-07-31 | ветка `feat/1c-base-card` | Поставлена карточка базы `configurations/PROJECT_1C.template.md`, которую `add-1c-base` копирует в `configurations/<base>/PROJECT_1C.md`. Версия БСП живёт там, а не в реестре: она меняется вместе с обновлением типовой и нужна человеку и агенту при выборе API, а не машинной проверке. Реестр хранит то, что проверяет валидатор. |
| 179 | Сканер секретов не знает имён рабочих баз | 2026-07-28 | 2026-07-31 | ветка `feat/1c-base-card` | Источником имён стал реестр сканируемого дерева: `test-no-secrets.py` читает `project_id`, `environment_id` и `configuration` из `config/1c-projects.tsv` и ищет их вне файлов, которые про эти базы и написаны. В самом стандарте реестр пуст по построению, поэтому проверка там пуста — и именно поэтому она прогоняется на фикстуре с заполненным реестром: придуманный список правдоподобных имён не проверял бы ничего. |
| 180 | Блокировка runtime-операций 1С на не-Windows не проверяется | 2026-07-28 | 2026-07-31 | ветка `feat/1c-base-card` | `one_c_session.require` отказывает в операции с живой базой вне Windows и называет, что репозиторий, документация, ревью и валидаторы доступны на любой ОС. Отказ стоит там, где начинается runtime, а не объявлен правилом про всю capability; тест проверяет обе ветки подстановкой платформы, поэтому проходит на любой машине. |
| 186 | Порт `6003` из диапазона Toolkit занят платформой, и это не обнаруживается | 2026-07-28 | 2026-07-31 | ветка `feat/1c-base-card` | `one_c_doctor.port_rows` проверяет фактическую занятость назначенного порта попыткой bind, а не подключением: подключение отвечало бы о том, кто слушает где угодно, а вопрос — может ли эта машина поднять сервер базы. Правило выделения портов не меняется и topology не переписывается: отчёт называет занятый порт, решение принимает человек. |
| 101 | Документированные имена output `transcribe` не совпадают со скриптом | 2026-07-25 | 2026-07-31 | ветка `feat/transcribe-skill` | Имена результатов задаются одной таблицей `OUTPUTS` в скрипте, а `SKILL.md` называет ровно их; тест сверяет документ с кодом, а не с копией обещания. Английские имена приняты каноническими (решение плана), русских имён скрипта больше нет — второй контракт означал, что результат нельзя найти надёжно. |
| 102 | `transcribe` не гарантирует безопасный lifecycle загруженного Gemini media | 2026-07-25 | 2026-07-31 | ветка `feat/transcribe-skill` | Проблема снята вместе с причиной: медиа никуда не загружается. Backend локальный (`faster-whisper`), внешнему AI-клиенту уходит только то, что запросил пользователь, — готовый transcript для summary и кадры для разбора интерфейса, и оба случая названы в skill и в карточке проекта. Тест запрещает в коде импорты и вызовы внешних API. |
| 103 | Runtime/provider contract `transcribe` невоспроизводим | 2026-07-25 | 2026-07-31 | ветка `feat/transcribe-skill` | Зависимости названы классами и проверяются: Python 3.9+ — required, `faster-whisper`, модель и FFmpeg — conditional, каждая с указанием, зачем и когда нужна. Отсутствие зависимости даёт `TranscribeError` с именем пакета и правилом установки, а не ImportError; пользовательская документация `TRANSCRIBE.md` обязана их перечислять, и это проверяется тестом. |
| 104 | Ошибка ffmpeg split может дать пустую транскрипцию с успешным завершением | 2026-07-25 | 2026-07-31 | ветка `feat/transcribe-skill` | Ровно этот случай стал двумя отдельными отказами: ненулевой код ffmpeg и нулевой код без единого созданного кадра. Расшифровка без сегментов — тоже отказ: тишина, повреждённый файл и несостоявшееся декодирование неразличимы отсюда, и все три означают, что результата нет. Часовая разбивка исчезла вместе с внешним API. Результаты пишутся одной транзакцией: половина набора с правильными именами неотличима от целого. |
| 146 | Reference-файлы skills capability не упомянуты ни в одном `SKILL.md` | 2026-07-26 | 2026-07-31 | ветка `fix/standard-tails` | Оба файла теперь названы в своих skills: `references/configuration.md` — в шаге настройки `setup-atlassian-mcp`, `references/models.md` — в первом шаге `work-with-jira-confluence`. И появилась проверка, которой не было: `check_skills.check_references` считает ошибкой файл в `references/`, на который канонический `SKILL.md` не ссылается. Осиротевший reference хуже отсутствующего: он поставляется, ревьюится и должен оставаться верным, но выглядит покрытием. |
| 147 | `test-bootstrap.ps1` падает на macOS с PowerShell 7 на mock-git | 2026-07-26 | 2026-07-31 | ветка `fix/standard-tails` | Причина оказалась в `.gitattributes`: `*.ps1 text eol=crlf` выкладывает файл с CRLF на любой ОС, поэтому here-string внутри него переносил CRLF в создаваемый `mock-bin/git`. Shebang читался как `#!/bin/sh<CR>`, ядро искало интерпретатор с таким именем и сообщало «No such file or directory» — про git, а не про shell. Windows-ветка пишет `.cmd`, где CRLF безвреден, поэтому CI был зелёным. Генерируемый скрипт нормализуется в LF, наличие CR проверяется сразу после записи, а suite PowerShell добавлен в macOS-прогон: без него этот класс снова был бы невидим. |
| 149 | `source_selector` не имеет контракта | 2026-07-26 | 2026-07-31 | ветка `fix/standard-tails` | Грамматика селектора зафиксирована и проверяется: `-` — весь файл, имя клиента — проекция всего файла, `heading:<заголовок>` — фрагмент. Запрещено объявлять один и тот же файл и целиком, и по частям: одна запись хэша не может описывать оба случая. Два фрагмента одного файла с одинаковым хэшем — тоже ошибка: это значит, что хэш взят от файла и не проверяет ни один из фрагментов. |
| 199 | `deploy-and-test-1c` не возвращает отчёт YAxUnit | 2026-07-31 | 2026-07-31 | ветка `feat/1c-yaxunit-report` | Разбор отчёта появился (`scripts/one_c_yaxunit.py`): jUnit читается, провал, ошибка и пропуск внутри набора считаются отдельно, потому что зовут к разной работе — дефект в коде, дефект в обвязке и непокрытое место. Главное различие в другом: `SKIP` теперь означает ровно «YAxUnit не установлен», а установленный YAxUnit, не оставивший отчёта, даёт `FAIL`. Прежняя формулировка «отсутствие даёт SKIP» покрывала оба случая, то есть не состоявшийся прогон выглядел пропущенным, а конвейер — зелёным. |
| 200 | Таблица подплана среды и каталог компонентов расходятся в способе установки | 2026-07-31 | 2026-07-31 | ветка `fix/1c-catalog-followups` | Таблица подплана перестала быть вторым списком: она генерируется из `config/1c-components.tsv` (`one_c_components.render_table`, флаг `--table`) и сверяется тестом точным сравнением блока между маркерами. Прежняя проверка искала первое слово имени подстрокой и расхождение способов установки увидеть не могла в принципе. |
| 201 | Windows prerequisites не имеют носителя ни в каталоге, ни в исполнителе | 2026-07-31 | 2026-07-31 | ветка `fix/1c-catalog-followups` | В каталоге появилась колонка `prerequisites`, в prompt — поле «Требуется заранее». Docker Desktop называет WSL 2, Virtual Machine Platform, виртуализацию в BIOS/UEFI и `LanmanServer`; остальные строки — свои предусловия. Компонент, который отказывается ставиться без единой строки о причине, и был тем, ради чего поле нужно. |
| 202 | Ключи `.dev.env` объявлены дважды: в каталоге и в коде диагностики | 2026-07-31 | 2026-07-31 | ветка `fix/1c-catalog-followups` | `one_c_doctor.catalog_key` берёт ключ из каталога по имени компонента; жёсткое значение осталось только запасным вариантом на случай, когда каталог не читается вовсе. Тест сверяет, что диагностика читает именно объявленные ключи. |
| 203 | Команды каталога зовут `python3`, а проверка идёт текущим интерпретатором | 2026-07-31 | 2026-07-31 | ветка `fix/1c-catalog-followups` | В командах каталога плейсхолдер `{python}`, который `one_c_setup.command_of` подставляет `sys.executable`. Тест запрещает литеральный `python3` в любой команде каталога. |
| 204 | Docker Desktop объявлен Windows-компонентом, хотя нужен на любой ОС | 2026-07-31 | 2026-07-31 | ветка `fix/1c-catalog-followups` | Строка Docker Desktop переведена в `platform: any`: зависящий от неё `required`-компонент «Внешний MCP provider» объявлен `any`, и диагностика зондирует `docker` на любой ОС. Итог `ready` при отсутствующем Docker больше не получается. |
| 173 | Гейты и диагностика среды проверяются только текстом skill | 2026-07-28 | 2026-07-31 | ветка `feat/1c-environment-executor` | Правило без исполнителя — это текст, которому следует тот, кто его прочитал. Закрыто в три приёма: гейт живой базы получил `scripts/one_c_session.py` (критерии 8, 9), ядро диагностики — `scripts/one_c_doctor.py` (13, 14), а здесь — остальные семь. Каталог компонентов стал данными (`config/1c-components.tsv`), поэтому prompt собирается из обязательных полей, а не пересказывается: строка без последствий отказа или без источника — ошибка загрузки. `scripts/one_c_setup.py` запускает только команду каталога и только по записанному ответу, после чего перепроверяет состояние тем же детектором — «команда завершилась успешно» результатом не считается — и никогда не пишет внутрь проекта, поэтому отказ установки не трогает готовый репозиторий. `scripts/one_c_provider.py` обнаруживает внешний deployment и отказывает на `docker run`, `compose` и `start`: второй комплект контейнеров отвечает на тех же портах и выглядит рабочим. Плагин и профили запуска требуются только строкам `application_kind=ordinary`, а `approved-write` не берётся без названной резервной копии и не берётся на production вовсе. |
| 172 | Upstream импортирован, но не доставлен в проект | 2026-07-28 | 2026-07-31 | PR #46 | Ledger был собран, а строк доставки не было: критерии 22 и 25 требовали строк `config/capabilities.tsv`, а не строк ledger. Доставка выполнена — 161 файл под `templates/new-project/capabilities/1c/upstream/`, сгенерированные строки `capabilities.tsv` и `skills.tsv`, манифесты десяти vendored skills. Критерий 25 закрыт тестом, критерий 22 — наполовину: тринадцать ролей поставлены в обоих клиентах и проверяются, тринадцать команд вынесены в №197. Критерий 7 к этому дефекту отношения не имел — см. №198. |
| 196 | `.gitattributes` стандарта портит vendored payload при каждом checkout | 2026-07-30 | 2026-07-30 | ветка `feat/1c-delivery` | Правило `*.ps1 text eol=crlf` действовало и на каталог доставки, поэтому 60 из 161 файла разворачивались в CRLF при checkout: в индексе байты верные, на диске — нет. Побайтность, ради которой введён класс `verbatim`, отменялась настройкой самого репозитория, и на любой платформе, а не только на Windows. Тот же класс, что Э3 ловил у генерируемого `.gitattributes` созданного проекта. В upstream нет ни одного CRLF-файла, поэтому нормализация каталога отключена целиком: `templates/new-project/capabilities/1c/upstream/** -text`. |
| 195 | Ветка доставки оставляет release в состоянии `blocked` | 2026-07-30 | 2026-07-30 | ветка `feat/1c-delivery` | Исправление маршрутизации `openspec/**` сдвинуло пять целевых путей в ledger, а `release_id` остался от `0.1.0`, то есть перестал удостоверять фактическую поставку. Состав доставки изменился, поэтому release пересобран на закреплённом staging и выпущен как `0.2.0` (`9f304996a809…`, 254 артефакта, статус `ready`). |
| 194 | Доставка payload молча отняла три seed-шаблона у их назначения | 2026-07-30 | 2026-07-30 | ветка `feat/1c-delivery` | У трёх назначений оказалось два производителя: наши шаблоны и строки маршрутизации `copy`. Генератор разрешил коллизию молча в пользу upstream, вычеркнув наши строки из `capabilities.tsv`, — выбор, которого не делало ни одно решение плана. Разведено действием `adapt`: upstream остаётся источником, наш текст накладывается адаптациями `project-user-rules`, `project-memory`, `project-llm-rules` со ссылками на S.5.1–S.5.3, и при изменении upstream пересборка падает с указанием на изменившийся фрагмент. Попутно снят более общий дефект генератора: класс поставки выводился константой `verbatim`, из-за чего вывод `adapt` не мог нести плейсхолдеры; теперь класс следует из действия, а `template` получает только seed. Дублирующие `*.template.md` удалены. |
| 178 | Потолок в десять экспонируемых баз не проверяется | 2026-07-28 | 2026-07-31 | ветка `fix/1c-verifiability` | Потолок высказан там, где он достигается. Уникальность порта внутри диапазона и так делала одиннадцатую экспонируемую базу невозможной, но отказ приходил как «порт занят» или «нужен порт из диапазона» — сообщения про одну строку, тогда как факт относится ко всему реестру. Регистрация баз по-прежнему не ограничена: те же одиннадцать проходят, пока одиннадцатая не экспонирована. Проверено тестом, падающим при удалении проверки. |
| 177 | Инвариант ядра capability не проверен со стороны миграции | 2026-07-28 | 2026-07-31 | ветка `fix/1c-verifiability` | Инвариант ядра со стороны миграции оказался не отсутствующим, а непроверенным: планировщик вызывает `validate_metadata`, и проект с capability `1c` и профилем ниже `operated` он уже блокировал. Теперь это закреплено двумя тестами — блокировка при снятом ядре и `up_to_date` при целом, чтобы guard не мог быть ослаблен незаметно и не мешал нормальному проекту. |
| 193 | `test-payload-classes.py` падает на Windows после доставки payload | 2026-07-30 | 2026-07-31 | ветка `fix/1c-wrapper-discovery` | Причина немого падения не найдена, но само молчание устранено там, где оно и вредит: скрипт печатает причину сам. Проверено: параметрическая ошибка в stderr доходила, а брошенная изнутри try/finally терялась; теперь catch пишет сообщение и выходит с кодом 1. Bootstrap, падающий без единой строки вывода, непригоден независимо от того, чей это баг. Уточнение при переносе: PowerShell-ветка bootstrap завершалась кодом 1 молча, поэтому искать нужно было причину немого падения, а не текст сообщения; при `pwsh` 7.6.3 на macOS тот же тест проходил, то есть дефект был специфичен для Windows PowerShell 5.1, а не для PowerShell вообще. |
| 189 | `check-environment.*` не знает мест установки вне `PATH` | 2026-07-30 | 2026-07-31 | ветка `fix/1c-wrapper-discovery` | check-environment спрашивает канон cli_discovery.py, когда Python найден, и обходит PATH только запасным путём. Для shell добавлен режим --porcelain: разбирать JSON в POSIX sh значит писать парсер, а парсер на скорую руку — это место, где всё ломается. На машине вехи W расхождение исчезло: codex теперь [ ok ] в обеих проверках. |
| 188 | Обёртки `check-cli.*` и `check-environment.*` не покрыты тестом | 2026-07-30 | 2026-07-31 | ветка `fix/1c-wrapper-discovery` | Добавлен scripts/test-powershell-wrappers.ps1: фикстуры вместо рассуждений — нулевая заглушка не выбирается и не запускается, рабочий интерпретатор за ней находится, обёртка, пишущая в stderr, не убивает проверку. Тест также требует, чтобы определение существовало ровно в одном файле. |
| 185 | PowerShell-обёртки ищут Python только по первому совпадению в `PATH` | 2026-07-30 | 2026-07-31 | ветка `fix/1c-wrapper-discovery` | Определение вынесено в scripts/lib/Find-Python.ps1 и подключается всеми одиннадцатью обёртками: все совпадения вместо первого, пропуск файлов нулевой длины без запуска, перехват отказа запуска. Тот же first-match у git в bootstrap-new-project.ps1 исправлен там же. Одиннадцать копий одной функции несли одну ошибку — теперь копия одна. |
| 192 | Placeholder-проверка не знает про vendored payload | 2026-07-30 | 2026-07-30 | commit `bca12c9` | Созданный проект с capability `1c` не проходил валидацию: upstream поставляет литерал даты-заглушки, а проверка читала его как неподставленный placeholder шаблона. Побайтно доставленный чужой файл не обязан соблюдать правила наших документов, и править его нельзя — release записал его хэш. Признак берётся из класса поставки: строки `capabilities.tsv` с `verbatim` или `binary` исключаются из проверки. |
| 184 | Сверка матрицы не видела инверсию смысла и ломалась на разметке | 2026-07-28 | 2026-07-28 | ветка `feat/1c-readiness` | Совпадение по множеству слов не различает «работают» и «НЕ работают» — отрицание только увеличивает пересечение. Экранированный `|` внутри ячейки разбирался как разделитель колонок, переименованный заголовок давал traceback вместо диагноза, вложенный пункт в плане поглощался родительским критерием, а счёт в «Итоге» проверялся близостью числа к слову. Теперь слова, меняющие полярность, не могут появиться в строке, если их нет в плане; разметка, заголовки и счёт разбираются явно. |
| 183 | Статус `MOVED` в проверке ссылок был недостижим | 2026-07-28 | 2026-07-28 | ветка `feat/1c-readiness` | `urlopen` сообщает о 404 исключением `HTTPError`, которое является подклассом `URLError`, поэтому «источник переехал» — единственное, ради чего существует отчёт — было неотличимо от «сети нет». Тест подсовывал мок с атрибутом `status = 404`, формой, которой реальный `urlopen` не порождает: ветка была мёртвой, а тест зелёным. Добавлен повтор через `GET` для порталов, отвечающих 403/405 на `HEAD`. |
| 182 | Сканер не видел Windows-пути, а самотест был написан под баг | 2026-07-28 | 2026-07-28 | ветка `feat/1c-readiness` | Паттерн требовал двух обратных слэшей, поэтому путь в том виде, в каком его пишет человек, проходил молча в `.md`, `.ini`, `.ps1` — на capability, чья целевая платформа Windows. Самотест подавал удвоенную форму и подтверждал сломанное покрытие: тест описывал баг, а не требование. Попутно: маркер `noscan` отключал проверки по вхождению подстроки где угодно в строке, бинарный файл без расширения валил сборку как «не UTF-8», а `.xml` и `.bsl` — формат исходников 1С — не сканировались вовсе. |
| 181 | Проверка внешних ссылок не запускалась по расписанию | 2026-07-28 | 2026-07-28 | ветка `feat/1c-readiness` | Решение 1.24 требует запуска в CI по расписанию; `check-1c-component-links.py` не был упомянут ни в одном workflow. Добавлен шагом в еженедельный `upstream-check`, где уже живёт такая же уведомляющая проверка. |
| 174 | Release не блокируется отсутствием принятой практики | 2026-07-28 | 2026-07-30 | ветка `feat/1c-release-gate` | Обязательный элемент ядра preset — стек Best Practices `1c` — мог остаться пустым, а release при этом выпускался: гейта решения 1.29 в сборке не было. `practice_gate` требует в `practices/1c` хотя бы одну практику со `status: accepted` и непустым `evidence`; `trial`, пустой evidence и отсутствие каталога блокируют выпуск. Отдельная дыра закрыта тем же изменением: паспорт обязан объявлять `best-practices` в `sources`, иначе гейт просто не запускался бы — checkout не запрашивается для источника, который не объявлен. Как pinned external component этот источник фиксируется commit и не инвентаризуется: release не создаёт второй канонический экземпляр его файлов (S.3). |
| 150 | Статусы release `ready`/`blocked`/`review-required` не выражены | 2026-07-26 | 2026-07-30 | ветка `feat/1c-release-gate` | `check_release` возвращал плоский список строк, поэтому «требует semantic review» было неотличимо от «всё хорошо» и от «блокировано» — а гейт практики без этого различия был бы просто ещё одной строкой в списке. Находка стала `Finding` — строкой, которая несёт severity, поэтому все существующие вызовы печати и поиска подстроки продолжают работать. `release_status` даёт три исхода; изменившийся source hash — `review-required` (человек должен прочитать содержимое), неполный инвентарь, конфликт и пустой стек — `blocked`. Находка без severity считается блокирующей: неклассифицированная проблема — не та, которую кто-то признал безопасной. `--write` отказывается на обоих ненулевых исходах. |
| 191 | Хэши источников считались по рабочей копии, а не по коммиту | 2026-07-30 | 2026-07-30 | ветка `feat/1c-adaptations` | Найдено при первой адаптации: якорь не совпадал, потому что рабочая копия upstream на Windows содержит CRLF, а коммит — LF. Следствие шире одной адаптации: `source_sha256` в ledger и сравнение staging в `build-capability-release.py` брались из файла на диске, поэтому при `core.autocrlf=true` тот же upstream-commit давал другой `release_id` на Windows и на Linux, а сборка на второй машине сообщала бы «source changed» без единого изменения содержимого. Контракт «одинаковый вход — одинаковый идентификатор» был ложным ровно на той платформе, которая объявлена базовой. Везде читается committed blob (`git show HEAD:<path>`) — тем же приёмом, каким это уже решено в проверке контракта Best Practices. Регрессия закреплена: staging с LF в коммите и CRLF на диске обязан дать хэш коммита. |
| 190 | Правило «admin-bypass только для release» расходится с практикой всех этапов | 2026-07-30 | 2026-07-30 | ветка `chore/align-merge-route` | Правило требовало того, что в этой конфигурации невыполнимо: ruleset `18603924` просил approval и Code Owner review, pull request открывает единственный владелец, а GitHub отвечает `Can not approve your own pull request`. Поэтому обходом влиты все PR этапов — №25–№30, №32–№36, у каждого ноль ревью, — и только два обхода 2026-07-16 попали в `ACTIONS.md`. Корневая причина не в дисциплине, а в неисполнимом контроле: правило, которое нельзя соблюсти, соблюдаться не начнёт. Требования approval и Code Owner review сняты с ruleset, обязательными остались pull request и пять зелёных required checks со strict-политикой; решение 1.25 и «Правила работы» приведены в соответствие. Побочный эффект прежнего состояния: `--admin` обходит и required checks, то есть красный CI вливался бы той же командой без предупреждения. |
| 187 | Обёртка `export-1c-source.ps1` не могла передать ни одну опцию конвертера | 2026-07-30 | 2026-07-30 | ветка `feat/1c-milestone-w` | Найдено обоими ревью вехи W: скрипт падал с usage при любом вызове, то есть XML-маршрут через штатную обёртку был неисполним на Windows — платформе, для которой он написан. Две независимые причины: значения `*-option` начинаются с дефиса, и argparse читал их как следующий флаг; Windows PowerShell 5.1 выбрасывает пустые строки из аргументов native-команды, поэтому флаг доходил без значения. Дефект предсуществующий, но веха добавляла к нему два новых параметра. Аргументы собираются формой `--option=value`, пустые пары не передаются. |
| 166 | Команда конвертера формата не зафиксирована | 2026-07-27 | 2026-07-30 | ветка `feat/1c-milestone-w` | Форма командной строки взята с живой установки EDT 2026.1.2, а не выведена: `1cedtcli -command export --project <канон> --configuration-files <выгрузка>` и `1cedtcli -command import --configuration-files <выгрузка> --project <канон>`. Корневая причина шире имени флага: направления называют одни и те же каталоги **разными** параметрами, а `export-1c-source` держал одну пару имён на оба направления — возврат получил бы флаги выгрузки. Добавлены `--back-source-option` / `--back-target-option`, обнаружение CLI в каталогах компонентов EDT — в `cli_discovery.py`, примеры — в skill и `MANUAL_SCRIPTS.md`. Сквозной прогон на реальной конфигурации не выполнялся: базы с исходниками на машине нет. |
| 137 | План опирается на discovery AI-клиента, сломанный открытым дефектом №61 | 2026-07-25 | 2026-07-30 | ветка `feat/1c-milestone-w` | Закрыт вместе с №61: контракты `activate-1c-client` и `doctor-1c` больше не спрашивают `claude --version` по имени, а вызывают `scripts/check-cli.*` и трактуют `skipped` как `SKIP` со списком отклонённых кандидатов. |
| 61 | Проверка instruction sources не запускает packaged Codex Desktop на Windows | 2026-07-14 | 2026-07-30 | ветка `feat/1c-milestone-w` | Проверка по имени даёт ложный ответ в обе стороны, и веха W показала обе: в 2026-07 имя разрешалось в нулевой файл `WindowsApps` и означало «есть» о программе, которая не стартует; в 2026-07-30 на той же машине `codex` в `PATH` отсутствует вовсе, а рабочий бинарник в профиле пользователя запускается. Корневая причина — принятие имени за программу. `scripts/cli_discovery.py` перебирает **все** совпадения в `PATH` и известные места установки вне его, отклоняет файлы нулевой длины не запуская их (запуск открыл бы Store), принимает кандидата только после фактического ответа и возвращает `skipped` с причиной по каждому отклонённому. Покрыто `scripts/test-cli-discovery.py`, включая случай «заглушка заслоняет рабочий бинарник». |
| 176 | Записи дефектов оказывались одновременно в `Open` и в `Fixed` | 2026-07-28 | 2026-07-28 | ветка `feat/1c-readiness` | №152, №153, №155 и №156 были добавлены в обе секции одним коммитом: заводя запись и закрывая её в одной задаче, я вписывал строку дважды. Секция — это и есть статус, поэтому две строки означают два противоречащих статуса. Дубли сняты, проверка добавлена в `scripts/test-1c-readiness.py`. |
| 171 | Тесты декодировали вывод CLI кодировкой платформы | 2026-07-28 | 2026-07-28 | ветка `feat/1c-source-format` | Инструменты пишут UTF-8 намеренно, а `subprocess.run(text=True)` в тестах декодировал канал кодировкой платформы: на Windows-джобе это дало `UnicodeDecodeError` в потоке чтения, `stdout` стал `None`, и падение выглядело как `TypeError`. Класс ошибки: сторона, читающая наш вывод, обязана декодировать его UTF-8 так же явно, как мы его пишем. |
| 170 | Мелкие пробелы контракта конвертации | 2026-07-28 | 2026-07-28 | ветка `feat/1c-source-format` | Отчёт о возврате ограничивался только в подробной части, поэтому дерево из тысяч файлов печатало тысячи строк; файлы в cp1251 объявлялись двоичными; путь конвертера в одинарных кавычках не разбирался; `--release` требовал читаемой записи об экспорте, хотя он и есть выход из сломанного состояния. |
| 169 | Символические ссылки переносились в канон незаметно | 2026-07-28 | 2026-07-28 | ветка `feat/1c-source-format` | Отпечаток и diff игнорировали ссылки, а `copytree(symlinks=True)` их переносил: смена цели ссылки попадала бы в канон, ни разу не показавшись человеку, либо возврат объявлялся `unchanged` и изменение молча отбрасывалось. Теперь ссылки в каноне и в staging отклоняются с указанием файлов. |
| 168 | Блокировку могли взять два процесса, а недоступный конвертер давал стек | 2026-07-28 | 2026-07-28 | ветка `feat/1c-source-format` | Тело lock писалось после его создания, поэтому конкурент читал пустой файл и считал блокировку истёкшей; реклейм по TTL был неатомарен и удалял чужой свежий lock. Возраст теперь берётся из mtime файла, реклейм идёт переименованием — выигрывает ровно один. `subprocess` бросал `FileNotFoundError` мимо `except SourceError`: незапускаемый конвертер печатал стек и код 1, который уже занят под «изменения показаны, но не применены». |
| 167 | `--apply` не сверял канон и терял ручную правку | 2026-07-28 | 2026-07-28 | ветка `feat/1c-source-format` | Проверка «канон сдвинулся» жила только в `--import`, а между показом и применением идёт разговор с пользователем. Правка канона руками в это окно затиралась молча, с кодом 0 и словами «канон обновлён» — тот же дефект, что №164, сдвинутый на шаг вправо. |
| 164 | Возврат применял не то, что показал | 2026-07-27 | 2026-07-27 | ветка `feat/1c-source-format` | `--apply` был отдельным запуском процесса и заново конвертировал дерево: между показом и применением выгрузка могла измениться, и пользователь утверждал одно, а применялось другое. Плюс diff был перечнем имён файлов без содержимого. Теперь staging сохраняется и применяется именно он, а diff показывает строки. |
| 163 | Замена канона могла его уничтожить | 2026-07-27 | 2026-07-27 | ветка `feat/1c-source-format` | Порядок «резервная копия → удалить канон → скопировать новое» терял канон, если восстановление тоже отказывало, а `folder = "."` из реестра означал удаление корня репозитория вместе с `.git`. Теперь замена идёт переименованиями, старый канон удаляется последним, а `folder` обязан быть каталогом строго внутри репозитория. |
| 162 | Уборка временных каталогов удаляла чужие выгрузки, а блокировка не работала между запусками | 2026-07-27 | 2026-07-27 | ветка `feat/1c-source-format` | Уборка обходила весь системный temp по маске и удаляла каталог, если внутри нет lock-файла, — а lock там никогда и не лежал, поэтому каждая выгрузка сносила все чужие. Блокировка хранила pid процесса CLI, который завершается сразу, поэтому следующий запуск считал её мёртвой. Проверка живости через `os.kill` к тому же непереносима: на Windows она завершает опрошенный процесс. Теперь каждый временный каталог помечен владельцем (репозиторий + база), уборка трогает только свои, а блокировка принадлежит сессии и истекает по времени. |
| 161 | CLI проекций падал на Windows на собственном выводе | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | Строка `SKIP` объясняет причину по-русски, а консоль Windows по умолчанию не UTF-8: `--write` завершался `UnicodeEncodeError` уже после записи файлов. Потоки вывода переводятся в UTF-8 с заменой; тест запускает CLI с `PYTHONIOENCODING=cp1252`, поэтому воспроизводится на любой платформе. |
| 160 | Дубль роли в каталоге и битый порт проходили в проекцию | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | Проверка уникальности имён работала только для per-base серверов, поэтому две записи одной роли давали две одинаковые таблицы в `.codex/config.toml`. Строка реестра с `mcp_enabled=true` и пустым портом давала endpoint `http://127.0.0.1:/mcp` — «выглядит установленным, отказывает при первом вызове», ровно то, что модуль объявляет недопустимым. Обе проверки перенесены на все записи. |
| 159 | Отказ на фазе переименования оставлял клиентов рассогласованными | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | Двухфазная запись защищала только от отказа staging: `Path.replace` тоже может отказать — на Windows на файле, открытом другим процессом. Первый файл оставался заменённым, второй нет. Теперь прежнее содержимое восстанавливается, staging-имя уникально (`mkstemp`), параллельный запуск не удаляет чужой файл. |
| 158 | `render_codex_config` разрушал пользовательский `.codex/config.toml` | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | Маркеры искались подстрокой без привязки к строке, без проверки количества и порядка. Висячий `BEGIN` после оборванной записи или мердж-конфликта приводил к тому, что следующая сборка вырезала всё между ним и маркером нового блока. Маркер внутри строкового значения ломал соседнюю строку. Теперь маркер — только отдельная строка, требуется ровно одна пара в правильном порядке, иначе отказ. |
| 157 | Объявленные `tools` обходили класс роли | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | `permission_rules` раскладывала инструменты по классам из каталога, игнорируя класс роли: строка `"tools": {"allow": [...]}` у роли `data` давала `allow` и убирала единственный `deny` политики. Каталог — файл проекта, то есть одна правка снимала запрет. Введены две величины: fallback (что получает сервер, пока инструменты не объявлены) и ceiling (максимум, который каталог может выдать); каталог может сузить класс, но не расширить. |
| 156 | Коллизия имён MCP-серверов теряла базу и ломала `.codex/config.toml` | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | `projected_servers` отказывается собирать проекцию при совпадении имён и называет обе строки реестра. Проверяется тестом `test-1c-clients.py`. |
| 155 | Серверное правило перекрывало точность до инструмента | 2026-07-27 | 2026-07-27 | ветка `feat/1c-skills-and-clients` | При объявленных `tools` серверное правило не выпускается вовсе; неклассифицированный инструмент спрашивает по умолчанию клиента. Тест переписан на проверку эффекта: широкого правила быть не должно. |
| 153 | `.gitattributes`-контракт 1С не поставлялся, а дефект №118 числился закрытым | 2026-07-26 | 2026-07-26 | ветка `feat/1c-project-scaffold` | Запись №118 переписана: контракт поставлен как capability-appendix, `epf`/`erf`/`cf`/`cfe`/`cfu`/`dt` объявлены `binary`, для `bsl`/`os`/`mdo`/`xml` зафиксирован eol. Проверяется тестом `test-one-c-scaffold.py`. |
| 152 | Индекс `docs/README.md` разваливался на повторяющиеся секции | 2026-07-26 | 2026-07-26 | ветка `feat/1c-project-scaffold` | Обе реализации bootstrap теперь дописывают пункт внутрь существующей секции после последнего пункта списка; тест требует уникальности заголовков в созданном проекте. |
| 118 | Нет контракта `.gitattributes` для 1С-репозитория | 2026-07-25 | 2026-07-26 | ветка `feat/1c-project-scaffold` | Generic `* text=auto` нормализовал бы поставляемые бинарные обработки и сломал их SHA-256 — гейт безопасности записи. Первоначальная запись о фиксации была ошибочной (см. №153): контракт фактически поставлен только в Э7 как capability-appendix `.gitattributes`. |
| 151 | Граница трёх реестров артефактов не описана в документации | 2026-07-26 | 2026-07-26 | текущий commit | Разделение выдерживалось в коде, но нигде не было записано, из чего во что течёт: build-time inventory, манифест доставки и ledger проекта различались только по смыслу в исходниках. В документ схемы добавлен раздел с таблицей вопросов, односторонним направлением данных и разграничением полей `ownership`, `policy` и `payload_class`. |
| 148 | Снятие ядра capability обнаруживается только по самой capability | 2026-07-26 | 2026-07-26 | `f4c2c0b` | Инвариант проверялся от `capabilities`, поэтому удаление строки вместе с манифестом практик снова давало валидный проект. Запись `capability_releases` теперь считается доказательством принадлежности: capability с установленным release нельзя убрать из `capabilities`. |
| 127 | `.best-practices.json` хранит preferences, а не реестр стеков, и стек `1c` пуст | 2026-07-25 | 2026-07-26 | `128349e` | Проверка «секция есть» не отличала подключённый стек от явного отказа. Валидатор теперь читает манифест и считает ядро нарушенным при `optout` по требуемому стеку; пустой стек в базе практик остаётся обязательством решения 1.29 и на создание проекта не влияет. |
| 126 | Инвариант ядра противоречит порядку шагов создания проекта | 2026-07-25 | 2026-07-26 | `128349e` | Валидатор запускался до записи `.best-practices.json`, поэтому обязательная проверка ядра падала на только что созданном проекте. Bootstrap записывает стеки preset сам, а инвариант «capability `1c` требует профиль не ниже `operated`» проверяется в metadata; preset раскрывается из `config/presets.tsv` одинаково в обеих реализациях. |
| 125 | SemVer capability и `release_id` не отображаются на движок миграций | 2026-07-25 | 2026-07-26 | `3755deb` | Движок знает только целочисленные переходы schema, поэтому история релизов capability в него не укладывалась. Оси разведены: артефакты capability планируются сравнением фактического состояния с ledger, а не цепочкой schema; `plan_migration.py` признаёт handler `capability_artifacts` как внешний и не пытается вести его сам. |
| 122 | Обработчик миграций `capability_artifacts` не существует | 2026-07-25 | 2026-07-26 | `3755deb` | Доставка обновлений в созданные проекты держалась на несуществующем обработчике. Добавлен `scripts/capability_artifacts.py`: многофайловый план (`create`/`update`/`remove`/`skip`), staging с переименованием и откатом всей цепочки при отказе, остановка на дрейфе вместо перезаписи, seed создаётся один раз и не удаляется, ledger обновляется только после успешной записи. CLI `apply-capability-artifacts` read-only по умолчанию и требует `--apply --yes`. |
| 124 | Механика доставки capability не поддерживает каталоги, бинарники и смешанные файлы | 2026-07-25 | 2026-07-26 | `a1ab42e` | Единственным способом доставки была текстовая подстановка `sed`/`Replace`, поэтому побайтный payload и бинарные артефакты повреждались бы при установке. В `config/capabilities.tsv` добавлена колонка `payload_class`: `-`/`template` сохраняют прежнее поведение, `verbatim` и `binary` копируются побайтно в обоих bootstrap. Неизвестный класс останавливает установку. Каталоги остаются вне манифеста по решению подплана: subtree разворачивается в строки на build-time. |
| 128 | Запись release в `.project-standard.json` требует bump схемы, не запланированного в этапах | 2026-07-25 | 2026-07-26 | текущий commit | Формат metadata менялся без повышения `STANDARD_VERSION`, из-за чего `validate_metadata` отклонил бы проект. Схема повышена до 5, добавлено поле `capability_releases` с проверкой SemVer и `release_id`, `1c` внесён в `CAPABILITY_NAMES`, добавлены миграции 0013–0015 и тест апгрейда схемы 4 → 5 с проверкой валидности результата. |
| 123 | Generic-механизм `.project-standard-artifacts.json` не спроектирован | 2026-07-25 | 2026-07-26 | текущий commit | Ledger существовал только в тексте плана: без формата, владельца схемы и валидатора. Введены `scripts/artifacts_ledger.py` со схемой 1 и правилами policy/payload_class/hash, раздел в документе схемы, проверка в `validate-project.py` для проектов и отдельная тестовая сюита. |
| 129 | Проверки skills не масштабируются на capability-скиллы и vendored subtrees | 2026-07-25 | 2026-07-26 | текущий commit | Список из девяти имён был захардкожен в обоих entry point, а обязательная триада с внутренним `agents/openai.yaml` не подходит побайтным vendored-скиллам. Введён реестр `config/skills.tsv` и общая проверка `scripts/check_skills.py` с двумя классами; обход дополнен сверкой фактических каталогов и `config/capabilities.tsv`, поэтому забытая строка реестра теперь ошибка, а не тихий пропуск. |
| 144 | UI-метаданные skills capability записаны в CP1251 и превращаются в мусор | 2026-07-26 | 2026-07-26 | текущий commit | `agents/openai.yaml` обоих skills `jira-confluence` содержали `short_description` в кодировке CP1251, поэтому в интерфейсе клиента отображалась бы нечитаемая строка, а чтение файла как UTF-8 падало с `UnicodeDecodeError`. Значения переписаны в ASCII по образцу собственных skills репозитория; проверка `check_skills.py` читает метаданные побайтно и валит ASCII-контракт вместо падения. Уточнение: исправлен шаблон; уже созданные проекты получат правку только после появления обработчика `capability_artifacts` (дефект №122). |
| 143 | В шаблоны skills capability попал неудалённый scaffolding skill-creator | 2026-07-26 | 2026-07-26 | текущий commit | `setup-atlassian-mcp/SKILL.md` и `work-with-jira-confluence/SKILL.md` содержали блок «Structuring This Skill» с инструкциями-заглушками и разделами `[TODO: ...]` — около 75 строк после реального содержания. Каждый проект, созданный с capability `jira-confluence`, получал этот мусор. Блок удалён; регресс ловится проверкой на `TODO`, которая раньше не применялась к capability-шаблонам, потому что они не были ни в одном тесте. Уточнение: исправлен шаблон; уже созданные проекты получат правку только после появления обработчика `capability_artifacts` (дефект №122). |
| 117 | План не фиксирует формат исходников конфигурации в Git | 2026-07-25 | 2026-07-26 | `2fbc80f` | Формат репозитория не был выбран, поэтому `export-1c-source`/`deploy-1c-source` не имели полного контракта, а две машины могли выгрузить одну базу по-разному. Решением 1.21 каноном выбран EDT-формат, введена колонка `source_format`, а XML-зависимые инструменты получили детерминированную конвертацию во временный gitignored каталог с явным шагом возврата и показом diff. Стоимость конвертации записана в остаточные риски. |
| 142 | Отложенное решение по hooks не пересмотрено при появлении сигнала | 2026-07-25 | 2026-07-26 | `cdf3696` | Условие возврата к отложенному решению не проверялось, хотя capability вводит необратимую запись в живую ИБ. Решением 1.27 вопрос разобран и hook отклонён с обоснованием: асимметрия Claude/Codex, исполняемая конфигурация как отдельный риск, дублирование принятых барьеров и слепота к переключению записи в UI. Остаточный риск на `managed` подтверждён как принятый. |
| 139 | Кандидат продвижения secret-safe inspection не учтён в диагностике `doctor-1c` | 2026-07-25 | 2026-07-26 | `cdf3696` | Контракт диагностики описывал только частный случай с токеном, тогда как `doctor-1c` читает файлы с допустимыми dev/test credentials. Решением 1.26 в план добавлен раздел про allowlist, запрет рекурсивного обхода профиля, исключение state/backup/session/logs и маскирование вывода; кандидат `PC-2026-e6f54a0fe78a` остаётся в backlog для отдельного решения о продвижении. |
| 136 | Release capability `1c` предписывает прямой push в защищённый `main` | 2026-07-25 | 2026-07-26 | `cdf3696` | План описывал прямой push как обычный шаг, умалчивая, что он проходит только через admin-bypass ruleset `Protect main`. Решением 1.25 маршрут сохранён, но назван bypass'ом и обязан сопровождаться записью в `ACTIONS.md` с перечнем локально прогнанных проверок. |
| 141 | Внешние URL каталога компонентов нигде не проверяются | 2026-07-25 | 2026-07-26 | `a8e7daf` | Адреса официальных источников установки были обычным текстом документа и протухали бы незаметно до момента установки. Решением 1.24 введена report-only проверка ссылок скриптом стандартной библиотеки, без новой зависимости и без блокировки сборки. |
| 140 | Бюджет цепочки инструкций проверяется только для native AGENTS | 2026-07-25 | 2026-07-26 | `a8e7daf` | Проверка учитывала только файлы AGENTS, тогда как companion-файлы попадают в контекст рекурсивным импортом и в лимит не входили. Решением 1.23 измеряется вся фактически загружаемая цепочка; порог 32 КиБ и проверка добавлены в этап 7 и критерии готовности. |
| 121 | Работа с типовой конфигурацией не отражена в модели данных проекта | 2026-07-25 | 2026-07-26 | `3fae528` | Режим поддержки и версия БСП существовали только как текст в user-owned `USER-RULES.md`, поэтому агент с write-доступом не мог проверить, допустима ли правка типовой. Решением 1.20 введена обязательная колонка `support_mode` в реестре баз, версия БСП добавлена в карточку базы, а правило «доработка расширением по умолчанию» перенесено в scoped `configurations/AGENTS.md`. Обновление через `cfu` вынесено в границы scope. |
| 120 | План не фиксирует границу scope для репозиторного lifecycle 1С | 2026-07-25 | 2026-07-26 | `3fae528` | Практики командной 1С-разработки не имели ни решения «включаем», ни решения «вне scope», из-за чего проект оставался без ответа, как код проверяется без агента. Решением 1.19 в v1 включены BSL Language Server как проверка репозитория вне MCP и модель ветвления/ревью; CI/CD, миграция из хранилища, SonarQube, АПК и `cfu` записаны как отказ с причиной. |
| 119 | Модель тестирования 1С объявлена, но не определена | 2026-07-25 | 2026-07-26 | `3fae528` | `TEST_MODEL.md`, `deploy-and-test-1c` и ключ `UI_TESTING` существовали без исполнителя: ни фреймворка, ни размещения тестов, ни контура. Решением 1.18 YAxUnit включён в v1 conditional-компонентом, описаны уровни проверок и контракт прогона, а отсутствие UI-тестов в v1 названо явно. |
| 138 | План не связывает свой маршрут с открытыми дефектами валидатора и стандартизации | 2026-07-25 | 2026-07-26 | `a7f1008` | Запись опиралась на устаревший раздел `Open`: дефекты №69, №70 и №71 были исправлены ещё 2026-07-16 в `a210e02`, но их записи не перенесли в `Fixed`. Проверка кода и прогон `test-validator.py` и `test-standardize-existing-project.py` подтвердили исправления. Оставшееся предусловие — только №61, оно покрыто отдельной записью №137. Корневая причина шире одного плана: несинхронизированный журнал дефектов заставляет повторно принимать решения по уже закрытым проблемам. |
| 71 | Планировщик стандартизации игнорирует profile из `.project-standard.json` | 2026-07-16 | 2026-07-16 | `a210e02` | При `--profile auto` профиль выводился эвристикой по числу отсутствующих артефактов, а authoritative `profile` из `.project-standard.json` не читался, из-за чего adopted-проект получал рекомендацию `re-bootstrap-from-existing`. `detect_candidate_profile` теперь принимает `metadata_profile` и использует его при `auto`, как это уже делал `validate-project.py`. |
| 70 | Резолвер wikilink видит только `.md` и не разрешает ссылки на вложения | 2026-07-16 | 2026-07-16 | `a210e02` | Индексы `direct` и `by_stem` строились только из Markdown-файлов, хотя обход отдаёт все git-tracked файлы, поэтому ссылка на существующее вложение никогда не разрешалась. Добавлен отдельный индекс `by_stem_attachment`, к которому резолвер обращается, когда заметки с таким stem нет; приоритет заметки над вложением сохранён. |
| 69 | Валидатор считает битым wikilink с экранированным `\|` внутри Markdown-таблицы | 2026-07-16 | 2026-07-16 | `a210e02` | `WIKILINK_RE` захватывал хвостовой `\` в target, а нормализация превращала его в `/`, поэтому обязательное для Obsidian экранирование alias-разделителя внутри таблиц давало ложную `wikilink.missing`. Target теперь очищается `rstrip("\\")` до нормализации разделителей. |
| 135 | Upstream-инвентарь плана не позволяет проверить полноту будущего ledger | 2026-07-25 | 2026-07-26 | `7050d44` | Полнота декларировалась прозой по секциям, без суммарной контрольной величины, поэтому проверить её было нечем. В разбор источника внесены 241 tracked-файл как критерий приёмки ledger и поимённо названы ранее собирательные позиции; `content/mcp-servers.json` получил действие `route:1c-mcp-catalog` вместо статуса «запрещён». |
| 134 | Раздел «Проект разработки кода» отрицает skill `develop-1c` и содержит устаревший счёт skills | 2026-07-25 | 2026-07-26 | `7050d44` | Раздел писался до решения S.6 и не был обновлён после появления `develop-1c`, а счёт skills устарел после решений 1.11 и 1.14. Основным dev-skill назван `develop-1c`, правило о кандидате в Best Practices сужено до непокрытых практик, порядковый номер убран. |
| 133 | Классы разрешений Claude обещаны разделу «Безопасность», но там не определены | 2026-07-25 | 2026-07-26 | `7689a1e` | Forward-ссылка из раздела адаптеров не была закрыта при написании раздела безопасности. Решением 1.17 добавлен подраздел «Классы разрешений AI-клиента» с распределением по всем ролям каталога и явным обоснованием единственного `deny`. |
| 132 | Размещение `1c-projects.tsv` и `ENVIRONMENT_REGISTRY.md` задано двумя несовместимыми способами | 2026-07-25 | 2026-07-26 | `7050d44` | Дерево рабочей области рисовалось раньше остальных разделов и предполагало отдельную папку `1C/`, а последующие разделы использовали корневые пути. Решением 1.15 выбран корень проекта: дерево, scoped-правила (`configurations/AGENTS.md`) и все упоминания приведены к одному варианту. |
| 131 | Таблица MCP-каталога закрепляет порты внешних Docker MCP | 2026-07-25 | 2026-07-26 | `7050d44` | Таблица осталась от модели, в которой capability сама назначала порты provider-shared контейнеров. Пересмотренное решение 1.8 передало endpoint provider manifest, но таблицу не обновили. Литеральные порты заменены на «endpoint из provider manifest», добавлен абзац о единственном собственном диапазоне `6003`–`6012`. |
| 130 | Профили запуска EDT противоречат решению 1.6 о типе приложения | 2026-07-25 | 2026-07-26 | `7050d44` | Раздел профилей писался до разделения по `application_kind` и переносил ordinary-атрибут `ATTR_CLIENT_TYPE` и read-only EPF на все базы. Профили разведены: для `ordinary` — оба шаблона, для `managed` профиль пока не поставляется, `doctor-1c` проверяет ordinary-атрибут условно. Автозапуск для `managed` требует пересборки штатной обработки Toolkit и зафиксирован отдельным решением 1.16 как отложенная работа. |
| 116 | Детальная проработка крупного 1С-плана накапливалась в одном файле | 2026-07-25 | 2026-07-25 | текущий commit | Мастер-план одновременно служил картой решений, подробной спецификацией всех подсистем и журналом обсуждения. Из-за этого связанные решения было трудно проверять и синхронизировать, что уже проявлялось в дефектах №88 и №112. В `AGENTS.md`, глобальную политику и шаблон нового проекта добавлено правило «master plan = краткая карта, детали = связанные подпланы»; принятый контракт среды вынесен в отдельный двусторонне связанный подплан. |
| 115 | План смешивал создание проекта с настройкой машины и называл feature-зависимости optional | 2026-07-25 | 2026-07-25 | текущий commit | Границу транзакционного bootstrap ошибочно превратили в пользовательский запрет на установку, а все неядерные runtime packages объединили словом `optional`, хотя без них недоступны заявленные функции. Зафиксирован единый двухфазный workflow `create → setup → doctor` с отдельным approval внешних изменений и классы `required`/`conditional`/`optional`; Node.js, `docx`, Pillow, `v8unpack` и OpenSpec CLI стали conditional. |
| 114 | План не определяет позднее подключение второго AI-клиента | 2026-07-25 | 2026-07-25 | текущий commit | Постоянная поставка двух статических проекций была ошибочно принята за полный lifecycle: она не материализует актуальную topology для клиента, установленного позже. Добавлен идемпотентный `activate-1c-client`, client-scoped диагностика, `SKIP` отсутствующего CLI и тест поздней активации без re-bootstrap или изменения уже работающего клиента. |
| 82 | Автопереназначение порта вносит машинный конфликт в shared MCP-конфигурацию | 2026-07-25 | 2026-07-25 | текущий commit | План смешивал shared topology с локальной занятостью сокета и предписывал машинному конфликту менять committed TSV, EDT-профиль и client projections. Теперь назначение порта стабильно: `doctor-1c` блокирует локальный конфликт и предлагает освободить порт; переназначение возможно только как явное общее изменение topology. |
| 81 | Диапазон портов ограничивает число зарегистрированных баз, а не только одновременно запущенных | 2026-07-25 | 2026-07-25 | текущий commit | Уникальность порта ошибочно применялась ко всем строкам TSV. Теперь `server_port` обязателен и уникален только для `mcp_enabled=true`, у отключённых строк пуст; зарегистрированных баз может быть сколько угодно, одновременно экспонируемых — не более десяти. |
| 113 | Обязательность Docker ошибочно означала блокировку всех 1С-workflow | 2026-07-25 | 2026-07-25 | текущий commit | При синхронизации обязательного external provider формулировка «1С-workflow блокируется» стала шире уже принятой точечной деградации: отсутствие контейнерных MCP не делает недоступными независимые EDT и Toolkit. Текст сужен до MCP-зависимой разработки и обязательных проверок со ссылкой на таблицу доменов отказа. |
| 112 | Хвост плана capability `1c` не был синхронизирован с решениями S.1–S.13 | 2026-07-25 | 2026-07-25 | текущий commit | Последовательный разбор upstream обновлял канонические секции S.1–S.13, но не имел обязательного финального прохода по перечню артефактов, skills, зависимостям, этапам и критериям готовности. Поэтому там остались старое имя `verify-1c-workspace`, ровно шесть skills, собственное Docker-развёртывание capability и слишком широкое Windows-only ограничение. Хвост синхронизирован; отдельные нерешённые противоречия портов №81–82 намеренно не замаскированы. |
| 111 | INDEX преждевременно называл draft-план capability `1c` утверждённым | 2026-07-25 | 2026-07-25 | текущий commit | После возврата плана в draft навигационная строка сохранила старое слово `accepted`, хотя итоговый approval ещё не получен. INDEX приведён к фактическому статусу последовательной проработки. |
| 110 | Generic skill `transcribe` ошибочно считался частью capability `1c` | 2026-07-25 | 2026-07-25 | текущий commit | Полнота импорта из 1С-репозитория была ошибочно отождествлена с 1С-владением каждого payload. S.9.10 теперь исключает skill из capability, сохраняет явный import route и переносит все предложенные изменения в отдельный общий план. |
| 109 | Корневые support-файлы upstream не имели import routes | 2026-07-25 | 2026-07-25 | текущий commit | После разбора функциональных payload оставались `README.md`, `References.md` и `.gitignore`: прямое копирование README затёрло бы identity проекта, а отказ потерял бы справочник и runtime ignore rules. S.13 назначил provider-only documentation route, побайтный справочник и managed merge ignore patterns. |
| 108 | Upstream installer конкурировал бы с lifecycle стандартного проекта | 2026-07-25 | 2026-07-25 | текущий commit | `install.ps1` объединяет init/update/client placement/MCP/doctor/remove в собственном `.ai-rules.json`, тогда как план уже назначил эти операции bootstrap, migrations, renderer и validator. S.12 классифицировал installer как provider-only reference и пофункционально маршрутизировал полезные контракты без второго runtime owner. |
| 107 | Upstream adapters не имели явной границы с принятым client renderer | 2026-07-25 | 2026-07-25 | текущий commit | Одиннадцать YAML можно было либо потерять, либо ошибочно использовать как второй installer, получив глобальные Codex prompts, статический MCP и дубли rules/skills. S.11 сохранил все adapters как build inputs, назначил compiled Codex/Claude runtime descriptors и закрепил приоритет контрактов S.4.2/S.7/S.8. |
| 106 | OpenSpec оставался отложенным без import и lifecycle contract | 2026-07-25 | 2026-07-25 | текущий commit | Основной ruleset ссылался на отдельный будущий разбор, но план не назначал судьбу scaffold, четырёх workflow, client bundles, CLI dependency и approval boundary. S.10 закрепил полный route, роли относительно `PROJECT.md`/ADR/docs, обязательный approval перед `apply` и build-time refresh. |
| 105 | Два оставшихся dependency-light skill не имели явного import route | 2026-07-25 | 2026-07-25 | текущий commit | `prompt-enhancer` и `v8unpack-cf` оставались в upstream inventory без решения, хотя внутренней адаптации не требуют. S.9.9/S.9.11 закрепили побайтный payload, внешние client bridges, user-owned outputs и внешний optional dependency contract для `v8unpack`. |
| 100 | План не фиксирует общий PowerShell guidance для Windows-first 1С-операций | 2026-07-25 | 2026-07-25 | текущий commit | Upstream agents ссылались на `powershell-windows`, но отдельного import/update-контракта skill не было; generic guidance также можно было ошибочно активировать на POSIX или превратить в владельца provider lifecycle. S.9.8 закрепил побайтный platform-scoped skill, внешнее владение Compose workflow и Windows PowerShell 5.1 contract tests. |
| 99 | План не назначает поставку generic skill `mermaid-diagrams` внутри полной 1С-capability | 2026-07-25 | 2026-07-25 | текущий commit | Полнота upstream требовала явного route даже для generic documentation skill, но он не имел import/update-контракта и мог быть ошибочно превращён в обязательный renderer. S.9.7 зафиксировал побайтный dependency-free skill, условный trigger, обязательный sidecar и запрет автоматической передачи приватных данных внешнему сервису. |
| 98 | `md-to-docx` не фиксирует фактическую минимальную версию Node.js | 2026-07-25 | 2026-07-25 | текущий commit | Upstream называл только Node.js, хотя bundled `nanoid` исключает старые runtime. S.9.6 сохранил payload без изменений, но закрепил effective Node 18 LTS/≥20 во внешнем dependency contract, `doctor-1c` и отдельной строке пользовательской документации; npm packages устанавливаются локально из lock только с разрешения. |
| 97 | `overlay-grid.py` допускает деление на ноль для некорректной или предельно плоской сетки | 2026-07-25 | 2026-07-25 | текущий commit | Upstream не валидировал `cols`/`rows`, а auto-расчёт мог вернуть ноль. S.9.5 разрешил единственную внутреннюю адаптацию скрипта — guards положительных размеров, включая auto-result, с regression tests для zero/flat inputs. |
| 96 | `img-grid-analysis` требует Pillow без управляемого dependency-контракта | 2026-07-25 | 2026-07-25 | текущий commit | Upstream предлагал ad-hoc глобальный `pip install`, а первоначальное решение учитывало только технический manifest и не делало зависимость видимой пользователю. S.9.5 ввёл pinned release dependency, project-local install с разрешением, диагностику и обязательную отдельную строку `Зависимость: Pillow` в пользовательском гайде и `TOOLS.md`. |
| 95 | План не определяет жизненный цикл handoff-файлов между AI-клиентами | 2026-07-25 | 2026-07-25 | текущий commit | Upstream skill не имел import/update-контракта, а его session artifacts можно было ошибочно смешать с `memory.md`, Git-документацией или автоматически игнорировать, лишив межмашинного переноса. S.9.4 зафиксировал побайтный skill, user-owned output, ссылочную структуру, запрет secrets/automatic memory writes и явный выбор local versus portable target. |
| 94 | План сохраняет команду `caveman`, но не фиксирует поставку её skill | 2026-07-25 | 2026-07-25 | текущий commit | S.8 описывал изменение `CAVEMAN`, но не назначал import/update-контракт каноническому поведению режима. S.9.3 зафиксировал побайтный перенос skill, внешний client bridging, неизменные defaults/precedence/safety boundaries и проверки toggle без renderer/restart. |
| 93 | План не разделяет behavioral skill `mcp-1c-tools` и runtime MCP-каталог | 2026-07-25 | 2026-07-25 | текущий commit | Девять upstream-файлов не имели отдельного import/update-контракта, а формулировка «single source of truth» пересекалась с принятым `config/1c-mcp-catalog.json`; fallback `remember` также ослаблял S.5.2. S.9.2 разделил connection topology и task→tool policy, сохранил восемь файлов побайтно и ограничил адаптацию одной memory fallback-формулировкой. |
| 91 | План не сопоставляет 13 upstream commands с принятыми границами capability | 2026-07-25 | 2026-07-25 | текущий commit | Commands не имели явных владельцев и пересекались с provider lifecycle, refresh и write gates. S.8 назначил semantic owner всем 13 командам: канонические skills, provider delegation, maintainer-only refresh и upstream persistent controls в `.dev.env` с обязательными mutation/production guards. |
| 90 | План не сопоставляет 13 upstream agents с безопасной моделью Codex/Claude | 2026-07-25 | 2026-07-25 | текущий commit | Agents ошибочно рассматривались как одинаковые client-файлы с общим `allowParallel`. S.7 ввёл нейтральный канон и две generated projections, role-based permissions с реальной записью mutating ролям, model inheritance, serialized writes/worktree isolation, explicit triggers и parent-owned approval/integration/verification gates. |
| 89 | План не назначает владельца основному 1С-ruleset upstream | 2026-07-25 | 2026-07-25 | текущий commit | Ruleset рассматривался как клиентские копии, а не как единый семантический пакет. S.6 ввёл тонкий scoped AGENTS, канонический `develop-1c` skill с references, пофайловый semantic map, project-managed update contract и проверки полного покрытия/context budget/Codex-Claude parity. |
| 75 | Разбор upstream `AGENTS.md` потерял три обязательных companion-слоя | 2026-07-25 | 2026-07-25 | текущий commit | Файловый inventory сначала анализировался без связанного precedence-контракта. S.5.1–S.5.3 назначили `USER-RULES.md`, `memory.md` и `LLM-RULES.md` корневыми project-seed, развели правила/факты/self-improvement, определили writers, обновление, приоритет и одинаковую загрузку Codex/Claude. |
| 88 | Последовательный разбор плана повторно открывает уже принятые решения | 2026-07-25 | 2026-07-25 | текущий commit | Новый подпункт рассматривался изолированно без восстановления уже принятых ограничений. В «Режим проработки» добавлен обязательный шаг наследования журнала и канонических разделов; S.5.1 разобран уже внутри решений S.2, S.3, project memory и клиентских адаптеров. |
| 86 | План не сопоставляет три дополнительных MCP из upstream-каталога | 2026-07-25 | 2026-07-25 | текущий commit | Начальные семь ролей ошибочно считались полным inventory. S.4.4 сопоставил десять ролей: сохранил семь initial, назначил Code Metadata и Graph optional, а Data — optional-disabled до отдельного security review. |
| 85 | Нормализация shared MCP в `onec-*` может повторно зарегистрировать upstream endpoints | 2026-07-25 | 2026-07-25 | текущий commit | Ограничение OpenCode на letter-leading function names было ошибочно распространено на все клиенты. S.4.4 сохраняет canonical provider ids в Codex/Claude, вводит identity reconciliation и запрещает второй alias одного endpoint; `onec-*` остаётся для generated namespaces и нуждающихся в нормализации клиентов. |
| 84 | Scope `shared` не определяет владение портами между несколькими проектами | 2026-07-25 | 2026-07-25 | текущий commit | Provider runtime и project adapters рассматривались как один слой. S.4.4 ввёл `provider-shared`: capability переиспользует существующие endpoints, не разворачивает второй комплект и при dynamic external mode читает фактические ids/URLs из provider manifest/registry без записи машинных значений в Git. Изоляция stateful content выделена отдельно в №87. |
| 83 | Анализ S.4.4 ошибочно отождествил встроенный Toolkit с Docker-прокси | 2026-07-25 | 2026-07-25 | текущий commit | Docker-снимок был интерпретирован без повторной проверки канонического раздела плана. Контейнер `1c-mcp-toolkit-proxy` на `6003:6003` не входит в принятую архитектуру: план явно использует встроенный HTTP-сервер обработки Toolkit внутри runtime 1С. Диапазон `6003`–`6012` относится к этим встроенным per-base endpoints; shared контейнерные MCP используют отдельные `800x`. |
| 80 | S.4.2 допускает машинный local-слой во входах shared MCP-проекций | 2026-07-25 | 2026-07-25 | текущий commit | Входы runtime resolution и shared renderer были объединены без явной границы. S.4.3 запретил машинные литералы local-слоя в Git-проекциях: он используется только для runtime resolution/проверок; output допускает committed значения, имена переменных и поддерживаемые плейсхолдеры. |
| 79 | Нейтральный MCP-каталог не имеет scope для EDT workspace | 2026-07-25 | 2026-07-25 | текущий commit | Первичная модель ошибочно считала любой server либо общим, либо привязанным к базе. S.4.3 добавил `per-workspace`, отдельный EDT namespace на уникальный workspace и ссылки нескольких баз на один workspace endpoint без дублирования. |
| 77 | `select-1c-project` не может переподключить статический MCP Codex к выбранной базе | 2026-07-25 | 2026-07-25 | текущий commit | `select` ошибочно трактовался как динамическая перенастройка MCP. S.4.3 заранее объявляет отдельные namespaces и превращает `select` в проверку точного endpoint с session lock; новая сессия нужна только после изменения топологии или невозможности переподключения. |
| 78 | S.4 определяет конфигурационный адаптер Codex, но не Claude Code | 2026-07-25 | 2026-07-25 | текущий commit | Первичный разбор ошибочно принял Codex config за весь client contract. S.4.2 ввёл единый нейтральный MCP-каталог, обязательные проекции для Codex и Claude Code, key-aware владение `.mcp.json`/`.claude/settings.json` и запрет изменения локальных пользовательских слоёв. |
| 76 | План capability `1c` не определяет безопасное владение `.codex/config.toml` | 2026-07-25 | 2026-07-25 | текущий commit | Upstream-адаптер рассматривался как готовый renderer общего TOML-файла. S.4.2 ограничил владение маркированным MCP-блоком, исключил недокументированные ключи, ввёл artifact-ledger conflict detection и транзакционное обновление с сохранением остальных настроек. |
| 74 | Непроработанный план capability `1c` был преждевременно отмечен как `accepted` | 2026-07-22 | 2026-07-22 | текущий commit | Не было явного гейта между обсуждением отдельных гипотез и утверждением всего документа; `status: accepted` и галочка первого пункта были ошибочно приняты за разрешение начать реализацию. План возвращён в `draft`; добавлены порядок разбора всех разделов и запрет менять артефакты capability до явного итогового утверждения. |
| 73 | Глобальные и проектные AGENTS не имели явного контракта владения правилами | 2026-07-22 | 2026-07-22 | текущий commit | Глобальный шаблон, project baseline и локальный `AGENTS.md` стандарта развивались как отдельные тексты без документированной границы ответственности. В project baseline дважды повторялись ограничения размера и перезапуска, а repository rule допускал ошибочную трактовку подпапки как отдельного проекта. Schema 4 формализует три уровня, оставляет только объявленный portable safety subset и доставляет изменение последовательными миграциями. |
| 72 | Contract fixture не копирует обязательный capability manifest | 2026-07-19 | 2026-07-19 | текущий commit | При добавлении `config/capabilities.tsv` PowerShell fixture обновили, а POSIX fixture секции `Manifest drives bootstrap output` — нет. Изолированный bootstrap завершался до проверки изменённого `profiles.tsv`, потому что обязательный capability manifest отсутствовал. Fixture теперь копирует его вместе с остальными конфигурационными manifest-файлами; полный POSIX contract suite проходит 68/68. |
| 68 | `standardize-existing-project` не доводил legacy-проект до green | 2026-07-16 | 2026-07-16 | текущий commit | Обнаружен на `Other` (дефект `favorit-web` №13). Круговой пробел: `plan-migration --target project-agents` блокировал `unmanaged_conflict` советом «run standardize-existing-project first», а adopt-in-place относил `AGENTS.md` к manual review и baseline не вносил. Блок был обоснован: под то же состояние попадают проект с чисто локальными правилами (дописать снизу) и проект с немаркированной копией baseline (дописывание продублирует правила) — различить их может только человек. Planner принимает этот review как `--accept-unmanaged-as-local` и без него по-прежнему блокирует; поведение выровнено с target `global`, который уже принимал `unmanaged_conflict`, включая backup. Отдельно `validate-project` требовал `docs/quality/STANDARD_ADOPTION.json` для профиля `minimal`, которого adopt-in-place не создавал: файл добавлен в safe creates, а журнал начинается с `adopted_at` из `.project-standard.json` (fallback — сегодня), потому что дату создания legacy-проекта восстановить нельзя. Проверено сквозным прогоном на фикстуре, повторяющей исходное состояние `Other`: 9 errors → 0 errors без ручных шагов, локальные заметки сохранены построчно. |
| 67 | `plan-migration.ps1` отвергает target `project-agents` | 2026-07-16 | 2026-07-16 | текущий commit | Обнаружен в `favorit-web` (его дефект №12). `config/migrations.tsv` объявляет `0003-adopt-project-agents-managed-block` и `0006-upgrade-project-agents-managed-block-v2` для target `project-agents`, `plan_migration.py` его поддерживает, а POSIX-обёртка target не валидирует и передаёт как есть — но `ValidateSet` в `plan-migration.ps1` перечислял только `project` и `global`. Обе baseline-миграции были недостижимы на Windows через documented path; обходом оставался прямой вызов Python. `ValidateSet` дополнен `project-agents`; проверено на `Other` — plan возвращает `up_to_date`. |
| 66 | `sync-global-agents --check` падает traceback'ом на состоянии `managed_upgrade` | 2026-07-16 | 2026-07-16 | текущий commit | Обнаружен в `favorit-web` (его дефект №11). Parser корректно определял `managed_upgrade` и печатал `status=managed_upgrade`, после чего `status_message()` падал с `KeyError: 'managed_upgrade'`: словарь `messages` перечислял семь состояний из восьми. Пользователь получал traceback вместо диагностики ровно в тот момент, когда инструмент должен объяснить, что нужна миграция. Существующий тест доходил до `inspect_state`, но `status_message` не вызывал, поэтому пробел не ловился. Добавлено сообщение для `managed_upgrade` и регрессионный тест, проверяющий непустое сообщение для каждого достижимого состояния. |
| 65 | Validator сканирует зависимости и выдаёт ложные errors | 2026-07-15 | 2026-07-15 | текущий commit | Обнаружен в `favorit-web` (его дефект №8). `iter_files()` обходила всё дерево через `rglob` и исключала только `.git`, `__pycache__` и `.trash`, не учитывая `.gitignore`: 12 ложных `wikilink.missing` и 4 `path.machine_specific` из README пакетов в `node_modules`, exit code `1`, и 10378 прочитанных файлов вместо 47 проектных. Обход стал git-aware (`git ls-files --cached --others --exclude-standard`) с fallback на `rglob` при отсутствии git; `IGNORED_PARTS` расширен зависимостями и применяется к пути относительно корня, а не абсолютному. |
| 64 | Scheduled governance audit требовал недоступные `GITHUB_TOKEN` данные | 2026-07-14 | 2026-07-14 | текущий commit | Default workflow token не читает collaborators другого repo и полностью скрывает bypass actors. Полный локальный audit остаётся strict; scheduled self-audit проверяет active guards/checks и sole owner-admin собственного repo, не заявляя проверку скрытого bypass. |
| 63 | Migration planner test сравнивал сохранённый prefix с LF-only строкой | 2026-07-14 | 2026-07-14 | текущий commit | Windows Git fixture сохранила корректный локальный prefix с CRLF, а assertion требовал LF bytes. Проверка нормализует только line endings перед семантическим сравнением; production сохраняет исходный стиль. |
| 62 | Pinned BP hashes зависели от working-tree line endings | 2026-07-14 | 2026-07-14 | текущий commit | Contract verifier хешировал checkout bytes, которые `core.autocrlf` меняет без изменения Git blob. Проверка теперь хеширует `git show HEAD:<path>` и отдельно блокирует dirty pinned files. |
| 60 | Create workflow блокируется внутренней validation актуальной Best Practices | 2026-07-14 | 2026-07-14 | BP `4640e7e`, `a492d82`; текущий commit NPR | BP validator сериализовал относительный путь через platform-native separators; NPR E2E не запускал полную validation. BP использует POSIX paths и трёхплатформенный gate со стабильным check `validate`; NPR запускает full validation/real report, live watch и fail-safe create workflow без ручных outcomes. |
| 59 | Bypass защиты `main` выдан роли Admin, а не конкретному аккаунту | 2026-07-08 | 2026-07-14 | текущий commit; API audit в [[ACTIONS]] | GitHub ruleset предоставляет bypass роли, а не отдельному пользователю. Admin-bypass принят как trust boundary для прямого push владельца; scheduled audit требует, чтобы владелец оставался единственным Admin обоих репозиториев. |
| 58 | PowerShell contract test содержал Cyrillic без совместимой Windows encoding | 2026-07-08 | 2026-07-08 | (текущая ветка) | В `test-skills.ps1` добавили русские literals; Windows PowerShell 5.1 разобрал UTF-8 без BOM как legacy encoding и завершился parser error. Cross-platform assertion переведён на ASCII contract marker, POSIX test сохраняет проверку русской формулировки. |
| 57 | `reflect-and-record` сохранял отменённый запрет mid-session edit | 2026-07-08 | 2026-07-08 | (текущая ветка) | После исправления дефекта №55 один связанный skill не был включён в синхронизацию. Формулировка согласована с каноном: редактировать можно, но изменение действует в новом процессе; shell/PowerShell contract tests запрещают возврат старого текста. |
| 56 | Команда проверки instruction sources запускала интерактивный Codex TUI | 2026-07-07 | 2026-07-07 | `e00de26` | В аудит и производные инструкции попала форма `codex ... "prompt"` без subcommand; в non-TTY она завершалась ошибкой `TERM is set to "dumb"`. Во всех исполняемых guides/templates/skills добавлен `codex exec`, после чего проверка выполняется отдельным non-interactive процессом. |
| 55 | Global policy и шаблоны закрепляли неподтверждённые ограничения AGENTS.md | 2026-07-07 | 2026-07-07 | `7b6fad0`, `e00de26` | Глобальный, проектный и template-слои копировали community-рекомендации как технические факты: лимиты 150/300 строк, recite-back, запрет scoped override и неверную причину mid-session. Аудит выявил расхождение с документацией; global policy сокращена, проектные процессы оставлены в template/skills, validator и policy contract очищены от вымышленных порогов. |
| 54 | Consumer writer принимал секции, которые BP loader отклоняет | 2026-07-07 | 2026-07-07 | (текущая ветка A4) | A3 проверяла только синтаксис section slug и использовала пример `python`, не сверяя enum с `validate.ALLOWED_STACKS` Best Practices. Writer ограничен семью canonical sections; cross-repo E2E исполняет NPR writer против реального BP loader на трёх ОС. |
| 53 | Create workflow писал несовместимый consumer manifest | 2026-07-07 | 2026-07-07 | (текущая ветка A3) | Skill, template и guides закрепляли schema 1 top-level `optout` и section `applied`, которых canonical BP schema 2 не поддерживает. Добавлен единый schema 2 writer с atomic write и strict existing-manifest validation; workflow использует `preferences`, а outcomes делегирует BP tooling. |
| 51 | Assessment классифицирует rules repository как consumer project | 2026-07-07 | 2026-07-07 | (в этой ветке) | Planner применял consumer profile inference к любому дереву. Добавлен строгий `target_kind=rules-repository` по совокупности пяти markers, `not_applicable` report и запрет всех consumer plan/apply; regression покрывает read-only JSON, прямые builders, CLI plan/apply и отрицательный consumer fixture. |
| 52 | Стратегический план сообщает устаревшие schema и GitHub governance | 2026-07-07 | 2026-07-07 | (в этой ветке) | Статус реализации дублировался вручную и не был обновлён после schema `2` и включения ruleset. План актуализирован, следующий этап отделён от завершённых A–F; навигация больше не называет его proposed-планом. |
| 50 | `STANDARD_VERSION` не отражает новый managed knowledge contract | 2026-07-07 | 2026-07-07 | PR №6, merge `3dc5e0a`; [[docs/reviews/archive/PHASE_5_SEQUENTIAL_MIGRATIONS_REVIEW_2026-07-07|review]] | Version оставалась `1`, а planner поддерживал только одиночный `0 → current`. Введена schema `2` и детерминированный graph `0 → 1 → 2` для metadata/global/project-agents; exact history, bootstrap parity, atomic apply, backups и schema-1 upgrades покрыты regression и трёхплатформенным CI. |
| 49 | Нет исполняемого cross-repository контракта NPR ↔ BP | 2026-07-07 | 2026-07-07 | PR №4, merge `ee4677a`; [[docs/reviews/archive/PHASE_4_CROSS_REPO_CONTRACT_REVIEW_2026-07-07|review]] | Репозитории тестировали себя независимо. Добавлен pinned offline contract и live checkout gate: repository/commit/hashes, accepted promotion source, retired routes и ADR consequences; suite подключён к Ubuntu, Windows и macOS CI. Broad governance verification выполнена по обновлённому PLAYBOOK-паттерну. |
| 48 | `new-project-rules/main` не защищён на GitHub | 2026-07-07 | 2026-07-07 | GitHub ruleset `18603924`; [[docs/reviews/archive/PHASE_2_MAIN_PROTECTION_REVIEW_2026-07-07|review]] | Governance оставался только документацией после прежнего `403`. Добавлены CODEOWNERS и regression test, затем включён active ruleset: PR + 1 approval + Code Owner review + resolved threads, strict `shell`/`powershell`, запрет deletion/non-fast-forward. API postcondition: `main.protected=true`. |
| 47 | Standardization tree digest читает transient `.git` locks | 2026-07-06 | 2026-07-06 | (в этой ветке) | Test helper включал внутренности `.git` в проверку отсутствия mutation. На macOS runner с Python 3.14 Git maintenance успевал удалить `objects/maintenance.lock` между `is_file()` и `read_bytes()`, вызывая flaky `FileNotFoundError`. Digest теперь проверяет только project artifacts и исключает `.git`, состояние которого тестируется отдельными Git assertions. |
| 46 | PowerShell metadata test зависит от whitespace `ConvertTo-Json` | 2026-07-06 | 2026-07-06 | (в этой ветке) | Новый regression test сравнивал JSON как строку с форматированием PowerShell 7, тогда как Windows PowerShell 5.1 добавляет другое число пробелов после `:`. Проверка переведена на `ConvertFrom-Json` и семантическую валидацию полей, дат и commit SHA. |
| 45 | Bootstrap не создаёт `.project-standard.json` | 2026-07-06 | 2026-07-06 | (в этой ветке) | Manifest и оба bootstrap-адаптера создавали документы и git repository, но пропускали обязательную provenance metadata. Новый проект сразу считался legacy, хотя README называл metadata источником истины. Metadata добавлена как manifest-driven generated artifact; shell/PowerShell фиксируют schema, profile, source, source commit и даты, а без Git bootstrap теперь явно отказывает. |
| 43 | Promotion backlog создаёт merge-конфликты как единая таблица | 2026-07-06 | 2026-07-06 | `b459bf3` | Все кандидаты изменяли один Markdown-файл. Backlog переведён на one-file-per-candidate; стабильный index больше не меняется при добавлении записи, schema удерживается validator и CI. |
| 44 | Последовательные promotion ID требуют централизованной координации | 2026-07-06 | 2026-07-06 | `b459bf3` | `PC-YYYY-NNN` требовал общего счётчика между ветками. Генератор использует 48-битный hex suffix с retry при локальной коллизии; validator запрещает дубли, legacy ID остаются валидными. |
| 28 | Активный `~/.codex/AGENTS.md` разошёлся с переносимой копией, managed markers утрачены | 2026-07-02 | 2026-07-02 | см. [[ACTIONS]] | После adoption 2026-06-30 активный файл был перезаписан старой редакцией без markers (вероятно, старый `setup-global-agents` на другом компьютере; root cause подтвердить). Исправлено: user-reviewed копия portable → active, затем migration `0002` (fingerprint `18a73ff1…f063d`), postcondition `managed_match`. |
| 29 | `docs/README.md` не содержит два research-файла | 2026-07-02 | 2026-07-02 | `7fb88eb` | Секция «Исследования» не пополнялась при добавлении research-файлов; ссылки на AGENT_RUNTIME_CAPABILITIES_2026 и AGENT_COMMUNITY_PRACTICES_2026 добавлены. |
| 30 | `USE_THIS_PROJECT.md` неполон | 2026-07-02 | 2026-07-02 | `21f3cdf` | Гайд писался до финализации набора skills: секция knowledge promotion не получила Claude Code эквиваленты, а `reflect-and-record` выпал из перечня workflows. Добавлены `/`-вызовы и секция 9 с фразами и таблицей выбора. |
| 31 | CHANGELOG `Unreleased` разросся и отстаёт | 2026-07-02 | 2026-07-02 | `21f3cdf` | Пять циклов работы после `v1.9.0` не нарезались в релиз, и записи о новых артефактах отставали. Нарезан `v1.10.0 — 2026-07-02`, добавлены записи о гайде и дефектах 28–29. |
| 32 | `TOOLS.md` не каталогизирует большинство скриптов | 2026-07-02 | 2026-07-02 | `21f3cdf` | Правило про TOOLS.md выполнялось только для Python/PowerShell runtime. Добавлен раздел `Script entry points`: ссылка на [[INDEX]]/[[docs/quality/TESTING|TESTING]] как авторитетные каталоги, семейства скриптов и явное исключение из правила парности `.sh`/`.ps1`. |
| 34 | Standardization regression suite не запускается в CI | 2026-07-03 | 2026-07-03 | `fcf16e7` | Suite добавлялся в локальную матрицу TESTING, но шаг в workflows не создавался. Подключён к ubuntu/windows jobs `ci` и `macos-smoke`; непортабельные тесты получили skip-guards (POSIX wrapper на Windows, symlink fixtures без прав на symlink). |
| 38 | PowerShell scoped-agent test падает локально на macOS из-за `/var` и `/private/var` | 2026-07-03 | 2026-07-03 | `83d5e11` | Containment-проверка сравнивала строки без резолва symlink-предков (`/var` → `/private/var`). Добавлен `Get-CanonicalExistingPath` (chdir + getcwd даёт физический путь на Unix); root, probe и directory сравниваются в канонической форме. `test-agent-setup.ps1` проходит на macOS pwsh (31/31). |
| 39 | Environment check требует HTTPS credential helper при SSH Git transport | 2026-07-03 | 2026-07-03 | `83d5e11` | Проверка не различала transport. Теперь при `gh git_protocol=ssh` и пустом `credential.helper` выводится `[ ok ]` (SSH аутентифицируется ключами); для HTTPS/неизвестного transport поведение прежнее. Обе ветки проверены в sh и pwsh с изолированным git-конфигом. |
| 35 | Standardization apply пишет через symlink за пределы проекта | 2026-07-03 | 2026-07-03 | `3ace8ca` | Планируемые writes никогда не проверялись на symlink/containment. Добавлен `unsafe_write_reason()`: план блокируется, а `apply_plan()` отказывает, если путь выходит за root или проходит через symlink; regression test с `INDEX.md` → внешний файл. |
| 36 | Re-bootstrap разыменовывает symlink и копирует внешний файл | 2026-07-03 | 2026-07-03 | `3ace8ca` | `copy_transfer_item()` использовал `shutil.copy2()` без проверки symlink. Теперь план блокируется при symlink в transfer set (`build_transfer_manifest()`), а apply дополнительно отказывает копировать symlink и проверяет containment target. |
| 37 | Re-bootstrap fingerprint не защищает содержимое transfer set | 2026-07-03 | 2026-07-03 | `3ace8ca` | Fingerprint включал только имена top-level paths. В payload добавлен `transfer_manifest` — полный список файлов с sha256 содержимого; изменение файла после review даёт fingerprint mismatch, что покрыто regression test. |
| 40 | Re-bootstrap разрешает destination внутри legacy repo и `.git` | 2026-07-03 | 2026-07-03 | `3ace8ca` | Guard сравнивал только `destination == root`. Добавлены проверки `is_relative_to` в обе стороны: destination внутри source root (включая `.git`) и source внутри destination блокируются; regression test покрывает оба вложенных пути. |
| 41 | План компрессии закоммичен с template-плейсхолдером даты, `validate-project` падает | 2026-07-06 | 2026-07-06 | (в этой ветке) | При описании плейсхолдера прозой в план попал сырой литерал `<`+`YYYY-MM-DD`+`>`; правка порогов `4c83ab9` закоммичена без прогона валидатора, и `placeholder.remaining` дошёл до `main`. Формулировки переписаны без сырого литерала; урок — прогонять `validate-project --report-only` перед commit docs и не писать template-плейсхолдеры прозой. |
| 42 | Сплит CHANGELOG в компрессии оставлял архив без обратной ссылки | 2026-07-06 | 2026-07-06 | (в этой ветке) | `split_changelog` создавал `CHANGELOG_ARCHIVE.md` со ссылкой `[[CHANGELOG]]`, но не оставлял обратную ссылку в `CHANGELOG.md` — архив был связан в одну сторону (риск orphan/дрейфа навигации), в отличие от паттерна DEFECTS. Найдено dogfood-применением на самом репозитории. Добавлен `CHANGELOG_ARCHIVE_POINTER` (идемпотентная вставка в преамбулу) и регрессионный тест `test_pointer_is_idempotent`; архив внесён в [[INDEX]]. |

## Won't Fix

| # | Title | Discovered | Reason |
|---|---|---|---|
| 92 | План не фиксирует формат 1С-исходников для metadata tooling | 2026-07-25 | Не дефект при принятом контракте минимальной адаптации. `1c-metadata-manage` переносится без изменения и работает только с ожидаемым им XML-деревом; наличие `Configuration.xml` и нужной структуры проверяют сами upstream tools. EDT workflow остаётся отдельным владельцем и не требует общего `source_format`. Решение 2026-07-25: поле и compatibility router не вводятся. |
| 87 | Provider-shared MCP не имеют согласованной модели state/mount isolation | 2026-07-25 | Не дефект consumer capability: S.4.4 уже назначил внешний MCP-проект владельцем runtime, портов, mounts, индексов и container lifecycle. Повторное проектирование provider internals расширяло scope и дублировало внешнюю систему; наш контракт проверяет только обнаруженный endpoint, identity, health и tools. Решение 2026-07-25: отдельный S.4.5 не реализуется. |
| 4 | POSIX symlink-тесты падают в Git Bash без symlink support | 2026-06-28 | Git Bash `ln -s` на этой Windows копирует файл, поэтому resolver-тесты получают неверный каталог. Windows поддерживается PowerShell-набором; POSIX symlink-сценарии выполняются в macOS/Linux и CI. |
| 33 | PowerShell bootstrap-тест падает локально на macOS в mock-git секции | 2026-07-02 | Дефект тестовой обвязки, а не продукта: pwsh на macOS кеширует резолв `git` в удалённый `mock-bin` путь из временного PATH (command caching). CI (Windows PowerShell 5.1 / pwsh 7 на windows-latest) сценарий не воспроизводит и полностью покрывает; профильные проверки скрипта проходят. Решение 2026-07-03: не чинить. |
