---
type: defect-log
status: active
owner: project
last_verified: 2026-07-26
related:
  - "[[docs/README]]"
  - "[[docs/quality/TESTING]]"
---

# Дефекты

Обнаруженные дефекты не удаляются: после исправления запись переносится в
соответствующий раздел с датой и commit.

## Open

Найдены архитектурным ревью 2026-08-02, см. [[docs/reviews/REVIEW_2026-08-02_ARCHITECTURE]].

| # | Title | Discovered | Component | Description |
|---|---|---|---|---|
| 242 | `apply-capability-artifacts --apply` оставляет проект невалидным | 2026-08-02 | `scripts/apply-capability-artifacts.py` | Обработчик владеет файлами, а вторую половину установки capability — `.project-standard.json`, `INDEX.md`, `docs/README.md`, `.best-practices.json` — делает только bootstrap. Воспроизведено: добавление `jira-confluence` в существующий проект документированной командой записывает 19 файлов и ledger, после чего валидатор даёт `capability.removed` с сообщением «capability нельзя удалить, пересоздайте проект» — то есть описывает действие, обратное выполненному. Для `1c` добавится `capability.stack_missing` и требования wikilinks из `check_capabilities`. Сценарий объявлен поддержанным в `capability_artifacts.py:174`. Восстановление — только правкой metadata руками. |
| 243 | Session lock не имеет исполнителя | 2026-08-02 | `scripts/one_c_session.py` | Модуль, решающий, можно ли писать в живую базу, и запрещающий запись в production, не импортируется ничем, кроме собственного теста: нет `__main__`, нет обёртки, ни один `SKILL.md` его не называет; единственное упоминание `.1c-state` в поставке — строка в `appendix/gitignore`. При этом критерий готовности 8 «Live-base skill отказывается без session lock» стоит в `READINESS_1C.md` со статусом `тест`: тест грузит модуль напрямую и ни одного скилла не запускает, поэтому проверяет сторону связи, которой не с чем соединяться. |
| 244 | `capability_releases` не заполняется никогда | 2026-08-02 | `scripts/capability_artifacts.py`, metadata | Поле валидируется, вставляется пустым миграцией 0013 и пишется пустым обоими bootstrap; писателя нет. Проверено на проекте, созданном `--preset 1c` из release `0.6.0`: `"capability_releases": {}`. На поле держатся три утверждения — `ARCHITECTURE.md:56` про сравнение `release_id`, критерий приёмки Э6 и дефект №148. Фактически принадлежность ловит ledger, а сравнения `release_id` не происходит нигде. Долг признан в `one-c/IMPLEMENTATION_PLAN.md:181`, но три документа описывают механизм как действующий. |
| 245 | Матрица готовности проверяется как таблица, а не как критерий | 2026-08-02 | `scripts/test-1c-readiness.py` | Сверяются формулировки с мастер-планом и существование названного теста. Совпадение критерия с кодом не проверяется, поэтому критерий про скилл остаётся зелёным при доказательстве, которое скилла не касается (№243). |
| 246 | Проверка фантомных флагов реализована блоклистом | 2026-08-02 | `scripts/test-check-skills.py` | `PHANTOM_FLAGS` перечисляет два найденных случая, а комментарий объявляет инвариант «скилл не называет флаг, которого нет в скрипте». Закрыты экземпляры, не класс. Там же `CHECKOUT_CAVEAT` считает достаточным появление литерала `new-project-rules` в любом месте файла. |
| 247 | `CAPABILITY_NAMES` не сверяется с манифестом | 2026-08-02 | `scripts/project_metadata.py` | Множество имён — хардкод, в отличие от соседнего `CAPABILITY_CORE`, у которого сверка с `config/capability-core.tsv` есть и проверяется тестом. Bootstrap принимает capability по `config/capabilities.tsv`, валидатор — по этому множеству, поэтому расхождение даёт созданный и тут же невалидный проект. |
| 248 | Рендерер клиентов проверяет URL и не проверяет имя сервера | 2026-08-02 | `scripts/one_c_clients.py` | `project_id`/`environment_id` из seed-файла `config/1c-projects.tsv` попадают в заголовок TOML-таблицы без проверки. Воспроизведено: `project_id` со скобкой и точкой даёт `[mcp_servers.onec_toolkit_erp.a"_dev]` — невалидный TOML, после которого Codex не прочитает и пользовательскую часть `config.toml` за маркерами. Правило `ONE_C_ID_RE` существует в `validate-project.py:417`, но `render-1c-clients.py` запускается отдельно и валидатор не вызывает. Тот же класс, что закрытая инъекция через URL, только на втором поле. |
| 249 | Shell-bootstrap не проверяет destination строк capability | 2026-08-02 | `scripts/bootstrap-new-project.sh` | `.ps1` отвергает небезопасный destination и конфликт с артефактом профиля (`:159`); `.sh` для тех же строк проверяет только наличие шаблона и класс payload, а проверку `..` и `//` применяет лишь к `profiles.tsv`. Parity-тесты сравнивают полученные деревья и эту разницу увидеть не могут: при здоровом манифесте выход одинаковый. Ни одна из реализаций не ловит две capability с одним destination. |
| 250 | Тавтологический symlink-барьер в плане артефактов | 2026-08-02 | `scripts/capability_artifacts.py` | Условие на строке 162 сравнивает выражение с самим собой (`parent` и есть `(project_root / target).parent`, а `.absolute().resolve()` равно `.resolve()`) и никогда не истинно; проверено на symlink наружу. Работает один барьер из двух внешне независимых, с одинаковым текстом ошибки. Рядом `ledger_document` решает политику записи сравнением человекочитаемой строки `detail == "rendered by bootstrap"`. |
| 251 | Порог размера `CHANGELOG.md` противоречит политике удержания | 2026-08-02 | `scripts/compress-project.py` | Файл 59 КБ при пороге 32 КБ, и компрессия сообщает, что `--changelog-keep 5` не даёт уменьшить дальше: предупреждение не может быть снято без изменения политики, поэтому оно постоянное. Отдельно: 8 исправленных дефектов ждут архивации, а ревью 2026-08-01 не было внесено ни в `INDEX.md`, ни в `docs/README.md`. |

## Fixed

Записи, исправленные до релиза `v1.10.0` (№ 1–3, 5–27), перенесены при
консолидации в [[docs/quality/DEFECTS_ARCHIVE|архив дефектов]] без изменения
содержимого. Нумерация сквозная и не переиспользуется.

| # | Title | Discovered | Fixed | Commit | Root Cause |
|---|---|---|---|---|---|
| 240 | Подтверждение переключателя записи на `managed` не имело исполнителя | 2026-08-02 | 2026-08-02 | ветка `feat/managed-write-switch` | У обычного приложения режим обеспечен артефактом: read-only сборка технически не может записать, и сборки различаются по SHA-256. У управляемого барьера в артефакте нет — режим задаёт переключатель в UI Toolkit, — поэтому правило «подтверждается вызовом, а не предположением» было записано в скиллах и выполнялось человеком. Теперь это предусловие исполнителя: `acquire` не берёт lock на управляемую базу без прочитанного состояния переключателя, требует, чтобы в подтверждении было названо само состояние (`on` или `off`), и отвергает несовпадение с режимом — `analysis` требует выключенного, `approved-write` включённого. `require` отказывает по замку без этого доказательства, включая `analysis`: именно его утверждение о невозможности записи и держится на состоянии переключателя. Замок, потерявший доказательство или противоречащий своему режиму, не действует. |
| 241 | Обработки Toolkit не доставлялись в проект | 2026-08-02 | 2026-08-02 | ветка `feat/managed-launch-profile` | Профили ссылались на абсолютный путь к рабочему столу одной машины: он не работал ни у кого другого, и потому никто не замечал, что самих обработок в поставке нет вовсе. Путь исправлен на внутрипроектный, а три собранные обработки — read-only, read-write и privileged — доставляются классом `binary` в `configurations/toolkit/` рядом с профилями, которые их запускают. SHA-256 записаны в паспорт release и в `TOOLCHAIN.md`; тест каркаса сверяет байты, потому что бинарник, доставленный с подстановкой, не откроется. Release `0.6.0`. |
| 215 | Скиллы ссылаются на `python3 scripts/…`, которых в проекте нет | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Скрипты стандарта живут в чекауте `new-project-rules`, а не в созданном проекте, и путь к нему проект не записывает. Оговорка была в пяти скиллах из восьми; добавлена в `deploy-and-test-1c`, `manage-1c-mcp` и `select-1c-project`. Класс закрыт инвариантом: скилл capability, называющий скрипт стандарта, обязан сказать, где тот лежит. Проверено откатом. |
| 235 | `AGENTS.md` предписывает push, который ruleset отклоняет | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Правило требовало push в `origin/main`, который ruleset `Protect main` не пропускает; единственный документированный обход — `--admin`, записанный дефектом №190. Правило переписано на фактический маршрут: ветка и pull request, причём открытие PR — часть того же шага, а слияние — нет и требует согласия пользователя. |
| 236 | Обновление capability миграцией реализовано наполовину | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Оказалось не дефектом кода, а разрывом в документации. Обработчик `capability_artifacts` объявлен внешним намеренно: обновление capability определяется сравнением `release_id`, а не шагом целочисленной схемы, и планировщик миграций **отвергает** строку с этим обработчиком в `config/migrations.tsv` с объяснением. Пустота манифеста — выполненное требование. Это записано в `ARCHITECTURE.md` вместе с разделом о двух осях версий. |
| 237 | `ARCHITECTURE.md` не описывает слой, несущий сложность | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Документ описывал шесть слоёв и не упоминал capability, preset, release и ledger. Добавлен седьмой слой и раздел о нём: какие манифесты чем владеют, чем класс поставки отличается от политики владения, почему capability с `docs_section` требует профиля не ниже `software`, и почему две оси версий нельзя смешивать. |
| 238 | `DEFECTS.md` не архивируется, критерии архивации расходятся | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | `reflect-and-record` называл порог «больше 30 записей», `compress-project` знал только возраст в 90 дней — и ни один не срабатывал: проект закрывает много дефектов за месяц, поэтому по возрасту не уходило почти ничего, а счётный порог жил только в тексте скилла, без исполнителя. Порог стал исполняемым (`--fixed-max-entries`, по умолчанию 30), скилл теперь ссылается на скрипт вместо второго критерия. Применено: 177 записей перенесено, `DEFECTS.md` уменьшился со 156 КБ до 26 КБ. |
| 239 | Стандарт не применил к себе Уровень 1 компрессии | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | `compress-project --root .` применён: убран `.DS_Store`, четыре релиза перенесены в `CHANGELOG_ARCHIVE.md`, 177 дефектов — в архив. Попутно исправлено сообщение, которое при размере выше порога печатало «split не нужен»: теперь оно предупреждает, что дальше резать мешает нижняя граница удерживаемых релизов, и называет, каким флагом её опустить. |
| 234 | Диапазон портов `6003–6012` продублирован | 2026-08-01 | 2026-08-02 | ветка `fix/single-source` | Диапазон был записан литералом и в валидаторе, и в рендерере клиентских проекций: расширение в одном месте дало бы базу, которую валидатор принимает, а рендерер молча отбрасывает. Определение перенесено в `validate_project_support`, оба читают один объект. Тест требует именно тождества объектов и запрещает литерал в валидаторе. |
| 233 | Новые capability и skill не дошли до пользовательских руководств | 2026-08-01 | 2026-08-02 | ветка `fix/single-source` | Тот же корень, что у №232. `transcribe` добавлен в `README.md` и `USE_THIS_PROJECT.md`, где появился раздел о возможностях поверх профиля с минимальным профилем каждой. Расхождение теперь ловится тестом, а не вниманием. |
| 232 | Список capability объявлен в восьми местах без сверки | 2026-08-01 | 2026-08-02 | ветка `fix/single-source` | Сверки не было, и списки разошлись: `transcribe` отсутствовал в `README.md`, в `PROJECT_STANDARD_SCHEMA.md` и в скилле `create-new-project` — то есть capability была невидима ровно там, где её выбирают. Документы дополнены, а `test-preset-core.py` теперь требует, чтобы каждое имя из `CAPABILITY_NAMES` встречалось на всех пользовательских поверхностях выбора. Проверено откатом. |
| 231 | `tracked_files` декодирует вывод git локальной кодировкой | 2026-08-01 | 2026-08-02 | ветка `fix/single-source` | При `core.quotePath=false` git отдаёт сырые байты, а `text=True` разбирал их кодировкой локали: на не-UTF-8 машине имя возвращалось искажённым, и такой файл выглядел одновременно отсутствующим в чекауте и лишним в нём. Обе точки — `import_1c_upstream.tracked_files` и `build-capability-release.git_output` — читают байты и декодируют UTF-8 явно. Тест кладёт в staging файл с кириллическим именем и падает при возврате к кодировке локали. |
| 230 | У джоб CI нет таймаута | 2026-08-01 | 2026-08-02 | ветка `fix/ci-parity` | Таймаут стоял только у `macos-smoke`, остальные наследовали дефолт в шесть часов: зависший прогон держал бы обязательную проверку без объяснения. Бюджет проставлен всем джобам — 20 минут для shell и cross-repo-e2e, 30 для PowerShell, 10 для ночных аудитов. |
| 229 | `test-standard-metrics.py` не документирован | 2026-08-01 | 2026-08-02 | ветка `fix/ci-parity` | Тест существовал и запускался в CI, но отсутствовал и в `TESTING.md`, и в `INDEX.md`. Добавлен, а вместе с ним инвариант в `test-check-skills.py`: каждый `scripts/test-*.py` обязан быть в обоих документах. Инвариант сразу нашёл второй такой тест — `test-best-practices-contract.py`. |
| 228 | `macos-smoke` не запускает пять существующих тестов | 2026-08-01 | 2026-08-02 | ветка `fix/ci-parity` | `test-cli-discovery.py`, `test-1c-session.py`, `test-1c-doctor.py`, `test-1c-upstream-routing.py` и `test-github-governance.py` существовали и в smoke не входили. Добавлены; в наборе стало 30 тестов. |
| 227 | `test-skills.ps1` слабее своей `.sh`-пары | 2026-08-01 | 2026-08-02 | ветка `fix/ci-parity` | Из четырёх литералов `reflect-and-record` PowerShell-версия проверяла один, не проверяла отсутствие снятого запрета и не проверяла плейсхолдер схемы в managed-маркере шаблона. Тест парности, который сам не соблюдал парность: вердикт на Windows мог быть зелёным там, где shell отвергал файл. Доведено до одинакового набора проверок; проверено правкой литерала в скилле. |
| 226 | Парсер PowerShell берёт 21 файл из 81 | 2026-08-01 | 2026-08-02 | ветка `fix/ci-parity` | Проверялись только `scripts/*.ps1`, а шестьдесят поставляемых `.ps1` — те, что реально запускает созданный проект, — не разбирались вовсе. Обход расширен на `templates/` рекурсивно и с `-Force`: без него `Get-ChildItem` пропускает каталоги с точкой, а весь payload лежит под `.agents`, поэтому первая попытка нашла ноль файлов и отчиталась успехом. Теперь разбирается 82 файла; проверено внесением синтаксической ошибки в файл payload. |
| 225 | `governance-audit` рапортует о проверке bypass-акторов, не выполняя её | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | При `--github-token-scope` пустой список bypass считался успехом, и ночной аудит печатал «reviewed bypass» о значении, которого не видел. Скрытое токеном поле — не нарушение, но и не проверка: теперь оно сообщается строкой `NOTE ... не проверено`, а строка успеха перечисляет только то, что действительно проверено. Тест, требовавший молчания, переписан на новое требование и падает при возврате прежнего поведения. |
| 224 | Пять мёртвых файлов в payload и битая ссылка в доставляемом README | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Дубли `openspec/*` остались в корне payload после исправления маршрута: материализатор писал файлы, но никогда не удалял те, которых доставка больше не производит. Причина исправлена — устаревшие файлы удаляются и называются в отчёте. Битая ссылка на `AGENT-INSTALL.md`, который маршрутизирован как provider-only и не поставляется, снята адаптацией `openspec-readme-pointer`: сломанный указатель в первой строке доставляемого README хуже отсутствующего, потому что велит агенту идти туда прежде всего остального. Release `0.5.0`. |
| 223 | Добавить capability в уже созданный проект невозможно | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Проверка `rendered and current is None` стояла до ветки `seed`, поэтому seed-шаблон давал конфликт вместо создания, и capability можно было выбрать только при создании проекта. Значения плейсхолдеров не были секретом bootstrap: имя и схему проект сообщает о себе сам, дата — сегодняшняя. Они читаются из метаданных, набор плейсхолдеров остался единственным, а недостающее значение по-прежнему даёт отказ с названием того, чего не хватает, а не догадку в файле. |
| 222 | Компонент с `detect: manual` нельзя подтвердить | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | `apply` принимал ответ «использовать» и доводил до `ready`, но `options()` для ненайденного компонента его не предлагал: пользователю показывали «установить автоматически / самостоятельно / пропустить». Для `detect: manual` «не найден» — нормальное состояние, обнаружение такой компонент не видит по определению, поэтому единственное доступное свидетельство — слово человека. Ответ теперь предлагается; проверено откатом. |
| 221 | Профили запуска EDT — `managed` с неподставляемыми плейсхолдерами | 2026-08-01 | 2026-08-02 | ветка `fix/delivery-tails` | Профили спрашивают `EDT_PROJECT`, `RUNTIME_INSTALLATION` и `TOOLKIT_READ_ONLY_EPF` — значения, которые знает только пользователь и которые bootstrap не подставляет. При политике `managed` заполнение файла, ради которого он и поставлен, превращалось в дрейф и останавливало транзакцию всей capability. Переведены в `seed`, как и одиннадцать бланков `jira-confluence` по №207. |
| 220 | Флаг `--include-memory` объявлен и не реализован | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Флага не было ни в скрипте, ни в обёртках, и быть не могло: уровень 3 требует суждения о содержимом памяти, а `compress-project.py` выполняет только механическую часть — это сказано в его собственной docstring. Скилл, руководство и план исследования приведены к факту: уровень выполняет агент по явному запросу. Добавлена проверка, запрещающая скиллам называть флаги, которых нет ни в одном скрипте. |
| 219 | Режимы `manage-1c-mcp` не поддержаны исполнителем | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Скилл объявлял `install`, `update` и `repair` как свои режимы, а `one_c_provider.py` знает только `--root` и `--manifest`. Это режимы провайдера: заводить свои флаги значило бы создать вторую реализацию жизненного цикла, чего этот skill избегает по решению. Таблица переписана на задачи и команды провайдера; отсутствие нужной команды в manifest останавливает работу, а не подменяется догадкой. |
| 218 | `evolve-1c-rules note …` не имеет исполнителя | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Скилл называл команду, которой нет. Запись сигнала в `Pending signals` — это правка файла агентом по явной просьбе, что и написано вместо команды. |
| 217 | `1c-metadata-manage` назван skill'ом, но для Claude Code им не является | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Три скилла называют его жёстким гейтом, а `bridge` у всех десяти vendored-скиллов был `none`: гейт, который в половине клиентов нельзя соблюсти. Мосты созданы для всех десяти. По решению S.9.1 они лежат вне побайтного subtree, поэтому проверка моста теперь определяет каталог capability, а не отсчитывает два уровня вверх. |
| 216 | `ONE_C_WORKSPACE.md` обещает заполнение скиллом, который его не трогает | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | `add-1c-base` ведёт реестр `config/1c-projects.tsv`, а этот обзорный файл не открывает. Текст разведён: реестр за скиллом, обзор за человеком. |
| 214 | `deploy-and-test-1c` читает флаг вместо каталога компонентов | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | Скилл утверждал, что состояние YAxUnit берётся из каталога компонентов, тогда как `--not-installed` — ручной флаг. Каталог знает, что компонент объявлен, но не проверяет его на этой машине. Формулировка приведена к факту: состояние выясняет `doctor-1c`, а флаг — осознанное утверждение человека. |
| 213 | `transcribe` обещает три файла, пишет один | 2026-08-01 | 2026-08-02 | ветка `fix/skill-promises` | В `OUTPUTS` числились `summary` и `detailed`, которых скрипт не пишет: суммаризатора у него нет, и сам скилл говорит, что summary пишет AI-клиент по готовой расшифровке. Ожидание трёх файлов было ожиданием того, чего никто не создаст. `OUTPUTS` оставлен один, скилл в дереве результатов помечает, что пишет скрипт, а что клиент. Попутно закрыт второй разрыв: `--format json` писал JSON в файл с расширением `.md` — расширение теперь следует формату, а неизвестный формат отвергается до записи. |
| 212 | `one_c_doctor.settings()` маскирует по содержимому, а не по имени ключа | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | Решение принималось по всей строке и по списку из пяти маркеров, поэтому `DB_PWD`, `ONEC_CREDENTIAL` и `BASE_LOGIN` печатались целиком. Теперь имя ключа и значение разбираются раздельно: маскируется ключ, чьё имя называет учётные данные, и значение, похожее на строку соединения. Тест, закреплявший утечку, расширен этими тремя ключами и падает при возврате прежнего списка маркеров. |
| 211 | `one_c_doctor.occupied()` на Windows не видит слушателя на `0.0.0.0` | 2026-08-01 | 2026-08-02 | ветка `fix/checks-that-lie` | Проба привязывалась только к `127.0.0.1`, поэтому порт, занятый на всех интерфейсах, считался свободным — ровно случай, ради которого проверка написана: платформа 1С слушает `6003` на `0.0.0.0`. Теперь спрашиваются оба адреса, а на Windows вместо `SO_REUSEADDR` ставится `SO_EXCLUSIVEADDRUSE`: первый там позволяет привязаться поверх активного сокета и делает пробу бессмысленной. Тест на «занят на всех интерфейсах» на POSIX не различает реализации, поэтому добавлен структурный: проба обязана спросить про каждый интерфейс, и он падает на старом коде. |
| 210 | Нет теста на связку профиль ↔ `docs_section` capability | 2026-08-01 | 2026-08-02 | ветка `fix/review-blocking` | №205 был исправлен данными — обеим capability подняли минимальный профиль, — но класс ошибки оставался открытым: ничто не проверяло саму связку. Инвариант добавлен в `test-preset-core.py`, где полный bootstrap не нужен: capability с непустым `docs_section` обязана требовать профиль не ниже `software`, на котором появляется `docs/README.md`. Проверено откатом — при возврате `jira-confluence` на `minimal` тест падает именно этой строкой. |
| 206 | Provider manifest мог дописать произвольную секцию в `.codex/config.toml` | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | Проверялся `health`, а регистрировался `url` — разные поля манифеста, и `url` не валидировался вовсе. Дальше он подставлялся в TOML конкатенацией: кавычка и перевод строки закрывали строку и открывали таблицу `[mcp_servers.…]` с произвольной командой, то есть внешний JSON решал, какую программу Codex запустит как MCP-сервер. Закрыто в двух слоях: манифест принимает только простой http(s)-адрес без кавычек и переводов строк, а `health` обязан принадлежать тому же серверу, что и `url`, иначе проверка доказывает не то; писатель TOML отказывается от значения, которое не может закавычить. |
| 205 | Capability с разделом документации роняет bootstrap на профиле без `docs/README.md` | 2026-08-01 | 2026-08-01 | ветка `fix/review-blocking` | Артефакт с непустым `docs_section` индексируется в `docs/README.md`, а тот появляется с профиля `software`. Ядро поднимало профиль только для `1c`, поэтому `minimal` + `jira-confluence` или `transcribe` записывал все файлы, падал на индексации и откатом удалял каталог назначения — при том, что `create-new-project` предлагает `minimal` как допустимый ответ. Обе capability получили `min_profile: software`; `CAPABILITY_REQUIRED_STACK` пропускает `-`, иначе прочерк стал бы именем обязательного стека. Класс закрыт не полностью: теста на связку профиль ↔ `docs_section` нет, он в №210. |
| 92 | План не фиксирует формат 1С-исходников для metadata tooling | 2026-07-25 | Не дефект при принятом контракте минимальной адаптации. `1c-metadata-manage` переносится без изменения и работает только с ожидаемым им XML-деревом; наличие `Configuration.xml` и нужной структуры проверяют сами upstream tools. EDT workflow остаётся отдельным владельцем и не требует общего `source_format`. Решение 2026-07-25: поле и compatibility router не вводятся. |
| 87 | Provider-shared MCP не имеют согласованной модели state/mount isolation | 2026-07-25 | Не дефект consumer capability: S.4.4 уже назначил внешний MCP-проект владельцем runtime, портов, mounts, индексов и container lifecycle. Повторное проектирование provider internals расширяло scope и дублировало внешнюю систему; наш контракт проверяет только обнаруженный endpoint, identity, health и tools. Решение 2026-07-25: отдельный S.4.5 не реализуется. |
| 33 | PowerShell bootstrap-тест падает локально на macOS в mock-git секции | 2026-07-02 | Дефект тестовой обвязки, а не продукта: pwsh на macOS кеширует резолв `git` в удалённый `mock-bin` путь из временного PATH (command caching). CI (Windows PowerShell 5.1 / pwsh 7 на windows-latest) сценарий не воспроизводит и полностью покрывает; профильные проверки скрипта проходят. Решение 2026-07-03: не чинить. |
| 4 | POSIX symlink-тесты падают в Git Bash без symlink support | 2026-06-28 | Git Bash `ln -s` на этой Windows копирует файл, поэтому resolver-тесты получают неверный каталог. Windows поддерживается PowerShell-набором; POSIX symlink-сценарии выполняются в macOS/Linux и CI. |

## Won't Fix

| # | Title | Discovered | Reason |
|---|---|---|---|
