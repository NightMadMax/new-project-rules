# Строки, которые обязаны присутствовать в .agents/skills/reflect-and-record/SKILL.md.
# Один источник для обеих проверок: пара test-skills.sh / test-skills.ps1 разошлась
# именно потому, что список был записан в каждой из них отдельно. Файл читается как
# UTF-8 явно — кириллица в исходнике .ps1 не разбирается Windows PowerShell 5.1,
# которая без BOM читает файл как ANSI.
instruction changes apply to new processes/sessions
файл можно изменить в текущей
новым процессам/сессиям
перебором нескольких неудачных вариантов
