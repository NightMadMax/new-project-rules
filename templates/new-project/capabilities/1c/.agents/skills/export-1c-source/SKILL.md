---
name: export-1c-source
description: Готовит XML-выгрузку конфигурации во временном каталоге вне репозитория для инструментов, которые не умеют работать с EDT-форматом, и возвращает изменения в канон отдельным явным шагом. Использовать только когда задача не решается EDT-маршрутом; не использовать для обычной работы с метаданными, модулями и валидацией.
---

# Выгрузка в XML и возврат в канон

Скрипты стандарта живут в чекауте `new-project-rules`, а не в проекте, и путь к
нему в проекте не записан. Первый шаг — спросить у пользователя путь к чекауту.

Канон репозитория — EDT-формат. Часть инструментов говорит только на выгрузке
конфигуратора, поэтому дерево конвертируется для них и возвращается обратно.

## Прежде чем начать

1. Назвать, **почему** задача не решается EDT-маршрутом. Не «так привычнее», а
   что конкретно инструмент делает и чего нет в EDT.
2. Предупредить о цене: на больших конфигурациях конвертация занимает заметное
   время и место на диске, обратное преобразование может терять нюансы, и
   возврат — отдельный шаг с показом diff.
3. Получить подтверждение. Молча этот путь не выбирается.
4. Проверить `source_format` в реестре баз: для `designer-xml` конвертация не
   нужна, инструменты работают с деревом напрямую.

## Порядок

1. Выгрузка: `./scripts/export-1c-source.sh --root <проект> --base <project_id>/<environment_id>`
   Каталог создаётся в системном временном каталоге, вне репозитория, и
   передаётся инструментам абсолютным путём. Выгрузка физически не может
   попасть в `git add`.
2. Инструмент работает с этим каталогом в своей исходной логике.
3. Возврат: та же команда с `--import`. Если инструмент ничего не менял, так и
   будет сказано. Если менял — показывается список изменений по файлам.
4. Показать список пользователю и получить подтверждение. Только после этого
   `--import --apply`.
5. Освободить: `--release`. Каталог и блокировка удаляются.

## Отказы, а не обходные пути

- **Конвертация недетерминирована** — одно дерево дало разные байты. Это
  остановка: без детерминизма diff возврата перестаёт что-либо значить.
- **Канон изменился, пока выгрузка была снаружи** — возврат отклоняется. Иначе
  он затёр бы работу, сделанную за это время. Выбор пользователя: отказаться от
  выгрузки или повторить её на новом каноне.
- **Конвертер EDT не найден** — `SKIP`, а не ошибка: маршрут недоступен, проект
  не сломан.
- **Другая конвертация уже идёт** — отказ. Две выгрузки одного дерева
  передали бы инструменту наполовину записанный каталог.

Остаток от прерванного запуска удаляется следующим запуском.
